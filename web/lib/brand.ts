/**
 * Product / application name.
 *
 * Shown only where the clinic's own name is not available: the browser tab title,
 * the sign-in landing (before any clinic is known), and as the fallback when a clinic
 * has no saved name yet (first-run / setup). Clinic-name surfaces (sidebar, header)
 * should prefer the clinic's own name from settings and fall back to this constant.
 */
export const PRODUCT_NAME = "Gestion Clinique"
