# web/components/ — Feature Components & UI Primitives

Feature/screen components for the clinic frontend; `components/ui/` holds shadcn/ui primitives. All feature components are client components. Most fetch through `lib/api/*`, surface errors via `sonner`/`lib/errors`, and subscribe to `useClinicRealtime` for live refresh. Money is French/Tunisian via `lib/format` (`formatDT`, `formatDateFr`).

## Chrome & navigation

| File | What it renders / does |
|------|------------------------|
| `dashboard-sidebar.tsx` | Left nav: Tableau de bord, Rendez-vous, Patients, Types de procédures, Dossiers médicaux, Documents, Factures, Créances, Plans de traitement, Fichiers, Stock, Relances, Liste d'attente, Laboratoire, Caisse, Mon profil, Paramètres. Any-admin adds **Médicaments / Actes dentaires** *and* **Utilisateurs** — the latter used to carry an extra `mode === "local" &&`, which was the *only* thing hiding it in Cloud (the page checks role, and `UsersController` works in both modes), so a Cloud admin could not see or revoke who reached their patient data. Footer shows the clinic's working-hours summary (`summarizeWorkingHours`); header brand = clinic name or `PRODUCT_NAME`. **Renders twice**: the desktop rail (`hidden md:flex`, collapsible via `useSidebar()`) and, below `md:`, a slide-over **`Sheet`** drawer driven by `useSidebar().isMobileOpen` — closed by default, opened only from the header's `Menu` control, and closed by the provider on every `pathname` change (§ 7.1). One `renderItem(item, collapsed)` serves both; the drawer always passes `collapsed: false` (there is room, and a phone has no hover for the collapsed tooltips), so the **persisted desktop collapse preference is never touched by the drawer**. |
| `dashboard-header.tsx` | Top bar: the mobile nav opener (`md:hidden`, the drawer's only trigger), live patient **search** (debounced `patientsApi.list` → navigate), `<ConnectivityIndicator/>` (Local), notification **bell + `<NotificationPanel/>`** (`useNotifications`, `99+` cap, deep-links + mark-read), user menu via `useSession()` (logout; local adds "Changer le mot de passe"). Reflows below `md:` — tighter padding/gaps and the name/email block drops to the menu's own header, so the row fits 375 px. Mounts `<PostVisitReviewPopup/>`. |
| `connectivity-indicator.tsx` | 3-state header badge (server-unreachable / no-internet / online) from `useConnectivity()`. ⚠️ **Gated on what the deployment can *say*, not on its auth mode** (P7, AC-62/AC-63): an unreachable server surfaces on **every** deployment, the two egress states only where the server publishes a reading (`egressSignalAvailable`), and a healthy server with no probe renders **nothing** — which is what keeps the Cloud header unchanged in the happy path. It used to return null unless `isLocal`, a flag derived from `AUTH_MODE`, which reads `local` on the hosted backend too — so it claimed three states there while the probe behind them 404s. |
| `clinic-guard.tsx` | Route gate via `useClinicAccess`/`useAuthToken`: loading → children if member → `unauthorized-page` if not → a distinct retry screen on transient errors → redirect to `/auth/login` if unauthenticated. Skips setup/join/login. Wraps protected pages. |
| `unauthorized-page.tsx` | "Accès restreint" screen when the user has no clinic. |
| `session-lock-gate.tsx` | **What a native shell shows instead of logging out at the 30-minute inactivity limit** (`mobile-native-shells` P7, AC-57…AC-60). An opaque « Session verrouillée » takeover over the **still-mounted** app, prompting `window.__clinicShell.confirmIdentity()` on mount and on each « Déverrouiller ». ⚠️ **Opaque is not decoration**: the limit exists so a phone left on a counter stops showing a patient's record, and awaiting the OS prompt over a visible page would keep it there for anyone who dismisses it. ⚠️ **Three attempts and a dismissal counts as one** — the session cookie is deliberately *not* cleared on the success path (AC-57), so the counter is the only bound on how long a live cookie sits behind a client-side overlay. `unavailable` (no enrolled biometric, no device credential, API < 28) falls through immediately: no dead control, no error (AC-60). Exports **`canConfirmIdentityInShell()`**, the feature detection `LocalSessionProvider` branches on — `false` in every browser, which is what makes AC-58 (« absent bridge ⇒ inactivity path unchanged ») true by construction. |
| `dashboard/kpi-card.tsx` | One dashboard figure: label/value/icon/description plus a **delta badge** against the previous period. **Always** a `Link` — that is the point, since the retired `stats-card.tsx` made `href` optional and four of seven cards landed on an unfiltered list. `DeltaSense` (`up-is-good`/`up-is-bad`/`neutral`) drives the delta's colour, because « Dépenses +18 % » and « Encaissé +18 % » are the same arrow and opposite news. Three distinct delta states, never conflated: a real percentage, « Pas de comparaison » (no baseline / undefined ratio), and nothing at all for a point-in-time figure. ⚠️ `KpiEmphasis` is **`lead` / `default` / `compact`**: `lead` is the answer its section is asked for, at `text-2xl sm:text-3xl`, and it replaced a `hero` that delegated to a filled accent panel (`hero-kpi.tsx`, **deleted** — its only caller was `netCard`). It sits in its own one-cell `KpiGrid` above the subordinate figures rather than spanning cells inside theirs, because a `text-3xl` money figure does not fit half of a 320 px grid. ⚠️ The lead needs **`items-start`**: a flex column stretches its children, so `DeltaBadge`'s `inline-flex` pill was painted the full width of the cell. ⚠️ That pill also carries **`max-w-full`** with a shrinkable label: « Pas de comparaison » is the one delta state whose text is a phrase rather than a short signed number, and at 320 px it measured ~123 px against a ~112 px cell — painted out over the hairline and into its neighbour. It wraps there; truncating it is not an option, since a missing baseline is exactly the case the reader needs told plainly. |
| `dashboard/kpi-grid.tsx` | The bordered surface those figures share — `gap-px` hairlines over a `bg-border` container, each child painting its own `bg-card`, never `divide-*` (which draws from DOM order and mis-aligns across a wrap). ⚠️ **It pads its last row with `aria-hidden` filler cells**, because the same technique makes an *unfilled* area show the container's `bg-border` as a grey block — and the figure count is user-controlled (« Facturé » and « Avoirs remboursés » are hidden by default), so an odd count is the normal case. Padding is to a multiple of the `xl` count: exact for 1, 2 and 4, and still one short at the base width for `columns={3}` (`/factures`' case, pre-existing). ⚠️ On the dashboard the subordinate grid takes **`auto-rows-fr xl:flex-1`** inside a `flex-col` column: both halves of « L'activité » / « L'argent » are grid items and already stretch, but the left one's own children sized to their content, so the stretched box was empty at the bottom and the two sides read as unequal cards beside a chart whose height is a *dynamic* list. `auto-rows-fr` is what sends the extra height to the CELLS rather than to the `bg-border` container showing through under them. |
| `dashboard/dashboard-section.tsx` | **The one section-heading primitive** — mono uppercase eyebrow + accent dot + hairline, with its own loading / « Indisponible » + Réessayer / real states (the `/factures` `RevenueValue` pattern). A refetch **holds the previous render** at reduced opacity — switching period must not blank the page or shift the layout. ⚠️ It absorbed `app/page.tsx`'s private `SectionBar`, which drew the same band for the day zones because this component knew only about a `hint`: the `href`/`action` pair and the **`control`** slot (période track, praticien filter) are that merge. `children` is optional — a header alone is a legitimate use. |
| `dashboard/period-selector.tsx` | Aujourd'hui / Cette semaine / Ce mois, in the bilan band's `control` slot — deliberately not per-section, so two cards can never describe different windows side by side, and deliberately not at page level, where it would appear to govern the day board. ⚠️ **Full-width and 3-up below `sm:`**, with short visible labels (« Jour / Semaine / Mois ») and the full French kept as each button's `aria-label`: the three long labels measure ~355 px against 288 px of content box at 320 px, so as an `inline-flex` the track overflowed the page. |
| `dashboard/collected-trend-chart.tsx` | **The repo's first chart** (`recharts`, already a dependency). Single-series area: `var(--chart-1)` (validated ≥3:1 on both the light and dark card surfaces), 2px line, ~10–16% wash, hairline **solid** gridlines, an 8px active dot with a 2px surface ring, the value direct-labelled **only** at the endpoint, and a crosshair tooltip whose value leads and label follows. No legend (one series — the title names it). A **table view** toggle keeps every value reachable without hovering. |
| `dashboard/appointment-status-chart.tsx` | **« Rendez-vous par statut »** — stacked columns over a window the **card itself owns** (Cette semaine / Ce mois / Personnalisé, in its own header). ⚠️ **The only block on the dashboard whose period is not the page's**, which is why it states its window in words (« par jour · du 17 au 23 août ») and why it has its own endpoint and its own hook: a card that can be re-scoped alone cannot be served by a response whose window is decided elsewhere. Its « comparé à » names **days** (« aux 7 jours précédents »), never « le mois dernier » — the server compares the same *number of days* immediately before, which for August is not July. ⚠️ **Five classes, not seven**, folded server-side (`AppointmentStatusClass`): six-class variants were *measured* and fail — two azur steps sit at ΔE 12.6 in normal vision (floor 15), azur + violet collapse to **ΔE 2.1** under deuteranopia. The Planifié/Confirmé distinction comes back as the legend's « dont N confirmés ». ⚠️ **Pastel fills, all below 3:1 against the card, and that is admitted rather than accidental** — legal only because the three reliefs ship with them: a same-hue `-edge` hairline per segment (a custom `shape`, since a plain `<Bar>` carries one stroke and a pale fill needs *both* the 2 px surface gap and the edge), a legend that **names and counts** all five, and a table view. Remove any one and the palette is a defect. ⚠️ **`min-w-0` on the card, the content and the plot wrapper is load-bearing**: a grid item's default `min-width: auto` refuses to shrink below its content, and the scroll track declares 31 × 16 px of it — without the release the card overflowed a 390 px viewport and the **page** scrolled sideways. ⚠️ The legend's last row is `sm:col-span-2` because five items in a two-column **hairline** grid leaves a sixth cell painting `bg-border` as a grey slab (`kpi-grid`'s documented trap, one card over). Column width is clamped both ways — 12 px floor (below it a stack is noise, so the plot **scrolls in its own container** per § 11 rather than coarsening the buckets, which would remove a capability) and 34 px ceiling (7 buckets across a desktop card is otherwise a saturated slab). |
| `dashboard/appointment-trend-chart.tsx` | **« Rendez-vous — 6 derniers mois »** — the deliberate twin of `collected-trend-chart.tsx`: same 2.4 px line, same ~26 % wash, same hairline grid, same crosshair, same table view, and `--chart-1` rather than any `--appt-*` pastel (this counts every séance whatever its outcome, so painting it in « À venir »'s azur would say the two mean the same thing). ⚠️ **The one thing it does that the money trend does not**: it says when the last month is **incomplete**, with a hollow ring and « mois en cours, pas encore complet ». On the 3rd of a month that point holds three days beside five whole ones and draws a cliff that is not in the data — the money trend has the same latent defect. `isPartial` is the server's, since only it knows the clinic's today. A Tous / Honorés toggle over one read (both measures are on the wire), on « Répartition des actes »' Durée/Nombre precedent. |
| `dashboard/dashboard-labels.ts` | French labels/descriptions per KPI key + period labels + `formatMonthShort`/`formatMonthLong` — display-time mapping over the English wire keys, the standing convention. |

## Notifications

| File | What it renders / does |
|------|------------------------|
| `notification-panel.tsx` | Presentational feed popover (props-driven): 50 newest rows with category icon, title, message, relative French time (`n.createdAt`), unread styling, empty/loading/error states, "Tout marquer comme lu", row deep-link. `CATEGORY_ICON` covers all **ten** `NotificationCategory` values — **`SubscriptionExpiring`** uses `CreditCard` (`clinic-subscription` Part E: what it asks for is a payment, and it is the only row about money owed to the software *vendor* rather than about the practice's own work) and takes the amber `-wash`/`-ink` tone, deliberately **not** red even on the last day, since nothing has gone wrong and the record is never at risk. `dashboard-header.tsx`'s `handleNotificationClick` carries the matching `targetKind === "Subscription"` → `/abonnement` branch, without which the four rows would badge the bell and go nowhere — `StockExpiringSoon` uses `Hourglass`, deliberately distinct from `LowStock`'s `AlertTriangle`: low stock means « order more », expiry means « use it or lose it », and both can land for the same item. |
| `post-visit-review-popup.tsx` | Modal prompting staff to record a finished visit. Polls `notificationsApi.pendingReviews` (60s) + realtime; "Ajouter le dossier médical" → `/documents?appointmentId=`; "Plus tard" snoozes client-side (localStorage). Mounted once in the header. |

## Appointments

| File | What it renders / does |
|------|------------------------|
| `appointment-list.tsx` | Dashboard "Rendez-vous du jour" — API-wired via `useAppointments`; hides Cancelled/NoShow. |
| `appointment-calendar.tsx` | Day/week/**month** calendar; optional `doctorId` filter + show-cancelled/completed toggles. Per-card "non synchronisé" badge + "Push to Google" gated on `useConnectivity().internetReachable`; push → `googleCalendarApi.syncAppointment`, `onChanged` refetch. **Since the agenda density pass it owns the whole desktop/tablet agenda bar in ONE row** — nav, date + window total, Aujourd'hui, the view switch, the praticien Select (visible from `xl:`, inside « Filtres » below it), a « Filtres » popover holding the two status switches *and* the legend, a **state-aware Google Agenda control**, a « ⋯ » popover holding Exporter, and « Nouveau ». ⚠️ **Google is a visible slot in the bar, not a « ⋯ » menu item.** Buried there it put the whole feature two clicks behind an unlabelled glyph and stated the connection **nowhere on screen**, so a practice that had never connected could not discover it existed. Not connected → a primary-tinted outline « Connecter Google » that connects **directly** (a popover in front of a one-action state is a click that buys nothing); connected → « Google Agenda » with a `CalendarCheck` in `--success`, opening Importer / Déconnecter — those stay one press deep because a destructive-looking « Déconnecter » at navigation weight, on a screen opened forty times a day, for a connection made once in the life of the practice, is what the density pass removed. Label from `xl:`, icon-only below, `aria-label`led at every width, and it is gone from « ⋯ » so the feature has exactly one home. ⚠️ **The view switch's active pill was `bg-background` — the tinted page ground (`0.977`) — on a `--card` (pure white) bar**, i.e. painted *darker* than the surface behind it and 1.3 % from its own `bg-muted` track: three flat words with no control around them. It is `bg-card` + `ring-1 ring-primary/30` + a bordered track now (`agenda-phone-header.tsx`'s twin was already on `bg-card` and took the ring and border to match). It used to be four rows across two files (two here, two in `app/appointments/page.tsx`), ≈ 215 px, which left a ~910 px window showing five hours of an eleven-hour day and put an administrative Google row between the date and the view switch. The page's four controls now arrive as props (`onNewAppointment`, `doctorFilter`, `googleControls`) because this component already owns everything that bar is made of: the export window, the appointment index and the view-aware date arithmetic. ⚠️ The row is `flex-nowrap` and the **title absorbs the slack** (`min-w-0 flex-1 truncate`) — that is what makes 820 px safe, where the 256 px rail leaves ~532 px and a wrap would be a third row on the device this product is used on most. |
| ↳ **the block's two colour axes** | **The act owns the surface and the left rail; the statut owns a 3 px strip on the RIGHT edge** (`STATUS_EDGE_PAINT` → `renderStatusEdge`), on the grid blocks *and* the month chips. Before this the act's hue was mixed at a hand-typed **12 %** — half the `--act-tint` (22 % light / 36 % dark) the dashboard paints the same catalogue colour at — so two different acts were the same near-white rectangle at arm's length; it now reads the token, which is also what gets dark mode right (the dark value is *stronger*, not inverted). ⚠️ **`pending` and `accepted` used to render pixel-identically**: the switch had no case for either, so « le patient a confirmé » — the one distinction a desk reads an agenda for, and one `appointment-labels.ts` documents — was unrepresentable. The strip is dashed grey for Planifié and solid primary for Confirmé. ⚠️ **The right edge is the only place that survives a 12 px block** (a quarter-hour visit at the gate-locked `HOUR_HEIGHT` of 48), which is why it is a strip and not a pip, an icon or a word. ⚠️ `negative`/`positive` no longer **overwrite** the rail with red/green — that erased the act, so « who didn't turn up, and for what? » lost its second half; every treatment left in the switch composes with the hue instead (dashed rail, inset ring, strike). ⚠️ A **« créneau occupé » returns early and gets neither a statut treatment nor a strip**: `AppointmentProgressJob` marks any started slot `InProgress`, so a reserved hour was picking up the amber ring and rendering as the loudest thing in the week — `f441d1d`'s line, one surface over. ⚠️ The strip is a **child element, not an `inset` box-shadow**: `box-shadow` is already spoken for by `hover:shadow-md` and `ring-2 ring-inset`, and an *inline* `boxShadow` beats a class in every state, so composing it that way would have silently deleted both. |
| ↳ **the legend** | Names **both** axes now, from one `renderLegend()` mounted twice (the desktop « Filtres » popover, the phone `<details>`) — they were two hand-written copies. ⚠️ The statut swatches **are** `STATUS_EDGE_PAINT`, the real paint, and `LEGEND_ITEMS` is **derived from `APPOINTMENT_STATUSES` × `APPOINTMENT_STATUS_TONE`**, one row per *paint* rather than per status: the old hardcoded list named five of six statuses, omitted « Confirmé », and gave « Planifié » a solid-blue swatch the grid has never drawn. Six tones over an open status set means two statuses may share a strip, so a shared row names **both** (« Planifié / Séance passée ») instead of teaching one as the meaning of a colour the other also wears. ⚠️ The **act** half is derived from the visible window (`actLegend`, keyed on the *lead* act because `procedureColorHex` is the lead act's colour), never a table kept here: `ProcedureType.ColorHex` is clinic-editable, so a second copy would be stale the first time somebody recoloured « Détartrage ». This is the second pass `appointmentAppearance`'s own note deferred. |
| ↳ **the grid's ground** | Three marks, all through tokens in `globals.css` (`--agenda-today` / `--agenda-guide` / `--agenda-closed`), because each needs a *different strength* in dark rather than an inversion. **Today's band** runs header-to-last-hour off one token, so the two halves cannot drift. A **half-hour guide** line sits at 50 % of every hour cell — a 30-minute visit is the default booking, so half the appointments in the book started on a line the ruler did not draw. **Closed hours are a diagonal hatch**, replacing `bg-muted/40 opacity-70`: the `opacity` faded the cell's *own gridlines* with the fill, so a closed column lost its structure and read as a rendering fault. ⚠️ The band and the shading were previously two `background-color` utilities on one element, so a closed hour **of today** was settled by stylesheet order rather than intent; "closed" is a `background-image` now, so they compose. ⚠️ The band stays a **class**, not an inline style — an inline style beats a class at `:hover` too, and painting it inline would remove `hover:bg-accent/30` from the column most likely to be clicked. |
| ↳ **`gridWindow`** | **The time grid renders the clinic's open hours, not 0–23.** The old grid rendered all twenty-four rows and merely *shaded* the closed ones, so two thirds of the scroll was dead travel and — since the initial scroll centres on "now" — the morning sat permanently above the fold. ⚠️ **It is a union, not a filter** (§ 0): the window covers the configured hours **and every appointment booked in the visible days**, so a 06:30 emergency or a visit running past closing *extends* the grid instead of vanishing from it; a « Afficher les 24 heures » disclosure covers booking *into* a closed hour. ⚠️ `gridWindow.fromHour * 60` is subtracted in exactly two places — `renderAppointmentBlock`'s `top` and the scroll effect's fallback — and **nowhere else**: the current-time line asks the DOM for its row's `offsetTop`, so it re-bases for free. ⚠️ **The current hour is part of the union, and it is load-bearing, not tidiness.** The line reads its position off the DOM row for the current hour, so trimming made that row *conditional* — this clinic opens 09:00–17:00, so from 17:00 every evening the query found nothing and the `else setCurrentTimePosition(null)` that a trimmed grid genuinely requires **deleted the « maintenant » line**. `nowHour`/`nowHour + 1` widen the window whenever today is on screen, so the line always has a row to sit in. ⚠️ And the two effects that key off the window take **`gridWindow.fromHour` / `.toHour`, never the object**: « maintenant » rebuilds it every time the wall-clock hour turns, so an object dependency would fire a `scrollTo` on the hour, every hour, dragging the agenda off whatever the user was reading. The line itself spans the **full grid width**, gutter to right edge, as it always has — the density pass scoped it to today's column and that was reverted on request. The block also drops the `NNm` badge (its **height** states the duration — the phone branch always said so) for the start time + act name, today's **column** is tinted rather than just its date pill, and each weekday header carries its own RDV count (« fermé » on a closed day, never « 0 »). |
| ↳ **`agenda-grid-drag.ts`** | **The two pointer gestures a calendar is expected to have** (`agenda-grid-gestures`): drag across empty hours to book a span, drag a block to move it. `useAgendaGridDrag` owns the gesture and nothing else — no fetching, no geometry constants, no rendering — so the hour window, the phone's taller rows and the week's column bands stay in the calendar. ⚠️ **Targets resolve through `elementFromPoint` + `dataset`, not per-cell handlers**: a week grid has 168 cells and a move may cross day columns, and it is the only shape that also answers « the pointer is over no cell at all » — the release-outside case, which must **cancel** rather than reuse the last cell. That makes the attribute names a contract between two files no type can see, so `check:responsive`'s **`agenda-gestures`** derives the required set from the hook's own `dataset` reads *and* asserts every cell calling `beginCellGesture` also spreads `cellDataProps`. ⚠️ **Touch arms on a ~350 ms long press, a mouse on 4 px of movement** — the same container scrolls 24 hours *and* hosts the Jour day-swipe, so a gesture starting on contact would take the thumb from both; past 10 px before the timer the touch is a scroll and the gesture is abandoned outright, never re-armed. Once armed, a **non-passive `touchmove` `preventDefault`** is the only thing that stops the day scrolling — setting `touch-action` mid-gesture is ignored by every engine, so the class-based version compiles, reads correctly and does nothing. ⚠️ It deliberately does **not** claim the plain tap on a block: the phone block is a real `<button>`, so Enter/Space produce a `click` and no pointer events at all, and the block keeps its own `onClick` guarded by `didConsumeGesture()` — which the day-swipe reads too, since `touchend` fires *after* `pointerup`. ⚠️ `select-none` on the scroller is **unconditional**: the browser anchors a text selection on `mousedown`, before any movement has said this is a drag, so gating it on the armed state lands the class after the smear already exists (measured, not assumed). |
| ↳ the grid's **empty state** | « Aucun rendez-vous cette semaine » is a quiet line in the **footer strip**, not the `inset-0` card it used to be. That card read as a modal, sat on top of the hours the user was trying to click, and — once the grid also answered to a drag across those hours — covered the whole gesture. The strip therefore renders whenever the range is empty, not only when the hour window is trimmed, and is suppressed while `loading` **or** `error` is set, so a failed fetch is never reported as a free day. The overlay's own « Nouveau rendez-vous » button went with it: the action already has two homes (the agenda bar, and the phone's floating action). |
| `appointment-acts-picker.tsx` | **« Actes du rendez-vous »** — the séance's act list, shared by both booking dialogs. Replaces the single « Type d'acte » Select each carried, which is why the second and third act of a visit could only be typed into the notes. Searchable Popover+`Command` (a clinic's catalogue is too long for a Select) with **one `CommandGroup` per clinical discipline** (`procedure-categories.ts`) — `cmdk` hides a fully-filtered group, so typing collapses this back to a flat ranked list for free. ⚠️ The discipline is joined into each item's `value`, since cmdk matches on `value` alone: without it the headings would be searchable to the eye but not to the keyboard. « Acte personnalisé… » sits in its **own** group, so creating an act is not filed under whichever discipline happens to come last. Removable rows, a live `n actes · N min` badge, and the inline « acte personnalisé » creation — which only the *create* dialog had, so sharing gave it to both. A picked act stays **visible and ticked** rather than filtered out (vanishing reads as "it failed"), a row whose procedure left the catalogue is **kept and marked** (silently dropping it would delete an act from a booked visit), and `totalActsDuration` is the one implementation of the summed length. **Since `appointment-negotiated-price` every act row carries « Prix pour ce rendez-vous »** — prefilled with the catalogue tarif, `inputMode="decimal"` through `parseAmountInput` (a `type="number"` refuses the comma this product prints money with), with « remettre au tarif » once touched. ⚠️ It is its **own line** under the identity row, not another cell on it: that row already wraps rather than truncates at 390 px because the act's name IS the row's identity, and a ~7 rem money field beside it would take the name back below the width that made it readable. ⚠️ `agreedCost` is `undefined` until typed and the field merely *displays* the tarif — so a booking nobody negotiated sends nothing and stays at whatever the tarif is on the day the fiche is filled. ⚠️ The panel's other money field is now labelled « **Tarif au catalogue** » because it sets a `ProcedureType`'s `defaultCost` **permanently**, and two money inputs a thumb's width apart — one local, one forever — is a mistake nobody would notice making. `agreedCostOf` / `hasInvalidAgreedCost` / `negotiatedTotalOf` / `toProcedurePayloads` are exported here so both dialogs cannot disagree: one that assembled `procedures` inline would silently restore every act to its tarif, since the server replaces the whole list. |
| `appointment-recap.tsx` | **« Le rendez-vous tel qu'il sera »** — the live recapitulation both booking dialogs carry, as a sticky right-hand **pane** from `lg:` (`variant="rail"`) and as the **strip** between the form and the footer below it (`variant="bar"`). It exists for two defects at once: the dialogs had **no summary of any kind**, so on an agenda several people fill there was no moment at which « Amine Trabelsi · vendredi 14 août · 10:00 → 11:10 · 2 actes » could be checked before committing; and the overlap warning lived inside the form behind a **reserved `min-h-[2.5rem]` slot** that was empty most of the time — a permanent ~60 px hole under the duration presets that read as a rendering fault at every width. Giving the clash a permanent home is what let that reservation go. ⚠️ **Every field is derived from the form beside it — no fetch, nothing invented.** That is what makes it safe to collapse; a pane holding a fact available nowhere else would be a capability removed by a layout decision, so anything *actionable* (the statut buttons, « Facturer ») stays in the form column and only *statements* go in the pane (`AppointmentRecapSection`). ⚠️ **`kind` is separate from `patientName`**: reading a null name as « créneau occupé » made the pane assert a block on every freshly-opened form while the control above it said « Patient » — two opposite facts with one rendering. A visit with no patient *yet* reads « Patient à choisir ». ⚠️ **« Prix convenu » appears only when an act was negotiated** (`negotiatedTotal`, null otherwise): money is now part of what is being committed, but a figure stated on every ordinary booking is a figure nobody reads by the second week — and it is in **both** renderings, since below `lg:` the `bar` *is* the récapitulatif and the phone is where the call that sets the price is taken. ⚠️ The « à l'enregistrement » block (rappel SMS, Google, fiche proposée) is **deliberately absent**: those depend on `ClinicReminderSettings` and the Google connection, neither of which this dialog reads, and a pane that names a reminder an unconfigured clinic will never send is the stale-figure defect in a new place. |
| `ui/mode-segmented.tsx` | A two-or-three-way **mutually exclusive** choice as one control. It replaced the two independent `Switch`es stacked at the right edge of the create dialog's Patient card — « Créneau occupé » and « Nouveau patient » are not independent, they are one question (*what is being booked, and for whom*) with three answers, and two toggles is the one shape that cannot say so; a user had no way to tell that turning the first on made the second meaningless, which the code knew perfectly well (it silently cleared the patient, the name fields and every act). A real `role="radiogroup"`, so the exclusivity is announced and arrow-navigable. ⚠️ **The active option is `bg-card` + `ring-1 ring-primary/30`, never `bg-background`** — the agenda view switch's documented lesson one control over: a dialog paints on `--background` (0.977), so an active pill of that token *is* the surface behind it, and `--muted` is 1.3 % away, so a `/40` track is no track. Same class string as `appointment-calendar.tsx`'s switch on purpose. ⚠️ Touch floor is **grown** (`min-h-9 coarse:min-h-11`), never `.touch-target` — the options are siblings a few pixels apart and an overlay would steal its neighbour's taps. |
| `create-appointment-dialog.tsx` | Create RDV (patient, **several acts**, doctor, date/time, duration). ⚠️ **Re-laid-out by `rdv-modal-redesign`: one column in decision order, plus the récapitulatif pane.** The four equal-weight `bg-muted/30` cards are gone (they said nothing about which decision mattered and cost ~120 px), the two switches are one `ModeSegmented` in the header, the **acts moved above the practitioner** (they decide the duration, the agenda colour and the fiche proposal; the practitioner decides none of those and is pre-filled in a single-chair cabinet), Notes folded behind a disclosure, and the subtitle is `sr-only` — measured, that one redundant line was the difference between a form that fits and a form that scrolls. Result at 1440×900: **951 px → 586 px, no scrolling**; one thumb-push on a phone. ⚠️ **A summed duration that is not a preset now gets its own active chip** — before, acts summing to 70 min highlighted *nothing* while a badge elsewhere said otherwise. ⚠️ **The `outline` variant is `bg-background`, i.e. the exact value this dialog paints on**, so the date trigger, the patient combobox and the unselected presets each carry `bg-card`: without a card behind them they read as transparent, which is what removing the cards exposed. | Advisory `useAppointmentOverlap` warning; phone validation. ⚠️ **`performCreate` is re-entered by design and must therefore be idempotent about the patient** — the past-time, slot-taken and duplicate-patient confirmations all re-submit it. It used to call `patientsApi.create` on every pass, so one « créer quand même » on a taken slot put the new patient on `/patients` **twice**; `createdPatientIdRef` now holds the committed id and the retry books onto it. The three overrides travel as one **merged** `CreateOverrides` object (`hours`/`overlap`/`duplicatePatient`) rather than positional booleans, because a booking can trip all three and each confirmation must not un-grant the last; a fresh `handleSubmit` resets them, the created id survives until the dialog closes. Once that id exists the new-patient fields and the « Nouveau patient » switch go **read-only** behind a `role="status"` note — the record is committed and this form cannot reach it, so letting the name drift would silently produce the second patient the ref exists to prevent. The « Ce patient existe peut-être déjà » prompt is the server's `patient_duplicate` refusal shown verbatim; its **destructive** action is « Créer quand même » and its cancel switches back to the patient picker (a duplicate cannot be merged — closing costs nothing). Takes **`presetPlanActs: PresetPlanAct[]`** (was a single `presetPlanItemId`): one entry books a step, several book them *together*, each keeping its own devis link. A plan act whose procedure does not resolve is seeded as a **link-only** row, not dropped. Duration follows the acts' sum until the user sets it by hand (`durationTouched`) — or until a caller supplies **`defaultDurationMinutes`**, which arrives with `durationTouched` already **true**: a span dragged across the agenda's hours is as explicit a statement about length as typing the number, so the first act picked must not silently replace it with the catalogue's 30 minutes. The page clears it on every non-drag door into the dialog, since one long-lived instance would otherwise carry the last drag's length into the next click-booked visit. |
| `edit-appointment-dialog.tsx` | Edit/cancel/complete an RDV (confirm AlertDialog); overlap warning; the same acts picker. ⚠️ **Same redesign as its sibling**, plus three of its own. (a) **The patient is an identity in the header**, not a card containing one *disabled* input and the sentence « Le nom du patient ne peut pas être modifié » — that is a fact about the record, not a field, and spending a card on it pushed everything editable further down. (b) **The header's statut Badge is gone: the selected button is the badge.** Two live statements of one value in one box is how they disagree; the pane states the *saved* statut, which is a different claim. (c) **« Annuler le rendez-vous » left the footer for the « ⋯ » menu** — it was a *second door* to a state the statut buttons at the top already offer (« Annulé » is one of them), so the dialog presented one outcome twice, once in red where the thumb lands. On a phone the footer was three stacked full-width buttons (~150 px of 844) with the destructive-looking one nearest the thumb **and under the assistant's launcher**; it is two rows now, and the `⋯` carries `order-2` below `sm:` so the launcher cannot cover the only path to cancelling. 1329 px → ~680 px on a two-act visit, which still scrolls: an edit form legitimately grows with its acts, and the decision it is opened for (the statut) is the first thing on screen. | Hydrates from `procedures`, falling back to the lead-act scalar for an older response — without that fallback the first save would persist an empty list over a real act. `durationTouched` starts **true** on hydration: a booked visit's stored length is already someone's decision, so merely opening it must not relengthen it. |

## Patients, records & odontogram

| File | What it renders / does |
|------|------------------------|
| `patients-table.tsx` | Patients list (`patientsApi`) with debounced search + flag filter + out-of-order guard; opens `EditPatientDialog` / `PatientSummaryModal`. Carries the shared **`WhatsAppAction`** in both trees, on the fournisseurs terms: **visible on the card and in the row, never inside the « ⋯ » menu**, and a patient with no deliverable number gets « Ajouter un numéro » (which opens the edit dialog) rather than a disabled or absent control. ⚠️ Deliverability is `PatientDto.phoneE164`, resolved **server-side** through `PhoneNumber.ToE164` — never re-derived here, or a patient becomes contactable on one screen and not another. ⚠️ The desktop row's four icon controls each carry `Button`'s 44 px `touch-target` overlay 8 px apart, so they grow together (`coarse:size-11`) instead: an overlay that overhangs its neighbour steals its taps, and the later sibling paints last. |
| `patients/import-patients-dialog.tsx` | **« Importer des patients » (L5)** — CSV → column mapping → **dry-run preview** → commit, in one `mobile="sheet"` dialog. Three steps and one `File` in one component, deliberately: nothing is staged server-side, so the browser holds the file across the steps and the commit must be able to send the *same* file + mapping the preview ran on. ⚠️ **Every mapping change re-runs the dry run on the server** — the client never re-derives outcomes locally, because duplicate matching needs the clinic's whole patient list and the validation is the server's own; a local approximation would be a second answer to « what will this import do ». Duplicates are **unticked by default** with the reason stated (two fiches for one person cannot be merged), the button carries the count it will create, and the result step lists **every** line, not only the failures. Gated on `isAdminOrDoctor` (presentation only — both endpoints are `AdminOrDoctor`). |
| `edit-patient-dialog.tsx` | Create/edit patient (demographics, address, insurance, CNAM identity, flags, medical/family history); Tunisian phone validation. In **create** mode a `patient_duplicate` refusal opens « Ce patient existe peut-être déjà » instead of the error toast — a question, not a failure — with « Créer quand même » as the *destructive* action and a cancel that leaves every field as typed. `handleSave` splits into the event handler (which resets `allowDuplicateRef`, so correcting a name re-asks) and `savePatient()`, which the confirmation can call directly; the grant is a **ref** because the confirmation re-submits in the same tick a state update would miss. **« Notes du patient » is the first section** and « Adressé par » the first field: both are read on every visit and were previously entered below CNAM and insurance, i.e. last. The important-notes box carries the same amber styling as the panel that displays it, so the box you type into looks like the box you read. Sends `notes`/`importantNotes` **always present (possibly `""`)** on update, since omitting a key means "unchanged" server-side and emptying the box would silently do nothing. |
| `patient/record-acts-summary.tsx` | A saved fiche's acts, **each with its own teeth**. It exists because the patient page read a séance back as two independent columns — a « Type d'acte » holding the server's comma-joined summary of every act and a « Dents » holding the flat UNION of every act's teeth — so a two-act séance printed both names beside all five teeth and said nothing about which belonged to which; the likeliest reading is that both acts were done on all five. The two columns are now one (« Actes »), and the card list's « Dents » field becomes « Actes » above one act. The single-act case is left exactly as it was, with no per-act scaffolding, because that is most fiches and the ambiguity does not exist there. |
| `patient/patient-notes-strip.tsx` | Notes under the patient's name — **one row, two balanced halves**: `Alertes` (amber) on the left, `Notes` on the right. Each half carries both natures of its kind — the patient's own standing facts *and* what each séance recorded — so an important séance note appears in Alertes rather than being sealed away (an alert you have to open is not an alert). Within a half the two are separated by **form, not colour**: a **chip** (or plain paragraph) is undated because it is simply true; a **dated line** names its date *and its act*, since « difficult patient » from yesterday and from two years ago are not the same fact. Both halves render through one internal `NotesPanel`, share one `PANEL_BODY_MAX_PX` and scroll past it — two halves that cap at different heights are not halves — and the header carries `min-h-9` because only one of them holds the « Modifier » button, which would otherwise start its body lower than the other's. ⚠️ This replaced a measured two-line clamp with a « +N autres alertes » toggle (and its `ResizeObserver`): that existed only because the band was full-width and always expanded, and at half width there is room to just show the content. Chips come from `splitPatientWarnings`, which splits the textarea and strips the typed bullet **only when it really is a short list** (≥2 pieces, each ≤48 chars) — prose falls through to a paragraph, never stuffed into a pill. ⚠️ **With BOTH halves empty and the fiches read healthy, the pair collapses to ONE dashed line** (« Aucune alerte ni note pour ce patient. » + « Ajouter »): two bordered panels restating twice that there is nothing to read spent the most valuable band on the page — directly under the patient's name, above the odontogramme, and two thirds of the first phone screen. The condition is *both*, deliberately: « aucune alerte » beside three recorded alerts is worth stating at the same weight as the alerts. `recordsFailed` short-circuits it for the reason `emptyLabel` is already guarded — a failed read also arrives as `records: []`, and « aucune » is a claim it is not entitled to make. Read-only; all fields are written through `edit-patient-dialog`. |
| `patient-record-modal.tsx` | Dental-record entry as **a pile of act cards**, one 780px column read top-to-bottom: alerts → meta → « Actes de la séance » (the cards + « Ajouter un autre acte ») → the full-width `RecordToothChart` → « Notes de séance » → totals footer. ⚠️ **There is no draft and no composer.** Every card is a real act that will be saved, and exactly one is *armed* — the chart writes to that one. `actColors` is the single resolver for an act's colour (rail, tooth chips, chart fill) and **enforces distinctness**, falling through to `ACT_PALETTE` when a catalogue hue is already taken: the chart paints every act at once, so two acts sharing a colour makes « quelles dents pour quel acte ? » unanswerable — which is exactly what painting each act in its *resulting condition*'s colour did, since two acts with no état at all were then identical. Derives `isAdultTeeth` on save (mixed dentition allowed), mirrors « montant payé » to the total until edited, and keeps the plan-item link. |
| `record/act-card.tsx` | **One act, as one card that is always both the record and the editor.** Armed: name, tarif, `/dent ↔ forfait` with the live `unit × n`, the tariff gesture, its own teeth as removable chips, a per-act « Détails » fold and « Changer d'acte ». At rest: one line — name · teeth · montant — that arms the card when tapped. It replaced `act-slot.tsx` + `session-acts-list.tsx`, whose two reported defects had one cause: « Ajouter un autre acte » meant *commit the draft, empty the field, empty the chart*, so the act just entered appeared to vanish (it had gone into a fold shut by default); and the read-back listed a three-tooth act under three « Dent NN » headings, two of them saying « inclus ». Both are unsayable here. ⚠️ `summariseTeeth` keeps a card at rest to one short phrase — « toute la bouche », « toute la mâchoire du haut », « 6 dents » — because 32 numbers on a line meant to be scanned pushes the name out and says less; the exact list stays in the chips, on the chart and in the line's `title`. ⚠️ The `/dent ↔ forfait` pair carries `coarse:h-11` and the chip's « × » `coarse:size-11`, never `.touch-target`: `buttonVariants` already centres a 44px overlay on every Button, so two 32px ones 4px apart overhang each other and the later sibling wins — tapping the right of « / dent » would have set « forfait », silently changing what the act is billed at. |
| `record/act-catalog-picker.tsx` | The catalogue as **one dense column** (a tile grid was unpickable — multi-column wrapping breaks vertical scanning and truncates long act names). Category stripe, name, état résultant, right-aligned tabular tarif. Group headings come from the shared `procedure-categories.ts` and **vanish while searching** — 19 seeded acts across 12 categories means headings otherwise outnumber rows — at which point the discipline moves onto the **row itself**, since flattening the list is exactly when two similarly-named acts from different disciplines end up adjacent with nothing to tell them apart. The filter matches `category` too: « endo » must reach « Traitement de canal », whose own name contains none of those letters. Accent-insensitive filter, ↑↓/↵/esc, free-text fallback row. Renders **inline, never in a Popover**: nesting Radix `Command` in a `Popover` inside a `Dialog` gave three components a claim on Enter. |
| `odontogram-plan-seed.ts` | What « Créer un plan » hands the devis editor — `OdontogramPlanSeed`, `SeedCandidate` and **`seedCost`**. Its own module so the plan form reads the shape and the pricing rule without importing a chart component, and so the two cannot answer differently. ⚠️ `seedCost` multiplies by the tooth count **only for an act that charts a state** — the fiche de soins' own `derivePerTooth` rule. An act with no resulting condition is a session fee, so without it a « Traitement parodontal » grouped over six teeth would be quoted at 720 DT for a 120 DT act, and the devis would go to the patient that way. |
| `procedure-categories.ts` | How the act catalogue is **grouped and ordered for display**, shared by this picker and `appointment-acts-picker` so the two cannot disagree about where an act lives. `PROCEDURE_CATEGORY_ORDER` (a mirror of the backend's canonical twelve, in clinical order) + `groupProceduresByCategory` + `UNCATEGORIZED_LABEL`. ⚠️ Display only — the category itself is data, filed per act and canonicalised server-side; the authoritative list *for input* is `procedureTypesApi.getCategories()`. The mirror degrades safely rather than breaking: a discipline added server-side and not here sorts with the clinic-authored ones instead of vanishing. It replaced the picker's local `CATEGORY_ORDER`, which bucketed on **`description`** and lumped everything it did not recognise into « Autres » — now a clinic-authored discipline gets its **own heading with its own name**, and only genuinely unfiled acts land in « Sans catégorie », last. |
| `record/act-detail-fields.tsx` | ONE act's remaining detail, folded inside its own card: état résultant, MODVL faces, note. ⚠️ The tarif, the `/dent ↔ forfait` switch **and** the free-text désignation are deliberately NOT here — they are on the card face. Folding the price away was a reported defect (the dentist saw the figure they wanted to change and could not reach it, so they lowered « Payé », which means the patient still owes); the désignation followed it out because it is the act's name and an unnamed free-text act was being dropped on save. |
| `record/record-section.tsx` | Collapsible section whose header renders a **live summary of its own contents** (« Obturation · 90,000 / dent · faces M, O »). This is what makes the folded density safe — collapsing makes a value read-only, never hidden. |
| `record/use-session-acts.ts` | The record modal's `useReducer` store: the séance's `acts` and the `focusKey` of the one the chart writes to. ⚠️ **`toothNumbers` belongs to the ACT**, not to a `selection` shared by the whole séance — that shared selection is what made adding a second act clear the first. `draft` / `hasDraft` / `commitDraft` / `editingKey` / `beginEditAct` / `cancelEdit` / `pendingAct` are all **gone**. Exports `resolveActCost`, `actTotal`, `hasInvalidPrice`, `isActNamed` and `isActTouched` — the last two are what let a blank trailing card be dropped in silence while one carrying teeth or a price is refused, since dropping *that* would throw away work visible on screen. ⚠️ **`applyAppointment` proposes EVERY act booked into the séance**, not just the first: with one card the header read « 1 acte » and one act's money for a visit booked for three, and the others lived in a faint dashed chip row users missed. It takes a `BookedActPrefill[]` whose **`agreedCost` is required** (`number | null`), so a caller that omits a price does not compile rather than silently getting the tarif; the chip row now renders for one case only — an act the dentist deleted, offered back. ⚠️ A saved fiche reopens with **nothing armed** (a stray tap on the chart would otherwise re-chart an act that may already be on a numbered note); a new fiche opens with its single blank card armed, so tap-teeth-then-pick still works on arrival. **Never infers per-tooth pricing when loading a saved act** — it reads `unitCost`/`isPerTooth` provenance, so re-saving is cost-neutral. ⚠️ `applyAppointment` and `addFromProcedure` take an **`agreedCost`**, and it is load-bearing: the fiche prices a booked act from the CATALOGUE, so without it a visit booked at a negotiated price opens the fiche at its tarif. It arrives `unitCostLocked` + `perTooth: false` with `perToothLocked` — a forfait, since teeth are unknown at booking, and locked because otherwise `applyProcedure` re-prices from the catalogue the next time the card is touched. `check:responsive`'s **N14** holds both dispatch sites. |
| `patient-summary-modal.tsx` | Read-only patient summary (info + records + a read-only `RecordToothChart` of worked teeth). Opened from the patients table. |
| `odontogram.tsx` | Interactive per-tooth chart (`odontogramApi`): chart/remove diagnoses, view all recorded states per tooth, « Créer un plan depuis l'odontogramme » seeds; realtime (Patients). ⚠️ **Seeds are one per DIAGNOSIS, not one per tooth** — two caries are one plan line carrying both teeth, where they used to be two identical lines with two empty designation fields. ⚠️ `candidatesByCondition` is built from each act's own **`treats`** (server-computed from `ConditionTreatments`). It replaced an inversion of `resultingCondition`, which answers a different question — « what state does this act leave behind? » — and so offered **nothing at all** for a pathology, since no act ends in « Carie »; that inversion also kept the *first* act per state out of a list ordered by category then name, so « Extrait / Absent » resolved to the surgical extraction (200 DT) over the simple one (60 DT) by alphabetical accident, and answered a missing tooth with an extraction. A line is pre-filled **only when one act holds the first rank**: two acts on the same rung (simple vs surgical extraction, détartrage vs surfaçage) is a clinical judgement, not a tie to break. |
| `record-tooth-chart.tsx` | Presentational FDI tooth-chart SVG (paint map passed in); shared by the record + summary modals. `ToothPaint` carries `selected` + this session's `color`/`count` **and** the prior recorded state as the tooth *outline* (`existingColor`, dashed when `existingIsDiagnosis`). Deliberately has no selection chrome of its own so the read-only summary modal can reuse it. |
| `tooth-multiselect.tsx` | FDI tooth-picker popover; exports `ADULT_FDI`/`CHILD_FDI` + `isAdultTooth(n)` — the single client-side authority for a tooth's dentition (mirrors the backend `FdiTooth.IsAdult`), so records holding both dentitions are always split by the tooth, never by `record.isAdultTeeth`. |
| `odontogram-conditions.ts` | Shared `ToothCondition` metadata: labels, colors/box classes, surface labels, `conditionStyle`/`parseSurfaces`/`serializeSurfaces`, plus `CONDITION_FAMILY` (à soigner / constat / déjà traité). **Fifteen conditions** since `odontogram-plan-suggestions` added the six that change what gets planned and already have a treating act — Fracture · Racine résiduelle · Dent incluse · Restauration défectueuse · Lésion périapicale · Maladie parodontale. ⚠️ Each new hue sits in a family the first nine left free **and** below its nearest neighbour in lightness: the « À traiter » note above records that two hues within ~20 ΔE are one colour at the 12 px swatch this legend draws. ⚠️ `NEEDS_TREATMENT_CONDITIONS` **mirrors the server** and `OdontogramConditionMirrorTests` parses this file and fails the build on drift — in both directions, for the whole set and for that subset. `DentIncluse` is deliberately **not** in it: an impacted tooth is usually monitored, and inflating « N dents à traiter » is how that figure stops being believed. |
| `patient-files-manager.tsx` | Per-patient folder/file browser (`patientFilesApi`), rebuilt by `patient-file-uploads` P5 on the five shared pieces below. **Paged** (`getFilesPaged` + the shared `DataTablePagination`) — it used to fetch a patient's entire drawer and cut it in the browser. One **actions menu** per row and per folder card (`coarse:size-11` trigger, `coarse:py-3` items) replaced two adjacent 32 px buttons and an `opacity-0 group-hover` delete that a finger could not reach; `showErrorToast` throughout; `EmptyState` + a **distinct skeleton** + a « Réessayer » banner, so *loading*, *empty* and *failed* are three states rather than one blank rectangle; the folder grid is `sm:grid-cols-2` (it was two columns at 320 px). Delivery still goes through `lib/download.ts`. |
| `files/patient-files-directory.tsx` | `/fichiers` — the whole screen (header included, so its subtitle quotes the read's own total rather than a second one). A card per patient: pastel tint, initials disc, the file count in a tile, total size and the last upload. **`toneIndexFor`** is imported from `ui/initials-avatar.tsx` so the card's hue and the disc's can never disagree — only the index is shared, each surface keeping its own literal class strings because Tailwind never generates a composed one. The hue lands on a corner glow and the tile, **never on text** (tinted ink measures ~3.3:1 against its own wash), and only a patient who *has* files gets it: spending the colour on every card spends it on none. The count-and-date row is `flex-wrap` because a card is narrowest at **820 px** — tablet portrait, two columns beside the rail — not on a phone. |
| `patients/files/use-file-preview.ts` + `file-preview-dialog.tsx` | **One** preview (AC-5.3). It existed twice — here and in `app/patients/[id]/page.tsx` — byte for byte down to the object-URL revoke, with only the PDF frame ever extracted, so the half that leaks memory when it goes wrong was the duplicated half. The hook holds the live URL in a ref for the unmount release (state there captures a stale value), and skips the fetch entirely for a format nothing can show. ⚠️ **Where the bytes come from is decided by residency** (`clinic-file-decoders`): a **coffre** original is read from the paired folder, because asking the server for one can only 404 — it never reached the deployment — and doing so showed « ce format ne s'affiche pas » on the one machine actually holding a 400 Mo study. ⚠️ The dialog renders four ways (`image` · `pdf` · `archive` · placeholder) and the placeholder carries a **reason**: « l'original est au cabinet » and « rien ne peut l'afficher » are opposite facts calling for opposite actions, and one sentence covered both. ⚠️ **A file needing a decoder shows its stored stand-in FIRST, and that is the difference between usable and not** — measured, libheif takes **11 s** on a 51 Mpx HEIF (the resize and re-encode are 1171 ms and 91 ms, so tuning the pipeline was never the answer), and a dentist opening a photo met eleven seconds of spinner. The stand-in paints in ~300 ms. The original stays reachable behind « Pleine résolution » (§ 0), a **button** rather than a background upgrade, and it is offered only when the stand-in was really shrunk — an original smaller than `PREVIEW_EDGE` **is** its own stand-in, so two 640×480 TIFFs were offering a control that spent seconds to produce a pixel-identical picture. `stage` distinguishes the two waits, because one sentence for a fifth of a second and for eleven reads as a hung screen. ⚠️ **A DICOM carries an  strip under the picture, outside the scrolling pane so it cannot be scrolled away from what it qualifies** — that format only becomes 256 greys once a window is chosen, so a finding outside it is simply not in the image, and a rendering that looks authoritative and is not is worse than none. It shows on the fast path too: the stand-in was built by the same windowing. `onDelete` is optional: the patient page's tab offers no destructive action. |
| `patients/files/dicom-viewer.tsx` + `dicom-viewer-stage.tsx` | **The DICOM study viewer** (`dicom-interactive-viewer`) — window/level, zoom/pan, frame scrolling and one measurement, on its **own full-screen surface over** the preview dialog. ⚠️ **Not a mode of that dialog**, because the dialog is shared by four formats and **owns the horizontal swipe** — sideways means « next file », which is exactly the gesture window/level needs, so the two cannot live in one element without one becoming a modifier of the other. It is reached from a « Visionneuse » button that **replaces** « Pleine résolution » for a DICOM (the viewer opens the original at full resolution *and* adds the controls, so the older button would be a second weaker route to the same bytes — and a fifth control in that row does not fit at 390 px). ⚠️ **The dialog's `window`-level ←/→ handler is suspended while it is open**: those keys step a FRAME here, and left live one press would step the frame *and* the file underneath, so closing would land on a different radiograph (verified: frame 0 → 3 while the counter held at « 1 / 15 »). ⚠️ It fetches its own bytes through the hook's `loadSource`, so the residency rule is not written a second time and it needs no `vault` prop. ⚠️ **The gesture conflict is resolved by an explicit tool, not a hidden mode**: the stage never scrolls (`touch-action: none` — pan and zoom are its own transform), one finger does whatever the `radiogroup` says, and two fingers **always** pinch while *rolling back* whatever the first finger's travel did before its partner landed — without that, every zoom also nudges the contrast. Every gesture reads its baseline from a **ref**, never from state, because a pinch fires several events per frame. ⚠️ **Presets are anchored on the file, and there are deliberately no « os / tissus mous » windows**: CBCT is not HU-calibrated and not one of the ten real samples carries `RescaleType`, so a fixed Hounsfield preset would be confidently wrong on the DICOM a dental practice actually produces. « Inverser » is the control a dentist reaches for instead. ⚠️ Below **560 px of viewport HEIGHT** (a landscape phone) the layout becomes a row with the chrome in a 320 px column: stacked it left the picture 78 px of 390. ⚠️ The stage is dark in **both** themes on purpose, and the advisory strip sits outside it so it cannot be panned away |
| `patients/files/file-kind.ts` | `previewMode` / `isPreviewableFile` / `isThumbnailable` / `fileIcon`. ⚠️ **Previewability is the served policy's answer, not a list kept here** — the catalog carries `isBrowserPreviewable` per format, so a HEIC (an image by MIME type, unrenderable by every desktop browser) is known rather than discovered by a broken `<img>`. With the policy unread we stay optimistic and let the element's own `onError` say so. ⚠️ **`lib/files/decoders` is not a second copy of that list**: the catalog answers « does a *browser* paint this unaided? » (a fact about the format), the registry answers « does this *build* ship a decoder? » (a fact about the module graph, which no server can know). `previewMode` **unions** them and never compares them. |
| `patients/files/file-thumbnail.tsx` | A row's thumbnail (AC-5.2): **two** gates before a byte is fetched — the row is on screen (`IntersectionObserver`) and it has a stand-in image to fetch. Live object URLs sit in a **bounded module pool** that revokes on eviction *and* tells the evicted owner to fall back to its icon, so a long drawer never retains every blob and never shows a dead `blob:`. ⚠️ **The two gates this replaced both asked about the ORIGINAL while the tile paints the PREVIEW** (`clinic-file-decoders`): one required the original to be browser-previewable, hiding the thumbnail of every HEIC and TIFF whose stand-in was ready to serve; the other refused any file over 8 MB, left over from when this component downloaded the original — so a 40 Mo panoramique with a 200 Ko preview showed an icon to save a download nobody was making. Neither failed anywhere; the row just showed an icon. |
| `patients/files/upload-queue.tsx` | Per-file upload with a per-file outcome (AC-5.4), on `import-patients-dialog.tsx`'s `RowCard` shape, replacing a `Promise.all` whose rejection reported **the batch** as failed — ten radiographs, one refused on its signature, and the toast said all ten failed while nine were already stored. Concurrency is bounded at 3 (the caps are 150 MB now). Each hosted upload now also carries a **stand-in image** built by `lib/files/preview.ts` from the same file — before it did, no hosted file had a thumbnail at all, because `PreviewStorageKey` was written by the coffre registration and by nothing else. The client-side refusal comes from `refusalFor(policy, file)`, i.e. the server's own sentence; the server still re-checks. ⚠️ **A file larger than one chunk goes through the RESUMABLE path** (`lib/files/resumable-upload.ts`), chosen by `shouldUploadInParts(policy, file)` — the threshold is the server's own `resumableChunkBytes`, never a constant here, and 0 on the three doors with no such endpoints. That path is also what makes a **real per-file progress bar** possible for the first time: `fetch` exposes no upload progress for a single POST, so the smaller files still show none, deliberately — a fake one is an animation pretending to be a measurement. The number shown is the server's own byte count coming back on each confirmed part. An in-flight chunked upload also gets « Annuler », which **abandons** the session; every other failure leaves it, because the parts already sent are the point. ⚠️ A cancellation renders as « Annulé » and never as « Échec » — an aborted `fetch` arrives as the same `AbortError` a fired deadline does, and `client.ts` words that one « Vérifiez votre connexion ». ⚠️ **Two members exist purely so `resume-uploads-notice.tsx` cannot contradict this queue**, and each fixes a defect that was on screen: `settledCount`, because `items.length` changes when an upload *starts* and never again — so a resumed file finished while the offer to resume it stayed up; and `activeUploads`, the sessions running right now — without it the notice offered « Reprendre » for a file whose progress bar was at 54 % directly below, and accepting would have opened a second client against one session and had its parts refused as out of order. ⚠️ The record is also **not written until a part has actually landed**: `onProgress` fires as the session opens, so remembering unconditionally left « 0 o sur 29,8 Mo déjà envoyés » behind every upload dropped in its first seconds — an offer to resume nothing, since no staging area exists to continue from. |
| `patients/files/storage-usage-line.tsx` | « 3,1 Go sur 10 Go » — the cabinet's storage ceiling, where files are added (`large-file-transfer` Part 4). ⚠️ **Renders nothing where nothing is enforced**: the server publishes a quota of **0** on a deployment whose clinic owns the disk, and that absence is the whole signal — the browser never infers it from a deployment kind it is not told. ⚠️ **One line, not a card, and always on screen rather than appearing near the limit**: hiding it until 80 % was the other candidate and it is worse, since the first time a practice learned there was a ceiling would be the day it stopped them. ⚠️ The tone escalates on the **meter and the figures**, never on a background wash — it sits directly above the drop zone, where a tinted band reads as an error on a screen where nothing is wrong. |
| `patients/files/resume-uploads-notice.tsx` | « Un envoi a été interrompu » — the offer to carry on with an upload a closed tab or a reload left unfinished (`large-file-transfer` Part 2). The server keeps the staged parts for 24 h, so the only thing missing is the bytes. ⚠️ **The user re-picks the file, and that is a decision**: a `File` is structured-cloneable, so the bytes *could* live in IndexedDB and save a click — at the price of a patient's imaging sitting unencrypted in a shared clinic PC's browser profile, surviving reboots. `lib/files/upload-resume-store.ts` stores a **description** instead. ⚠️ The identity check is name + size + **`lastModified`**, the last of which catches the case that happens: the same study re-exported from the scanner between attempts, which would otherwise be assembled from two exports with no error anywhere. ⚠️ It renders **nothing** when there is nothing to resume — the ordinary case, every day — deletes an expired record rather than offering it (its parts have been reclaimed), and never offers one the queue is **currently uploading** (`inFlight`, from `useUploadQueue.activeUploads`). |
| `patients/files/rename-file-dialog.tsx` | Rename / describe / move (AC-4.2, AC-4.6) — the app's first rename affordance for anything. The **extension is a fixed suffix beside the field**, because the server recomposes the name from the stored one and an editable extension would promise a format change that is unrepresentable. |
| `patient-file-pdf-preview.tsx` | A patient file's PDF preview — **a frame on a mouse, the file itself on a finger**. Shared by this manager *and* `app/patients/[id]/page.tsx`, whose two copies previously said « Kept in sync with … » in their own comments; Phase 4 of `mobile-native-shells` routes this same surface through the native viewer, so two copies would have scheduled that fix to land in one of them. The `#toolbar=0&navpanes=0` fragment both used to pass is **Adobe/Chromium-only** — Android WebView ignores it and renders the frame **blank**, a white A4 rectangle that reads as a corrupted radiograph rather than an unsupported viewer — so it is gone on every pointer (`pdf-viewer-params` check). ⚠️ The two trees are chosen by the **`coarse:` variant, not `useMediaQuery`**, which returns `false` on the first client render by contract and would paint-then-swap a loaded PDF on every phone. ⚠️ Nothing here may set `display` in an inline style: the frame used to carry `style={{ display: 'block' }}`, and an inline style beats a class, so `coarse:hidden` would be silently inert and both trees would render at once. |

## Billing — factures / créances / plans

| File | What it renders / does |
|------|------------------------|
| `factures/invoices-table.tsx` | Invoices (« notes d'honoraires ») — `invoicesApi`, realtime (Invoices): create/edit draft, issue, record payment, cancel, delete, PDF. |
| `factures/invoice-form-modal.tsx` | Draft create/edit with per-line CNAM/DCH act picker (fills designation + default fee, drives the reimbursable split). |
| `factures/payment-modal.tsx` | Record an invoice payment (method/amount/date) + "Télécharger le reçu". Since L8 it mounts `ChequeFields` when the method is `Cheque`. |
| `factures/cheque-fields.tsx` | **L8** — the three fields that identify a cheque (n°, banque, encaissable le), plus **`chequePaymentFields()`**, the single payload builder. Shared because an invoice payment, an échéance payment and the fiche's « Facturer » all take a cheque, and three copies of a conditional sub-form is how one loses its gate and sends a cheque number on a cash payment — which the server then refuses in a dialog whose user did nothing wrong. ⚠️ **The method check lives in the builder, not at the call sites**: it clears the fields when the method is not `Cheque`, so a value typed and then abandoned (user switches back to « Espèces ») can never be submitted, while staying on screen in case they switch back. ⚠️ `chequeDueDate` is sent as a bare `YYYY-MM-DD` — never `toISOString()`, which would shift a cheque due on the 1st into the previous month. |
| `factures/invoice-labels.ts` | French labels + badge classes for invoice status and payment method. |
| `factures/bill-dental-record-dialog.tsx` | « Facturer cette intervention » on a fiche de soins: issues the note d'honoraires **and** records the cash taken at the end of the session, in one action. It replaced a prefilled `InvoiceFormModal`, and that is the point — the old flow produced a *draft*, so money already handed over needed a second action nobody was prompted to take, which is how `DentalRecord.AmountPaid` became a field shaped like a receipt that no money read touched. Deliberately shows **no editable line table**: the per-tooth pricing rule moved server-side (`DentalRecordInvoiceLines`), so the acts are read-only and the server prices them. States the irreversibility (a number is consumed; corrections are avoirs) *before* the click. |
| `caisse/caisse-ledger-table.tsx` | The « extrait de caisse » — every movement behind la caisse's totals, oldest first, with Entrée/Sortie columns and a « Solde de la période » running balance (named after the period on purpose: it opens at zero and is **not** an account balance). Voided rows stay visible, struck through, with motif + author, and do not move the balance. Its `KIND_ICONS`/`KIND_LABELS`/`kindHref` are exhaustive `Record`s over the wire enum, so a new movement kind without a label or destination is a `tsc` error rather than a blank row. Since L8 a cheque row also names itself — « n° 4512873 · BIAT · encaissable le 15/09 » — through **one** `chequeSummary()` used by both trees, since a cheque described differently in the card list and the table is the drift `ConventionPrompt` documents; it returns `null` when there is nothing to say, so the field is omitted rather than printed as « — ». | ⚠️ **Its « Corriger » column is `sticky end-0` with an opaque `bg-card`, and that is a § 0 fix.** Nine columns come to ~914 px at min-content, so measured inside this card the actions cell sat **outside the scrollport from 1024 px to 1366 px** — every ordinary 1280 px laptop — and the only way to it was the horizontal drag nobody tries. That column is the sole route to correcting a payment's date, opening a note's correction, and (since `caisse-monthly-expenses`) editing or deleting a dépense. Sticky keeps all nine columns *and* the control; it is the same technique `TableEmptyRow` uses on the other edge of this very table. It deliberately does not tint with the row's hover — a sticky cell paints over the row's own background, so inheriting it is not available. ⚠️ **The card list now passes `actions` too**: it had none at all, so every correction on this statement existed on a desk machine and nowhere else — the capability removed by the choice of tree rather than by any platform limit. `CaisseRowActions` returns null for a row it has nothing to offer, so a card without an action gets no menu rather than an empty one. |
| `caisse/caisse-row-actions.tsx` | The « Corriger » cell's contents, one component per row. **A payment** gets « Corriger la date » (the cash date, which no document carries) and « Corriger le montant… » (which opens the *note's* correction, because an amount is on a numbered document and typing over it here would leave la caisse and the note disagreeing) — offered only while the payment is live. **A dépense** delegates to `ExpenseMovementActions` and gets the whole form, because it has no document behind it: the row IS the record. ⚠️ It returned `null` for a dépense until `caisse-monthly-expenses`, so the cell was empty on exactly the lines whose correction needs no document. ⚠️ **`localDayIso(movement.occurredOn)`, never `.slice(0, 10)`** — `occurredOn` is an instant at the start of a *Tunisian* day, so a payment received on the 1st serialises `2026-08-31T23:00:00Z` and slicing pre-filled « Corriger la date » with the 31st of August. |
| `caisse/expense-movement-actions.tsx` | « Modifier la dépense » / « Supprimer la dépense » on a dépense line of l'extrait, over the shared `ExpenseFormDialog` — so every field is editable, date and catégorie included, and there is no second definition of what a dépense may be. Reads the row before opening (the movement carries a composed `label`, not the dépense's fields) over the dépense's **own day** through `localDayIso`; a row that has since gone reports so rather than opening a blank « Nouvelle dépense » that would invite re-creating it. Delete mirrors the server's AdminOnly rule. ⚠️ **It owns its own trigger and menu**, and the two dialogs are SIBLINGS of that menu: Radix unmounts `DropdownMenuContent` on close and `onSelect` closes it, so a dialog rendered inside was destroyed in the same tick it was asked to open — the menu item silently did nothing. |
| `caisse/expense-form-dialog.tsx` | **The one form a dépense is written in** — « Nouvelle dépense » and « Modifier la dépense », every field editable on an existing row. It was a same-file helper in `app/caisse/page.tsx` while the dépenses table was the only editor; l'extrait's « Corriger » column opens it too now, and a second copy of a form carrying a date-resolution rule, an amount parser, a concurrency token and a dirty guard is four ways for the two to disagree about one row. Also holds « **Répéter chaque mois** » — offered on a NEW dépense only, since on an existing row « does this repeat this row, or turn a past month into a template? » has no good answer on a money screen. |
| `caisse/monthly-expense-list.tsx` | « Dépenses mensuelles » — the standing commitments, with Modifier (future months only) and Arrêter (no motif, moves no figure). Renders **nothing** until the cabinet has one: a permanently empty card teaching an unused feature is what makes a screen feel complicated, and the switch in « Nouvelle dépense » is where the feature is discovered. Not period data — no window, no pager, and outside the page's search box. ⚠️ On the **`lg:`** hinge though it has only six columns, against `card-list.tsx`'s « eight or more » rule: measured, they come to 530 px in the 451 px an `md:` table gets at 820 px, and the column pushed out of view is **Actions**. |
| `caisse/monthly-expense-dialog.tsx` | Editing one series. **No date field, and that absence is the feature** — a series has a day of the month, not a day, and offering a date would invite « je corrige le loyer de septembre », which is a different act done on the dépense row itself. |
| `caisse/expense-fields.ts` | The vocabulary a dépense is written in — `PAYMENT_METHODS`, `EXPENSE_CATEGORIES`, `methodLabel`. Shared by the one-off form and the monthly one because both write into the same `Expenses` table, and a second copy is two French labels for one enum value. |
| `creances/receivables-table.tsx` | Clinic-wide « Créances » (`billingApi.getReceivables`); row → patient detail; total-due header. |
| `treatment-plans/treatment-plans-table.tsx` | Plans/devis **list** — `treatmentPlansApi`, realtime (`[TreatmentPlans, Appointments, Invoices]`). Rows navigate to `/treatment-plans/[id]`; what remains is create + a small **labelled** dropdown (Ouvrir / Devis PDF / Modifier / Supprimer le brouillon). Takes **`filtered`** + `onClearFilters` so the empty state keeps **three** kinds apart, not two: a parent that narrows by date or statut (the plans page now opens on the current week) gets « Aucun devis pour ces filtres » + « Voir tous les devis », while the first-run invite stays reserved for a clinic that genuinely has no plan — otherwise a quiet Monday tells a practice with three hundred devis that it has none, and offers to create a duplicate. *(The "Gérer" dialog and its row of 8 unlabelled ghost icons were retired — the dialog was the only view of a plan's contents and offered every action on every row regardless of état.)* |
| `treatment-plans/plan-workspace.tsx` | **Séance grouping (`multi-act-appointments`)**: tick « À planifier » acts, then « **Planifier ensemble — 1 RDV** » — one appointment carrying every ticked act. ⚠️ **There is no « Planifier séparément » any more.** It shipped beside « ensemble » so neither split was a default, but booking N acts as N visits is exactly what each act's **own** « Planifier » on `plan-act-row` already does, one row at a time — so the bar's version was a second door to the same place that additionally had to walk a queue of dialogs to get there, and it did not behave as expected. Removing it leaves the mixed split (tick two → ensemble, then book the rest from their rows) intact. With one act ticked the bar's button is simply « Planifier ». Still implemented as a `bookingQueue: PresetPlanAct[][]`, now only ever empty or a single group. ⚠️ The dialog is deliberately **not** `key`ed on the queue head: that key changed in the same render that flipped `open` to true, so the closed instance unmounted and a new one mounted *already open*, which `useDirtyGuard`'s history round-trip then closed a frame later. It reports success by calling `onSuccess` **then** `onOpenChange(false)`, so `justAdvancedRef` keeps that close from reading as the user backing out. The devis workspace body: header + actes + échéancier + parcours, the plan's actions (Accepter / Facturer / **Modifier le devis** / Terminer / Devis PDF / Annuler), « **Modifier l'échéancier** » on the Échéancier card, and the Encaisser / Planifier / amend / revise / détacher dialogs. Rendered by `/treatment-plans/[id]`. Two gates, both mirroring the server rather than inventing a rule: `canAmend = isActive && !billed` (same as « Facturer », matching `EnsureAmendable` + the billed-plan block) and `canCorrectActs` = **not** Draft/Cancelled, which **includes `Completed`** — marking the last act done auto-completes the plan, so gating the correction on an *active* plan could never reach the case it exists for (`EnsureCorrectable`). |
| `treatment-plans/plan-act-row.tsx` | Also carries the grouping tick box (`selection`, rendered *disabled* for a non-bookable act so the column stays aligned) and a « **séance de N actes** » badge from `sessionActCount` — derived from acts sharing one `scheduledAppointmentId`, since two acts in one appointment *are* one séance and nothing is stored. Without the badge, four acts on the same date read as four visits. One planned act: désignation, `codeActe`, dents, coût, its derived état, and **exactly one** primary action — Planifier / Voir le RDV / Enregistrer la fiche / Voir la fiche. Navigates via the existing deep links (`/appointments?appointmentId=`, `/patients/{id}?addRecord=1&appointmentId=`, `/patients/{id}?tab=`). A `done` act is the one exception: alongside « Voir la fiche » it carries « **Détacher** » (`onUndo`) — reading the fiche is what tells the dentist it is the wrong one. Omitted on a Draft/Cancelled plan. |
| `treatment-plans/plan-timeline.tsx` | « Parcours » — a chronological feed (created / accepted / séance planifiée / acte réalisé / paiement encaissé / annulé / facturé) built only from fields already on `TreatmentPlanDto`. Reuses `notification-panel.tsx`'s feed shape rather than inventing a timeline primitive. |
| `treatment-plans/plan-progress-bar.tsx` | Hand-rolled `role="progressbar"` (no `progress.tsx` primitive, no `@radix-ui/react-progress`). Renders **nothing** at 0 acts rather than a zero-width bar. Used by the workspace, and by `plan-act-pips` as its **long-plan fallback** — it is no longer the patient band's primary progress display, because "nothing at 0 *acts*" still left a full-width grey slab at 0 *done*. |
| `treatment-plans/plan-act-pips.tsx` | One pip per planned act, coloured by its derived état (`planItemState`). Encodes **four** states — réalisé / séance planifiée / séance passée sans fiche / à planifier — in less width than a bar needs for one, and keeps meaning at 0 réalisé, where a bar degenerates into a grey slab. Acts stay in **plan order**, not sorted by état: the API orders them by `sequenceNumber`, and a plan whose 1st and 3rd acts are done while the 2nd is not is telling you something got skipped. Falls back to `PlanProgressBar` + fraction past **12** acts (a row of eighteen dots is a smear). Pips are `aria-hidden`; the text fraction carries the accessible value — which is *why* they replaced the progressbar role, whose `aria-valuemax` of 0 announces as undefined progress. Per-état treatment is an **inline `boxShadow` map, not Tailwind arbitrary values**: the ring widths differ per state and one needs a palette colour, which in v4 is `var(--color-amber-500)` — v3's `theme(colors.amber.500)` resolves to nothing and renders an *invisible* pip rather than an obviously broken one. |
| `treatment-plans/patient-plans-strip.tsx` | The patient page's treatment **band** above the tabs (`border-y`, no card — ~76 px, replacing the ~250 px `patient-plan-card.tsx`): lead plan + statut + révision + « Facturé », the **counted** next action as the headline (`planHeadline`, e.g. « 1 acte à enregistrer » — the only line that says what to *do*, and it re-derives as visit times pass), then pips · Reste · prochaine séance · status chips. Four defects of the card it replaced, each fixed: the 0 %-progress grey slab; « 0/2 actes réalisé » (the participle was pluralised on `itemsDone`, not the noun); « Reste » never shown though it is the money question staff have; and `Completed`/`Cancelled` plans filtered out of « +N autres », which made finished treatment **unrepresentable**. Chips now cover every statut (`planStatusCounts`), and the band **still renders with no active plan** when the patient has history — the card rendered nothing there, quietly asserting "never had a plan" about someone with three completed ones. Renders null only with zero plans. « Reste » turns `destructive` once the acts are done but the money is not. Design + mockups in `features/patient-plans-summary/`. |
| `treatment-plans/plan-next-action.ts` | Pure helpers: `planItemState` (the four états), `isPlanBilled`, `planNextAction`, `leadPlan`. No I/O. |
| `treatment-plans/treatment-plan-form-modal.tsx` | ⚠️ **A plan is validated and numbered on creation**, so **amend mode is the only editor a devis ever gets** — and everything the endpoint accepts is now sent: acts corrected **in place** (`updateItems`, id preserved so appointment/fiche links survive), additions, removals, the échéancier, the title and the notes. Only the patient stays fixed. The in-place edits were the load-bearing fix: the inputs were already editable but the submit computed `addItems`/`removeItemIds` only, so retyping a fee gave a success toast and **silently discarded the edit** — which is precisely what made a draft stage feel necessary. Only *changed* lines are sent, since re-sending an unchanged act would bump `revisionNumber` on a no-op save, and that counter is how a patient's earlier printout is identified. Draft plan editor: act lines (dental-act picker + `ToothMultiSelect`), installment schedule; accepts odontogram seeds. Also serves **amend mode** (`amendMode` + `editingPlan`, from the workspace): the same form derives `addItems` / `removeItemIds` / `installments` and posts to `POST /{id}/amend`, keeping the devis number and bumping `revisionNumber`. In that mode title/notes/patient and every **existing** act are read-only (the endpoint only adds and removes, so an edited act would be silently discarded — « Retirer » + « Ajouter un acte » is how you change one), and an act that is done or booked shows *why* it cannot be removed. `InstallmentRow` carries `id` + `amountPaid` for exactly this reason: a paid échéance must be echoed back by id or the server refuses the call. Since `odontogram-plan-suggestions` a seeded line also carries **`candidates`** — the acts that treat its diagnosis, least invasive first — offered as one-tap chips that reprice the line unless the dentist has typed a fee; and a line covering several teeth carries **« Séparer par dent »**, because a grouped line is booked and marked réalisé as a unit while two teeth in opposite quadrants are very often two sessions. Splitting **divides** the fee for a per-tooth act and **keeps it whole** otherwise, mirroring `seedCost`. `TreatmentPlanSeedLine` is now an **alias** of `OdontogramPlanSeed`, not a second declaration — the two had already drifted on whether the diagnosis fields were optional. |
| `treatment-plans/installment-payment-modal.tsx` | Record an installment (échéance) payment + receipt. |
| `treatment-plans/revise-installments-modal.tsx` | Re-spread an accepted devis's échéancier (`PUT /treatment-plans/{id}/installments`) without touching the acts. States every server rule **before** submit: which rows are locked (`amountPaid > 0` → cannot be deleted or lowered), and that the schedule must equal `totalPlanned` exactly. Opened from the Échéancier card under the same condition as « Facturer le devis ». |
| `treatment-plans/treatment-plan-labels.ts` | French labels + badge classes for plan/item status. |

## Catalogs (admin) & documents

| File | What it renders / does |
|------|------------------------|
| `cnam-letter-values-card.tsx` | The valeurs de la lettre clé (`dentalActsApi.listLetterValues`/`updateLetterValue`), with the « la convention en vigueur fixe cette valeur à … » prompt and its « Appliquer » action. ⚠️ It lives on **`/dental-acts`** now: `single-act-catalogue` retired `/cnam-nomenclature` along with `cnam-nomenclature-table.tsx` and `cnam-entry-form-modal.tsx`, whose 26 invented act codes mirrored the real `DCH` catalogue. |
| `medication-catalog-table.tsx` / `medication-form-modal.tsx` | Admin medication catalog (`medicationsApi`; backs the ordonnance picker). |
| `dental-acts-table.tsx` / `dental-act-form-modal.tsx` | Admin dental-act catalog (`dentalActsApi`; backs the treatment-plan/invoice act picker **and, since `adoption-qa-k` K1, the BS1 bulletin's act picker**). Carries an ungated note that **which act families require an accord préalable is unverified**: Prothèse no longer does (April 2019, sourced) and the flag was cleared, while Parodontologie and ODF stay flagged because the real list is fixed by an *arrêté conjoint* nobody could retrieve — so it is correctable per clinic and says so. Ungated by `hasProvisional` deliberately: confirming the catalogue does not make that list verified. |
| `document-editor-content.tsx` | Medical-document editor (certificat médical with CNOMDT mention, ordonnance with medication picker + CNAM reimbursement estimate, lettre de liaison, etc.); PDF via `medicalDocumentsApi`, Word export via `docx`. Rendered by `/documents/[type]`. **The `bulletin-cnam` branch was largely rewritten by `adoption-qa-k`:** the act picker reads the **DCH catalogue** (`dentalActsApi`) instead of `CnamNomenclatureEntry` — the two are disjoint and it was reading the wrong one, so every bulletin filled from it was rejected at the caisse on the code column; the **treating practitioner is a `Select`** defaulting to the caller's own doctor (or the only one) and **never** `doctors[0]`, since its code conventionnel is stamped on every act row (⚠️ **the `doctors[0]` fall-back K3 kept for the four free-form types is now gone too**, and the id is **sent** as `issuingDoctorId`: `MedicalDocumentsController` is `AnyClinicRole`, so the routine author is reception — who has no linked `Doctor` — which turned the guess from near-unreachable into the normal path, and the server resolves the cachet + n° d'ordre from that id rather than from the caller. A prescription attributed to a dentist who did not write it is worse than a mis-named bulletin, not better); the five mandatory fields are listed as **visible text** above a disabled Save, each naming where it is fixed (four live on the patient's fiche or the practitioner's profile, not here); « Imprimer » prints the **preview iframe's PDF** rather than a cloned `<Card>` (`documentRef` sits in the *other* branch of the preview ternary, so Print always failed on the one document a conventionné dentist prints all day); « Télécharger Word » is **absent** for a bulletin with a stated reason (its branch chain had no `bulletin-cnam` case and no `else`, so it produced a letterhead-only .docx *and* a success toast); and « Pré-remplir depuis les soins » compares **clinic-local calendar days** (a bare `YYYY-MM-DD` parses as midnight **UTC**, so the « Au » bound excluded its own day and today's séance was silently dropped). ⚠️ Two traps worth knowing: the **stored** acts of an existing bulletin are never rewritten (a pre-K1 mnemonic must still open and print — the renderer stamps whatever the row holds), and **every seeded DCH act has a null `Coefficient`** (it lives in the NGAP arrêté), so picking one fills the code and writes the **lettre clé alone** — never `D 0`, which would render as « non remboursable » — with a per-row note saying the cotation is what is missing. |
| `documents/honoraires-launcher.tsx` | "Note d'honoraires" flow: pick a patient → open `InvoiceFormModal` (draft) prefilled with the patient's un-invoiced dental records (numbering/TVA happen later at issue). |

## Profile, settings & admin

| File | What it renders / does |
|------|------------------------|
| `mon-profil-content.tsx` | Logged-in practitioner's document identity (CNOMDT ordre number + cachet image upload/preview) via `doctorsApi.updateMyProfile`. |
| `doctor-working-hours-card.tsx` | View/edit **one practitioner's** hours (§ 5.4). The missing caller for `GET`/`PUT /api/doctors/{id}/working-hours` — `doctorsApi.getWorkingHours`/`setWorkingHours` had **zero callers**, so the whole per-dentist override was backend-complete and unreachable. Shared by « Paramètres → Médecins » (admin, any practitioner, `embedded`) and « Mon profil » (a doctor, their own). States the inherit-vs-override case explicitly, because an empty `PUT` silently means "clear" server-side. Full `htmlFor`/`id`. Carries the same optional **pause** fields as the clinic-wide editor, and its validator is now `lib/working-hours.ts`' `validateWorkingHours` rather than a private copy. |
| `appointment-labels.ts` | French labels + badge classes for appointment status and patient gender. Replaced four render styles (one a string-mangler whose dead regex produced « Inprogress ») and three colour palettes. `appointmentStatusLabel` falls back to « Statut inconnu » rather than `?? key` — a status is a closed enum, so an unknown value is a bug, not user data; `normalizeStatus` maps the lower-cased forms the edit dialog really pushes through. ⚠️ Since `appointment-elapsed-status` there are **seven** statuses: **`AwaitingClosure`** (« Séance passée ») is written only by `AppointmentProgressJob` once a slot has ended, so the module also exports **`MANUALLY_SETTABLE_STATUSES`** — the six a human may choose. `edit-appointment-dialog` falls back to the full list when the server sends no `allowedNextStatuses`, and that fallback would otherwise let a user assert a slot has ended when it has not. ⚠️ It shares the `pending` tone with `Scheduled`: the scale has six tones and there are now seven statuses, so one pair must share, and this is the safe one — both mean « awaiting an action or a decision », while the pair that must **never** share (« En cours » vs « Séance passée ») stays distinct. |
| `doctor-document-identity-dialog.tsx` | **Admin**. Set **another** practitioner's CNOMDT ordre + cachet via `doctorsApi.updateProfile` → `PUT /api/doctors/{id}`. Deliberately separate from the « Médecins » roster save (`PUT /clinics/doctors`), which rewrites names/specialties/contact, **deletes any doctor not echoed back**, and never touches these two fields — so document identity gets its own dialog rather than being smuggled into a bulk roster rewrite. ⚠️ **The pre-check is the SERVED policy** (`useUploadPolicy("profile-image")`), never a constant here: it used to refuse anything over « 2 Mo » beside a comment claiming to mirror the server, which accepted **twenty-five** — PNG and JPEG are shared with the patient drawer and carried its cap. `profile-image` is its own door with its own ceiling now, and both the `accept` and the helper line come from it. |
| `clinic-settings.tsx` | Clinic profile + doctors + billing (matricule/TVA/timbre) + working hours (`clinicsApi`, realtime Clinics). Mounts `<ReminderSettings/>` for admins and `<BackupSettings/>` for local admins. Each of the four cards' « Modifier » is **admin-only** (`PUT /api/clinics` is `AdminOnly`, so a doctor used to type and meet a 403 at save time), and each save round-trips the clinic row's `version`. The hours editor carries the optional **pause** (`breakFrom`/`breakTo`) and validates through the shared `validateWorkingHours` before the round trip; the clinic name is refused **on the field**, never as a toast quoting the wire key. |
| `reminder-settings.tsx` | **Admin**. SMS/WhatsApp reminder settings (tri-state channel toggles, gateway/Graph URLs, lead times, message body, masked secrets), per-channel `effectiveStatus` badge, WhatsApp Embedded-Signup connect/disconnect (Cloud), recent delivery-status rows. |
| `backup-settings.tsx` | **Local, admin**. "Sauvegarde" card → `backupApi.backupNow`; reports path + size or failure; guards setState against unmount. |
| `backup/recovery-points-list.tsx` | **« Points de restauration »** (`clinic-recovery-points`) — the copies the server keeps of this cabinet's records, newest first, with one « Restaurer depuis ce point » per usable row. Mounted by `clinic-archive-card.tsx` **above** the archive block, and that ordering is the point: « j'ai supprimé une fiche » is the common emergency and is now two clicks away, while the archive below answers the rarer, worse one. ⚠️ **A card list at EVERY width, not a table with a card fallback** — six figures per row inside a ~284 px card is not a table that can compress, and unlike the lists § 6 governs this one has no desktop density worth preserving: it is normally seven rows, read once, in an emergency. ⚠️ **It states what a point does NOT carry**: a scheduled point is rows-only, so restoring one brings back a deleted patient, fiche or document row and **no radiograph** — left unsaid, an owner reads « restauré » and finds the image gone, so the row says « lignes seulement » and the confirmation warns before the click. ⚠️ The outcome is in **words** as well as colour (« Utilisable » / « Interrompu » / « Échec ») so greyscale, 200 % zoom and a screen reader get the same fact; `Running` and `Failed` share one badge because to the reader they are the same fact — there is nothing behind the row — and the error line says which. ⚠️ `ArchiveCopyNotice` is the load-bearing sentence of the card: a reader who has just seen seven healthy points will reasonably conclude they are covered, and they are not — these live on the same server. Its threshold comes from the server (`archiveStaleAfterDays`), so the card and the `ArchiveStale` bell row cannot disagree. Loading, empty and failed are three states (§ 13) |
| `user-management.tsx` | **Admin, both modes** (`/users`). **« Créer un compte » (US-3)** — a dialog (nom complet · email · rôle) that mints the account and shows the one-time password through the *same* dialog a password reset uses, because it is the same fact with the same handling rules. It exists because the hosted profile closes self-registration, so without it that deployment has no way to add a colleague at all. ⚠️ The « Code de la clinique » card **re-captions itself** when `authApi.getMode()` reports self-registration closed: left alone it instructs the admin to hand out a code that no longer creates anything, and a control that lies is worse than a missing one. Plus, since I5, the **pending-activation surface** — a `role="status"` banner and an « N en attente » badge (both from the response's clinic-wide `pendingActivationCount`, **not** from the loaded page: counting the rows on screen would read « 0 en attente » whenever the pending colleagues sort onto page 2, which is exactly when the number matters), a third per-row badge « En attente d'activation » in **both** the card and the table tree, « Activer le compte » instead of « Réactiver », and a confirm dialog that names the role the person chose for themselves and says the account gives access to patient records. « Inactif » on a five-minute-old self-registration read as a bug in the registration the person had just completed; three states fix that. Also: users table (rôle/status/must-change/last-login) with a per-row **rôle Select** (`usersApi.setRole`, confirm dialog that warns the session is revoked), deactivate/reactivate (self-guarded), clinic-code display + Regenerate; realtime (Users). Reset-password is **Local only** — `ResetUserPasswordCommand` correctly refuses non-local accounts, so in Cloud the button is hidden and a note explains that Auth0 owns passwords. A stored role outside the closed set keeps the read-only badge rather than pre-selecting a role we did not store. |
| `change-password-form.tsx` | **Local** (`/change-password`): current + new + confirm → `/bff/auth/change-password`; 8-char min; forced vs voluntary. |

## Abonnement (`clinic-subscription`)

| File | What it renders / does |
|------|------------------------|
| `subscription/subscription-banner.tsx` | **The strip on every screen of the app** from seven days before the end date (AC-3.1), and after it. ⚠️ **Mounted in `AppShell`, not `app/layout.tsx`**: that shell is `flex h-dvh`, so a sibling above it would push the document past the viewport and take the phone's bottom bar with it — here it is a flex row and `<main>` shrinks around it, exactly as `BottomNav` does. It also makes « absent on the auth pages » structural, since the six routes that render no shell are precisely `/login`, `/setup`, `/join`, `/change-password`, `/signup` and `/signup/verifier`. ⚠️ **Never a modal and never a toast** — it is met mid-consultation, so a takeover would sit between a dentist and a patient's file while a toast would be gone before anyone who can pay is looking. ⚠️ **Dismissible only while valid** (AC-3.2/3.3), and the control is *absent* once expired rather than disabled. ⚠️ The dismiss **grows its own box** (`coarse:size-11`) instead of using `.touch-target`: it sits 12 px from « Renouveler » in the same row, and the later sibling paints last, so an overlay would steal taps aimed at the one control that leads somewhere. ⚠️ `coarse:py-1` is what keeps the ≤ 15 %-of-a-380 px-viewport budget met once both controls reach their 44 px floor. ⚠️ **`flex-wrap` alone did not deliver that budget**: `min-w-0 flex-1` is `flex: 1 1 0%`, so the paragraph's hypothetical size is zero and it could never trigger the wrap — leaving it ~146 px of a 288 px box at 320 px, i.e. a ~160 px strip on every screen. `basis-64 sm:basis-0` gives it a real basis and the detail sentence is hidden below `sm:`. ⚠️ **No `role="status"`**: `AppShell` remounts per navigation, so a live region re-announced the whole strip dozens of times a day. ⚠️ **The CTA is part of the state** — « Renouveler » is false of a suspension (paying does not lift it) and of a missing entitlement (our fault), both of which read « Voir les détails ». |
| `subscription/subscription-history-table.tsx` | **« Historique des paiements »** (AC-2.3) — what the cabinet has paid its *software vendor*, admin-only. ⚠️ Cards up to **`lg:`** and a table above it (`CARDS_ONLY_LG`/`TABLE_ONLY_LG`), not the usual `md:` hinge: seven columns beside the 256 px rail leaves ~532 px on a tablet portrait and every cell in `ui/table.tsx` is `whitespace-nowrap`, so the table cannot compress — it would simply scroll the amount off screen. ⚠️ **A cancelled entry is marked in words (« Annulé ») as well as struck through**: the strike disappears in greyscale, at 200 % zoom on a poor display, and for a screen reader entirely, and the fact it carries is that this payment no longer counts toward the end date. Its `coveredPeriod` keeps **three** cases apart — cancelled (covers nothing), open-ended (« depuis le … — sans échéance »), and dated — because rendering any of them as a range with a hole in it would assert a period the cabinet never had. Failure is a `LoadFailureNotice`, never « aucun paiement » |

## Fournisseurs (`stock-fournisseurs`)

| File | What it renders / does |
|------|------------------------|
| `suppliers/suppliers-table.tsx` | « Fournisseurs » — cards below **`lg:`**, table above, over `GET /api/suppliers`. ⚠️ **`lg:`, not `md:`, and that is AC-9 rather than a density preference**: six `whitespace-nowrap` columns behind the 256 px rail leave ~532 px on an iPad portrait, so the table scrolled sideways and the Actions column — the WhatsApp button, the reason the screen exists — sat off screen at 820 px. ⚠️ **The filter chips read `categoriesInUse`; the form's combobox reads `categories`.** The two are different questions (« what narrowing is possible » vs « what filing is sensible »), and rendering the second as chips produced twelve controls over three rows on a cabinet with four fournisseurs. ⚠️ The primary action lives in the page's `PageHeader` and arrives back as a `createRequest` counter, so it is not duplicated in the count row and the empty state at once. ⚠️ **WhatsApp is a visible action on the card, not a menu item** (AC-9): a supplier list nobody can call is an address book, and the narrow layout is the one this action is used on most. Loading / empty / **filtered**-empty / failed are four distinct states — a failed read rendering as « aucun fournisseur » is a claim about the clinic where the truth is a claim about the network. The delete confirm names the record and states the « Désactiver » alternative *before* the click, since the server will refuse a referenced one anyway. |
| `suppliers/supplier-form-dialog.tsx` | Create / edit. **Only the nom is required** (AC-1) — a dépôt gets filed off a delivery note carrying nothing else. ⚠️ A non-Tunisian number is **accepted** with a `role="status"` note rather than an error (EC-1): refusing would lose a real foreign supplier, and what such a number actually costs is the WhatsApp action. The duplicate refusal branches on the server's **code** (`supplier_duplicate`), never its prose, and leaves the dialog open with every field as typed. |
| `suppliers/supplier-picker.tsx` | The « Fournisseur » field on the stock item form **and** the bon de prothèse form. « Aucun » is a real option listed first (AC-5) — most articles have none, and clearing a link must be as reachable as setting one. ⚠️ **A linked supplier absent from the active list is still rendered**, from `selectedFallback`: the list is active-only, so without it a deactivated supplier would read as « Aucun » and the next save would clear a link nobody touched (EC-4). Carries the inline « + Créer un fournisseur ». ⚠️ **`onChange` hands back the picked row as well as its id**, because the bon de prothèse prints a **name** of its own beside the link: without it that form could only fill the printed name when the supplier was created inline, so picking an already-filed laboratory made the user retype a name the app had just been given. A caller that ignores the second argument keeps compiling. |
| `suppliers/whatsapp-action.tsx` | « Contacter par WhatsApp », or « Ajouter un numéro » — AC-3 in one component, used by the fournisseurs list, the stock table, the bell panel and the laboratory board. ⚠️ **Never a disabled button and never an absent control**: a greyed icon says « this is broken » while the real situation — nobody recorded a number — is fixable in ten seconds if the user is told. ⚠️ Deliverability is the **server's** answer (`phoneE164`), so four surfaces cannot disagree about whether one supplier is reachable. It grows its own box (`coarse:size-11`) rather than using `.touch-target`, because it sits in a row beside « Modifier » and a « ⋯ » trigger. |

⚠️ **`lib/whatsapp.ts` is the one place a `wa.me` URL is built.** Five surfaces offer the action and each needs the
same three decisions — is the number deliverable, how is it written into the path (E.164, **no `+` and no
separators**, or WhatsApp answers "invalid number"), and does the link carry a pre-filled message. It also owns the
three French message bodies, so the bell's order text and the laboratory's follow-up cannot drift from the screens
that send them.

## Stock & onboarding

| File | What it renders / does |
|------|------------------------|
| `stock-table.tsx` | ⚠️ **Cards below `lg:`, table above** (`CARDS_ONLY_LG`/`TABLE_ONLY_LG`) — eight `whitespace-nowrap` columns is the § 1 threshold, and at the `md:` hinge it had been rendering the desktop table from a tablet portrait upward, where it overflowed by ~300 px. ⚠️ Its card header is **`xl:flex-row`**, not `sm:`: a title, a count badge, two filter chips, a 256 px search box, a 192 px select and « Exporter » is ~850 px, and `xl:` is the first width where that fits beside the 256 px rail. (The movement-history table inside the dialog keeps the `md:` hinge — four columns, `md:max-w-lg`.) Inventory (`stockApi`) with search/category/low-stock filters + delete confirm; scroll-to/highlight a deep-linked low-stock row (`clinic:deeplink`); an « expiration » column highlighting the earliest relevant lot (périmé vs. expire bientôt). The **entrée** branch of the adjust dialog captures that lot's own **expiry date + n° de lot** (AC-P4.2) — both optional, sent as `null` rather than `""` when blank (AC-P4.7). Only the entrée branch: a **sortie** draws down existing lots FEFO server-side, so it has nothing to date or number. |
| `stock-item-form-modal.tsx` | Create/edit stock item via `stockApi`. Since `stock-fournisseurs` the « Fournisseur » text input is a **`SupplierPicker`** and the catégorie a **`CategoryCombobox`** over the server's own options. ⚠️ `STOCK_CATEGORIES`/`STOCK_CATEGORY_LABELS_FR`/`stockCategoryLabel` are **deleted**: the six English keys were a *closed* set mapped at display time, but the set had stopped being closed and a clinic-authored category rendered raw beside six translated ones — the storage key is the French label now. ⚠️ `supplierId` is **always sent, even as null**, since the update command reads an absent key as « unchanged » and `|| undefined` would make clearing a fournisseur silently do nothing. |
| `procedure-types-table.tsx` / `procedure-type-form-modal.tsx` | Procedure-types CRUD (`procedureTypesApi`) + "seed defaults"; form has color, duration, cost, **catégorie**, resulting condition. ⚠️ **« Couleur de l'agenda » is a two-level picker — the hue, then its nuance** — over `getColorPalette`'s twelve families × three tones. It replaced one flat grid of every accepted colour, which was fine at ten and a wall at thirty-six, and the per-swatch French caption went with it (a name under every circle is what forced that grid down to three columns); the selection is instead stated in words once, below the strip, so greyscale and a screen reader get the same fact as the ring. The active family is **derived from the selected colour**, never its own state, so editing an act opens on its own hue already expanded and the row and the strip cannot disagree. Swatches are `flex flex-wrap` with a fixed `size-10 coarse:size-11` rather than a grid: a six-column grid in a 320 px dialog is 33 px cells, which meets no touch floor. The table also shows a **« Consommables »** column (how many stock items the act consumes) so a material list is discoverable rather than hidden behind a dialog nobody knows to open, and a **« Catégorie »** badge column with a category filter beside the search box — **both server-side**, because narrowing an already-cut page shrinks pages unpredictably (« Endodontie » showing 3 rows out of 25). Rows arrive ordered by catégorie then nom, so the badges read as blocks down the column *without* section headings, which a paged list cannot honestly draw anyway (a discipline straddling a page boundary would print its heading twice). At 8 columns it moved to the **`_LG`** hinge. The form's category field is a **combobox, not a Select**: the twelve disciplines are suggestions and a practice may add its own, and the offered list already includes the clinic's own (`getCategories` unions them), which is what makes an open field converge rather than drift. ⚠️ It sends `category` even when empty — the update command reads null as "unchanged", so `\|\| undefined` there would make unfiling an act silently succeed and do nothing. |
| `procedure-type-materials-dialog.tsx` | **Admin**. An act's **material list** — the stock a fiche de soins draws down when it records that act (AC-P4.14) → `PUT /procedure-types/{id}/materials`. The missing caller for `ProcedureType.SetMaterials`, which shipped with the join table and no UI, so a list could only be seeded directly into the database. Deliberately its own dialog rather than a section of the form modal, for the same reason as `doctor-document-identity-dialog.tsx`: the endpoint has **replace** semantics (an empty list is the meaningful "consumes nothing" opt-out), it only applies to an act that already has an id, and folding it into the form's save would create a window where one of two requests succeeded — which for this list silently clears it. Refuses duplicates client-side by not offering an already-listed item, and keeps rendering an item since removed from the catalogue so saving an unrelated line cannot drop it. |
| `setup-wizard.tsx` | First-run clinic creation (FR; Tunisian governorates; `clinicsApi.create`); local mode also collects the admin account → `/auth/setup`. |
| `join-unavailable.tsx` | What `/join` shows where self-registration is closed (US-3): the three steps by which an admin creates the account instead, and a link to `/login`. ⚠️ **Not a 404 and not a hidden link** — § 0 — because someone reaches here by following « Rejoindre la clinique » from the login page and has to find out *here* what to do. Shared by the page's capability probe and `join-wizard`'s 404 backstop so the two cannot word it differently. `coarse:h-11` on the CTA: `size="lg"` is `h-10`, i.e. 40 px, under the § 2 floor. |
| `join-wizard.tsx` | Join-by-code (role, specialty; `clinicsApi.join`); local mode self-registers via `clinicsApi.register` → `/auth/register`. Since US-3 a **404** from that endpoint renders `JoinUnavailable` rather than an error banner — an « introuvable » toast would blame the clinic code for a deployment decision. ⚠️ Since I5 the local branch ends on a « **Demande envoyée** » screen instead of `window.location.href = "/login"`: the account is created **pending**, so sending someone to a login form their correct password cannot pass is the worst available ending — the refusal reads as the product being broken, and the one fact that explains it (an admin has to let them in) never gets stated. |

## components/ui/ — shadcn/ui primitives

Standard shadcn/ui (new-york) wrappers over Radix + CVA + `cn()`. Treat as the design-system layer:

`alert-dialog`, `avatar`, `badge`, `button`, `calendar`, `card`, `checkbox`, `command`, `dialog`, `dropdown-menu`, `input`, `label`, `popover`, `progress`, `radio-group`, `select`, `separator`, `sheet`, `switch`, `table`, `tabs`, `textarea`, `tooltip`.

`progress` was added by `patient-file-uploads` P5 over the already-installed `@radix-ui/react-progress` (zero new dependencies), and it is the *shared* bar — `treatment-plans/plan-progress-bar.tsx` stays hand-rolled on purpose, since it renders **nothing** at 0 acts rather than a zero-width bar.

Add new primitives with the shadcn CLI (`web/components.json`, base color neutral, CSS vars, lucide icons).
⚠️ The current CLI emits imports from the **`radix-ui` umbrella package**, which this project does not depend on. Rewrite a freshly-added primitive's import to the scoped package that is already installed (e.g. `@radix-ui/react-dialog` for `sheet`, `@radix-ui/react-radio-group` for `radio-group`) and revert the CLI's `package.json`/lockfile edit — `sheet` and `radio-group` were added this way and add **zero** dependencies.

Non-shadcn additions to the same layer:
- **`empty-state.tsx`** — icon chip + title + optional description/action. The shared answer to ~30 hand-written « Aucun résultat. » one-liners, and the place the three empty *kinds* are documented and kept apart (nothing yet / nothing matching the filter / failed to load — the third is not an empty state and gets a retry banner). Pass `chipClassName={zoneChipClass(ZONES.x)}` to tint it with the page's zone; `size="compact"` inside a card, a dialog or a tab body. ⚠️ **Its action row lets its own children break their label** (`[&>*]:whitespace-normal! [&>*]:h-auto [&>*]:min-h-9`): wrapping the *row* is not enough, because `Button` is `whitespace-nowrap shrink-0` and a single long label — « Créer un plan depuis l'odontogramme », 253 px against 223 px — has no sibling for `flex-wrap` to move, so it was painted out through the card's edge. Break the words, never hide them: this is the one control on the surface.
- **`access-denied-card.tsx`** — what a screen renders when the caller's role cannot open it (I3). Shared because
  the three admin catalog pages had each grown their own copy and I3 needed three more: six hand-written access
  refusals is how the wording drifts, one keeps a `red-*` literal, and the sixth forgets the way back.
  ⚠️ **`backHref` is a parameter, not a hardcoded `/`**: a secretary refused « Caisse » must not be sent to
  « Tableau de bord », which refuses them too — two dead ends in a row is how a user concludes the software is
  broken rather than restricted. Defaults to `/appointments`, which every role can open. Not the gate: the API
  refuses these routes and the rail hides them; this covers a bookmark, a shared link, and a role changed while a
  tab was open.
- **`retired-page-card.tsx`** — what a route renders once its screen has been withdrawn from the product
  (`/recurring-series`, `/creances`). ⚠️ **Not `notFound()`**: there is no `app/not-found.tsx`, so Next's built-in
  404 page is English, and no English string may reach a user — and it is the wrong statement anyway, since the
  route exists and answers while the *screen* was removed on purpose. Shared for `access-denied-card.tsx`'s reason,
  and `backHref` is a parameter for the same one: the way back must be a destination the reader can open.
- **`table.tsx`'s `TableEmptyRow`** (the container is `@container/table-scroll` for it) — the « il n'y a rien ici » row, and the only sanctioned way to put an `EmptyState` in a `colSpan` cell. ⚠️ A full-width cell is as wide as the **table**, not as wide as the screen, so a table whose columns cannot compress below the box leaves a *centred* empty state centred on a width nobody can see (measured at 820 px: `/stock`'s invite in a 752 px cell inside a 451 px viewport, « Ajouter un article » half off screen). `sticky left-0 w-[100cqi]` is the fix — `cqi` reads the scroll container's **visible** inline size, so the block is as wide as what the reader can see and `EmptyState` centres inside it, and `sticky` keeps it aligned however far the table is scrolled. Cells wrapping made this rarer, not impossible: the header row alone can still outrun a narrow scrollport. Four tables had it, and it is the empty state a first-run clinic always meets.
- **`form-error-banner.tsx`** — the shared `aria-live` refusal banner used by the dialogs. ⚠️ It renders on `--destructive` tokens now, not `red-*` literals — it was the only red in the product that did not follow the palette, and roughly eighteen surfaces had copied the literal block instead of importing it (copying the hand-maintained `dark:` twin along with it).
- **`confirm-by-typing-dialog.tsx`** — a destructive confirm the operator must **type** a phrase to unlock (AC-P3.47). It exists because the repo's single two-button `AlertDialog` is used for everything from deleting a procedure type to deleting a patient, so an irreversible multi-table operation (anonymise, finalise a bordereau) had no way to *feel* different from the dialog dismissed twenty times a day. Deliberately a plain `Button` rather than `AlertDialogAction`, so a failure leaves the dialog open with the typed phrase intact.

## Conventions

- **A card header's icon goes in a tinted chip**, not in the same ink as the text beside it. `size-8 rounded-lg` + a 12 % wash + the matching ink (`bg-primary/10 text-primary`, a `zoneChipClass(ZONES.x)`, or a semantic `-wash`/ink pair when the card's subject really is a state). `app/documents/page.tsx` is the reference and `appointment-list.tsx` the sized-down template. A black glyph next to black text is not an icon, it is more text — 43 headers drawn that way were the largest single reason the app read as grey. **Not every card**: only ones that are a named section of a page with its own identity, never inside a dialog (the dialog title already names it) and never where the icon already carries a status colour.
- **Colour is expressed in tokens, never in palette literals.** `text-success` / `bg-success-wash` · `bg-warning-wash` + **`text-warning-ink`** (never `text-warning` on its own wash — it lands near 3.5:1) · `text-destructive` / `bg-destructive-wash` · `text-primary` / `bg-primary/10` · `text-muted-foreground` / `bg-muted/40` / `border-border`. Because a token already carries its dark value, converting a pair means **deleting the `dark:` twin** — that removal is most of the point, since a hand-maintained dark mode is what drifts. Genuinely categorical colour (tooth conditions, the act-colour picker) is *data*, not theme, and stays.
  ⚠️ **`bg-accent/30/10` is not a class.** Tailwind does not parse a double opacity modifier, so it emits nothing at all — four of these shipped, and one of them was the only thing distinguishing an enabled working day from a disabled one. Grep for `/[0-9]+/[0-9]+` in class strings.
- **`ui/stat-strip.tsx` is the ONE summary strip** — the row of figures over a list, on `/caisse`, `/factures`, `/cheques` and `/rappels`. `StatStrip` derives its column count from its cells and wraps `dashboard/kpi-grid.tsx`, so the surface, the hairlines and the filler cells stay in one place; `Stat` is a cell (small uppercase label · optional glyph · `text-xl` figure · optional hint), and `onSelect` turns it into a filter control with a 2 px `STATUS_TONE_RAIL` on the selected one. Four screens used to draw this object four ways — `text-2xl` on white with a decorative blue dot (caisse, factures), a small uppercase label with an icon (chèques), and **four edge-to-edge `-wash` tiles** (rappels) that read as a traffic light — breaking to 4-up at three different widths between them. ⚠️ **A `tone` colours the figure and its glyph, never the cell and never the label**: colouring the label too was tried and it is the filled tile again with the fill taken out. ⚠️ `text-xl` flat, deliberately not `KpiCard`'s `text-xl sm:text-2xl` — that is a *link with a period comparison* and earns the step; a strip is raw figures over the table that is the actual content, and `text-2xl` there is what « trop balky » named. It is also what keeps 320 px safe: « 19 460,000 DT » measures ~100 px against a ~112 px cell.
- **`ui/status-tone.ts` serves a tone in four shapes, and a feature maps its own statuses onto it rather than holding colours.** `STATUS_TONE_CLASS` (wash + ink, for a pill or a tinted tile) · **`STATUS_TONE_INK`** (ink alone, for prose or for a figure already sitting on the right wash — a second wash over a correct one reads as a smudge) · **`STATUS_TONE_RAIL`** (the full-strength hue as a raw `var(--success)` for an inline `backgroundColor`: a row stripe, a card rail, a meter fill — at wash strength a hairline is simply not there) · **`STATUS_TONE_RING`** (`ring-success` &c., paired with `ring-2 ring-inset`, for a surface that is *selected* rather than merely tinted; a ring colour with no width paints nothing, so it is safe to apply unconditionally and toggle only the width). `components/rappels/delivery-tone.ts` is the reference for the feature half — one `Record<Status, StatusTone>` — and it exists because that page carried **four** parallel colour maps that could disagree about what « bloqué » looks like. ⚠️ **Raw tokens in the rail map, utilities in the ring map, and the difference is not cosmetic**: the `--color-*` aliases are `@theme inline` entries that Tailwind emits to `:root` only when it judges them *used*, so a `var(--color-success)` in an inline style is a coin flip that paints **transparent** when it loses — silently, since an unresolvable custom property is not an error. Utilities are generated from the theme key and are always fine.
- **`ui/table.tsx` cells WRAP, and that is what keeps a table inside the screen.** The primitive used to put
  `whitespace-nowrap` on every `TableHead`/`TableCell`, so a table's intrinsic width was the sum of its longest
  *unbreakable* value and no column could compress: one free-text column set the width of the whole surface and
  pushed the rest out of the scrollport. Measured signed-in, `/caisse`'s ledger rendered **1563 px inside a
  1084 px box on a 1440 px laptop** — « Mode », « Entrée », « Sortie » and « Solde » simply absent — because
  « Libellé » (495 px) and « Mode » (323 px) hold sentences; `/stock` hid 254 px and `/waiting-list` 161 px at the
  same width. The reader's only clue was the container's 24 px fade, so those columns read as non-existent rather
  than as off-screen. Four separate workarounds had grown around that default — the fade, `TableEmptyRow`'s
  `sticky w-[100cqi]`, `CARDS_ONLY_LG`, and a hand-added `whitespace-normal` in `reminder-log-table` — and all
  four were treating the symptom.
  Pass **`whitespace-nowrap` at the call site** for a value that must stay atomic (a `d MMM yyyy` date, a phone
  number, a `F-2026-0142` reference — those break at their spaces and hyphens like any other text); **`numeric`**
  already implies it, along with `text-right` + `tabular-nums`. Pass **`clamp`** on a free-text column to cap it
  at two lines with the full value in the cell's `title` — opt-in, never the default, because it needs
  `display: -webkit-box`, which would flatten an action column's flex row of buttons.
- **A page's controls go through `PageHeader`'s own `actions` slot — never a hand-rolled flex row around `<PageHeader>`.** As a flex item beside a sibling the header shrinks to its title's width, so the controls bunch against the heading instead of sitting at the page's far edge where every other screen puts them. Five pages had it — `/caisse`, `/stock`, `/lab-orders`, `/waiting-list`, `/recurring-series` — each having reinvented the row `actions` already is (it is `flex flex-wrap`, so it wraps below `sm:` exactly as those wrappers did). `check:responsive`'s **`page-header-not-a-flex-item`** fails the gate on it.
- **`flex-1` is `flex: 1 1 0%`, and that basis beats a `w-full` beside it.** A child meant to take a row of its own below `sm:` must therefore *not* carry an unprefixed `flex-1`, or its hypothetical size is zero, it never triggers the wrap, and it collapses to whatever its siblings leave (measured: `ui/list-toolbar.tsx`'s search box at **4 px** on `/fournisseurs` at 390 px). Prefix the `flex-1`, or give the child a real `basis-*` below the hinge — `subscription-banner.tsx`, `list-toolbar.tsx` and `doctor-working-hours-card.tsx` all carry the same correction.
- Dialogs/modals are controlled (`open` + `onOpenChange`) and report success via callbacks; parent tables reload via a bumped `reloadKey`/`reloadToken` **and** `useClinicRealtime`.
- Components fetch through `lib/api/*`, format money/dates via `lib/format`, and surface errors via `lib/errors`/`sonner`.
- Confirm-destructive flows use `ui/alert-dialog`, and the confirm button takes **`variant="destructive"`** — `AlertDialogAction` used to render `buttonVariants()` unconditionally, so every destructive confirm had to remember a `className` and two forgot, shipping a blue « Détacher la fiche » / « Désactiver » beside an outline « Retour » where the irreversible option read as the recommended one. A confirmation must also **name what it destroys** (« Supprimer le brouillon de {patient} ? » / « Le devis du {date} — {montant} … »), not « Êtes-vous sûr ? »: with three drafts open, a generic dialog cannot tell you which one you are about to lose. Blob downloads use `lib/download`.
- **A failed read must never render as empty data.** `.catch(() => [])` turns a network error into « Aucun antécédent médical » — which, on the screen a dentist checks for allergies before injecting, is a confidently wrong answer rather than a missing one. Record *which* read failed and render a per-section « … n'a pas pu être chargée. » + « Réessayer » beside the sections that did load.
- Admin-gated surfaces read `useSession().user.role === 'admin'`; local-only surfaces also check `mode === 'local'`.
