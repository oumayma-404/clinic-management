{
  "name": "clinic-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end hot-path suite. Scenario ids match features/e2e-hot-paths/scenarios.md.",
  "scripts": {
    "test": "playwright test",
    "test:t0": "playwright test --grep @t0",
    "test:smoke": "playwright test --grep @smoke",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
