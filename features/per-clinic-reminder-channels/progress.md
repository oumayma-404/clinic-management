# Progress: Per-Clinic Reminder Channels & Credentials

**Started:** 2026-07-17
**Type:** Small
**Branch:** feature/per-clinic-reminder-channels (worktree)

## Status
- [x] Implementation
- [x] Quality checks (build, lint, typecheck)
- [x] Tests (added/modified — see Test Plan + Tests Run)

## Tests Run
Built to an isolated `OutDir` + `dotnet vstest` (Smart App Control blocks plain `dotnet test` at load —
`0x800711C7`; this recipe dodges it). Test project build: **0 errors** (13 pre-existing warnings only).

| Suite | Filter | Result |
|-------|--------|--------|
| Unit | `ReminderSettingsProvider` · `ClinicReminderSettings` · `UpdateClinicReminderSettings` · `GetClinicReminderSettings` · `ReminderScheduler` · `NotificationJob` · `ReminderChannelSender` | **38 passed, 0 failed** |

New classes: `ReminderSettingsProviderTests` (6), `ClinicReminderSettingsTests` (4),
`UpdateClinicReminderSettingsCommandHandlerTests` (3), `GetClinicReminderSettingsQueryHandlerTests` (3).
Scenarios added: `ReminderSchedulerTests` (+ClinicId-stamp, AC-4), `NotificationJobTests` (+settings resolution, AC-5).
Existing reminder tests (`ReminderChannelSenderTests`, scheduler, job) re-verified green under the new seam.

## Quality checks run
- Backend: `dotnet build ClinicManagement.sln` → 0 errors. Pre-existing CS8618 entity-pattern
  warnings only; a scoped `--no-incremental` build confirmed **0 new warnings/errors in changed files**.
- Frontend: `npx tsc --noEmit` → clean; `npm run build` (next build) → exit 0 (`/settings` compiled).
  ESLint gate skipped: repo has an `eslint.config.mjs` + `lint` script but no locally-installed `eslint`
  (`npx eslint` resolves a global v10 → `ERR_MODULE_NOT_FOUND`); tsc + build is the real FE gate here.
- EF migration `20260717192757_AddPerClinicReminderSettings` generated with `dotnet ef` (not WDAC-blocked
  this session): `Notifications.ClinicId` (nullable uuid) + `ClinicReminderSettings` table (ClinicId PK/FK
  → Clinics, cascade). Designer + snapshot regenerated by the tool.

## Working tree note (start of session)
Clean worktree based on main HEAD (7d38c81, includes merged `sms-whatsapp-reminders`).
Only untracked artifact: `features/per-clinic-reminder-channels/` (this feature's folder).

## Size note
Traced to ~25 files (encryption + key-ring, DB migration, Data Protection dependency, changed
sender signature, FE). The spec flagged this as at/beyond the small envelope; user chose to
**proceed as Small** in one pass (review still runs at /review-feature → /challenge-review).

## Files Changed
**Backend — new**
- `Domain/Entities/ClinicReminderSettings.cs`, `Domain/Repositories/IClinicReminderSettingsRepository.cs`
- `Application/Common/Interfaces/IReminderSecretProtector.cs`, `.../IReminderSettingsProvider.cs`
- `Application/Common/Models/ResolvedReminderSettings.cs`
- `Application/DTOs/ReminderSettingsDto.cs` (+ `UpdateReminderSettingsRequest`), `Application/DTOs/ReminderSettingsMappings.cs`
- `Application/Features/Clinics/Queries/GetClinicReminderSettingsQuery.cs`
- `Application/Features/Clinics/Commands/UpdateClinicReminderSettingsCommand.cs`
- `Infrastructure/Services/ReminderSecretProtector.cs`, `.../ReminderSettingsProvider.cs`
- `Infrastructure/Repositories/ClinicReminderSettingsRepository.cs`
- `Infrastructure/Persistence/Configurations/ClinicReminderSettingsConfiguration.cs`
- `Infrastructure/Migrations/20260717192757_AddPerClinicReminderSettings.*`

**Backend — modified**
- `Domain/Entities/Notification.cs` (+`ClinicId`), `Infrastructure/.../NotificationConfiguration.cs`
- `Infrastructure/Services/IReminderChannelSender.cs` / `HttpSmsSender.cs` / `WhatsAppSender.cs` (settings param; senders read resolved settings, not config)
- `Infrastructure/Services/ReminderScheduler.cs` (per-clinic channels + stamp ClinicId)
- `API/BackgroundJobs/NotificationJob.cs` (resolve per-row settings)
- `Infrastructure/Extensions.cs` (DI + Data Protection key ring), `Infrastructure/*.csproj` (Microsoft.AspNetCore.DataProtection 8.0.0)
- `Infrastructure/Persistence/ApplicationDbContext.cs` (DbSet + query filter)
- `API/Controllers/ClinicsController.cs` (GET/PUT `reminder-settings`, AdminOnly)

**Frontend**
- `web/lib/api/reminder-settings.ts` (new), `web/components/reminder-settings.tsx` (new)
- `web/components/clinic-settings.tsx` (mount `<ReminderSettings/>` for admins, both modes)

**Tests (compile-fix only — see deviations)**
- `UnitTests/Infrastructure/Services/ReminderChannelSenderTests.cs`, `ReminderSchedulerTests.cs`
- `UnitTests/Api/NotificationJobTests.cs`

## Auto-Approved Deviations
| Deviation | Reason |
|-----------|--------|
| `IReminderSettingsProvider` + `ResolvedReminderSettings` placed in **Application** (interface in `Common/Interfaces`, model in `Common/Models`), impl `ReminderSettingsProvider` in Infrastructure. | Spec said "(Application)"; honored it. The resolved-settings type must be visible to both the Application interface and the Infrastructure senders — Application-layer placement keeps Clean Architecture intact (Infra→App). |
| Existing reminder tests (`ReminderChannelSenderTests`, `ReminderSchedulerTests`, `NotificationJobTests`) updated to the new sender signature / scheduler+job ctors. | Build-required compile fix: the changed `IReminderChannelSender.SendAsync` signature + new DI deps break these at compile. Adapted them to the new seam preserving the original assertions (kept green). **New per-clinic scenarios deferred to /test-small-feature** (see below). |
| Provider exposes two methods (`ResolveEnabledChannelsAsync` for the enqueuer, `ResolveAsync` for the dispatcher). | Avoids needlessly decrypting secrets at enqueue time; spec described one resolver conceptually — this is an internal split, same behavior. |
| Data-Protection key ring: Local → `.local/dataprotection-keys` (via `LocalInstallPaths`); Cloud → optional `DataProtection:KeyRingPath` (else framework default). | Directly implements the spec's mode-resolved key-ring note. |

## Deferred to /test-small-feature (new test scenarios, NOT written here)
- Per-clinic override wins over per-install; null toggle inherits install default (provider).
- Secret round-trip via `IReminderSecretProtector`; decrypt-failure → channel treated as NotConfigured (provider edge case).
- PUT write-only secret semantics (blank ⇒ unchanged; value ⇒ replaced); GET/PUT admin gate + tenant scoping; secret masking in DTO.
- `ReminderScheduler` stamps `Notification.ClinicId`; dispatcher resolves settings for a row's ClinicId (null → per-install).

## Significant Deviations
(none)

## Review Fixes Applied (/apply-review-fixes, 2026-07-17)
Challenged all 6 findings from `reviews/feature-review.md`; applied 5, skipped 1 (challenged). Left uncommitted
(manual-commit workflow — /finish-feature commits). Build 0 errors; **39 unit tests pass** (added 1).

| # | Sev | Verdict | Fix |
|---|-----|---------|-----|
| 1 | Major | Applied | `Extensions.cs`: key ring encrypted at rest — `ProtectKeysWithDpapi(protectToLocalMachine: true)` on Local/Windows (guarded `OperatingSystem.IsWindows()`); Cloud path relies on ACL (ops). |
| 2 | Minor | Applied | `NotificationJob.DispatchAsync`: honor resolved `EnabledChannels` — skip a row whose channel is disabled (leave Pending, like NotConfigured). Closes toggle-off-after-enqueue gap + makes the field meaningful. New test `Disabled_Channel_At_Dispatch_Leaves_Row_Pending_Without_Sending`. |
| 3 | Minor | **Challenged** | Duplicated admin-gate block matches the established repo convention (RegenerateClinicCode/BackupNow/etc. all inline resolve+IsAdmin). A one-off helper for 2 handlers diverges from the pattern — optional repo-wide polish, not a defect. |
| 4 | Suggestion | Applied | `ReminderSettingsProvider`: memoize resolution per clinic for the scoped instance (avoids per-row N+1). **Bug found & fixed during apply:** initial `Dictionary<Guid?,>` rejects null keys (legacy null-ClinicId rows) → switched to `Guid.Empty` sentinel; the null-ClinicId test caught it. |
| 5 | Suggestion | Applied | `ReminderSettingsProvider.ResolveSecret`: `NotificationType` enum instead of `"SMS"`/`"WhatsApp"` string labels (log + dedup key). |
| 6 | Suggestion | Applied | `WhatsAppSender`: `DefaultTemplateLanguage` const instead of inline `"fr"`. |

## Test Plan
Repo test surface: `ClinicManagement.UnitTests` (xUnit + Moq + `Assert.*`). No integration/Testcontainers
project → plain unit tests. No Postman/Newman, no FE test framework.

| AC | Action | Target | Notes |
|----|--------|--------|-------|
| AC-4, AC-5, AC-6 | New class | `Infrastructure/Services/ReminderSettingsProviderTests.cs` | override wins; null toggle inherits install; explicit false disables; identity/secret fallback; null clinicId → install |
| AC-3 (edge) | New class (same) | `ReminderSettingsProviderTests.cs` | decrypt failure → channel secret null (NotConfigured), no fallback to install secret |
| AC-2, AC-3 | New class | `Features/Clinics/UpdateClinicReminderSettingsCommandHandlerTests.cs` | non-admin rejected; write-only secret (blank ⇒ unchanged, value ⇒ Protect+store); create vs update |
| AC-1, AC-3 | New class | `Features/Clinics/GetClinicReminderSettingsQueryHandlerTests.cs` | admin masked DTO (configured flags, no secrets); no-row → all-inherit; non-admin rejected |
| entity | New class | `Domain/Entities/ClinicReminderSettingsTests.cs` | ctor Id=clinicId; blank→null normalization; secret setters |
| AC-4 | Add scenario | `Infrastructure/Services/ReminderSchedulerTests.cs` | enqueued Notification stamped with ClinicId |
| AC-5 | Add scenario | `Api/NotificationJobTests.cs` | provider resolved with the row's ClinicId; resolved settings passed to sender |

**Coverage notes (ACs with no unit surface):**
- **AC-7** (enabled channel, missing creds → NotConfigured, row stays Pending): already covered by the
  existing `NotificationJobTests.NotConfigured_Channel_Leaves_Row_Pending_Without_Failing` +
  `ReminderChannelSenderTests` NotConfigured cases (behavior unchanged by this feature).
- **AC-8** (committed appsettings gains no secrets; per-install path env-only): verified by review —
  no secret values were added to any `appsettings*.json`; per-clinic secrets live only in the encrypted DB.
- Data-Protection key-ring wiring + EF migration/global-query-filter are host/DbContext-level (integration),
  not unit-testable here; the provider's decrypt seam (`IReminderSecretProtector`) is unit-tested directly.
