<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing this shell holds may leave the device: `SharedPreferences` carries the clinic's server address and the
  WebView's cookie store carries the live `local_session`. A cloud backup or a device-to-device transfer would
  hand a working clinical session to whatever restores it.

  This is the Android-12+ replacement for `allowBackup="false"`, which stays declared for older versions.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
