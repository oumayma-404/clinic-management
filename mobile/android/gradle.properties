org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# The address a fresh install starts on — the hosted deployment, the only one an APK is published for.
# Mirrors the desktop shell's `ServerConfig.HostedHost`; `-PclinicServerAddress=…` still overrides it per build.
clinicServerAddress=app.apexa.tn
