namespace ClinicManagement.Domain.Enums;

/// <summary>Direction of a stock movement: a sortie (consumption) or an entrée (replenishment).</summary>
public enum StockMovementType
{
    Consume,
    Restock
}
