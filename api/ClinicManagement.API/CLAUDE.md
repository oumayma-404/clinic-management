# ClinicManagement.API

ASP.NET Core 8 Web API — the presentation/host layer of the clinic-management Clean Architecture solution. Thin controllers delegate to Application via MediatR; **self-issued JWT auth** (HS256, per-install key — Auth0/`Auth:Mode=Cloud` is retired with the `CloudBrowser` profile); Hangfire (PostgreSQL) for background jobs; **SignalR** for realtime; Serilog logging; Swagger in dev. Composition root is `Program.cs` (top-level statements, **returns `int`** — a non-zero exit is a deliberate operator-facing failure). It wires Application + Infrastructure (`AddApplication()`, `AddInfrastructure(config)`) and applies EF migrations on startup (mode-branched — see below).

## Controllers (`Controllers/`) — 30 route controllers + `ApiControllerBase`
All are thin: inject `IMediator`, send a command/query, map `Result.IsFailure` → an error response. All extend **`ApiControllerBase`**, which renders every failure as the single canonical body `{ "error": "<message>" }` (via `HandleFailure(result, statusCode=400)` / `Failure(message, statusCode)`; a blank message falls back to `ErrorMessages.Generic`). The action keeps ownership of the status code (400/401/403/404). Tenant scoping (clinic id) is resolved server-side from the token (`IClinicContext` → DB lookup), never from the route.

**Local-mode release gate (FR-E3, fail-closed):** in Local mode `AuthorizationPolicies.ConfigurePolicies(options, isLocalMode: true)` installs a `FallbackPolicy = RequireAuthenticatedUser()`, so any endpoint without an explicit `[AllowAnonymous]` returns 401 — "anonymous-by-omission" cannot exist. The approved anonymous set is the `ExpectedAnonymous` list in **`ControllerAuthorizationCoverageTests`** — the auth bootstrap, `Connectivity.Get`, `GoogleCalendar.Callback`, the four LAN trust routes and `Meta.ClientRequirements` — asserted **equal in both directions**, so a new `[AllowAnonymous]` fails the build until it is reviewed and a removed one fails too. Read it there rather than from a copy here; this paragraph carried a stale four-item list for two features. Cloud keeps a null fallback → unchanged.

**Role policies** (`AuthorizationPolicies`, in Application) — **four, and every one of them applied**, since
`adoption-qa-i-access-control-and-audit` (I1). The set used to be five, three of which
(`DoctorOnly`/`SecretaryOnly`/`DoctorOrSecretary`) had **zero usages** for the product's whole life while 33
endpoints carried a bare `[Authorize]` — any authenticated user, any role. Those three are **deleted**; a policy
nobody applies is not a capability, it is a comment that compiles.

| Policy | Means | Where |
|---|---|---|
| `Authenticated` | authenticated, **no role required** | `Auth`, `Clinics` — the onboarding surface only. Not a fig leaf: in Cloud the role reaches the JWT only after Auth0's `app_metadata` is written, which happens *after* joining a clinic, so `user-status`, `POST /clinics` and `POST /clinics/join` are genuinely reached by a role-less principal and any `RoleRequirement` would break Cloud onboarding outright |
| `AnyClinicRole` | admin **or** doctor **or** secretary | reception's job, per-patient money, **the patient's clinical record (read + record, never delete)**, and the shared reads every role needs (the bell, « Mon profil », the catalog lookups the pickers read) |
| `AdminOrDoctor` | admin or doctor | clinic-wide money, the corrective money operations, **deleting from the clinical record**, and the bulk/lifecycle patient operations (CSV import, archiver) |
| `AdminOnly` | admin | users, clinic + integration configuration, and the destructive operations whose effect cannot be read off any screen afterwards |

⚠️ **`AnyClinicRole` includes `admin` deliberately, and that is why it exists** rather than the old
`DoctorOrSecretary`. `CreateClinicCommand` makes a clinic's creator an **admin** and, for the « cabinet à un seul
dentiste » case, links a `Doctor` record to that same account — so in the common Tunisian practice the
owner-dentist's role is `admin`, and a literal `{doctor, secretary}` policy on the agenda, the patient list or
the till would lock the owner out of their own practice. `RoleAuthorizationHandler` grants **no** implicit admin;
a policy admits exactly the roles it names.

**Every one of the 32 route controllers now carries a class-level named policy — there are no bare `[Authorize]`
attributes left**, which is what lets the coverage guard demand an explicit policy on every action with **no
exemption list**. Per-action attributes then tighten (never loosen) the class: the money aggregates
(`invoices/revenue`, `billing/caisse`, `/caisse/ledger`, `billing/receivables`), the destructive deletes
(patient, expense, stock article → `AdminOnly`), and the plan-authoring actions. The two rows that stay
**deliberately open** to a secretary are the ones reception cannot work without: `POST /api/invoices/{id}/payments`
and `GET /api/patients/{id}/billing-summary`.

⚠️ **The clinical record moved to `AnyClinicRole`, reversing a decision `adoption-qa-i` recorded on purpose.**
Fiches de soins, the odontogramme, the antécédents (médicaux + familiaux) and the medical documents were
`AdminOrDoctor` at class level, so for a secretary the patient page's « Dossiers médicaux » and « Documents » tabs
were a wall of « Vous n'avez pas les droits » — the **reads** included. A practising dentist reported that the
assistant(e) is who fills much of the record in, and the old line was never true of the code around it anyway:
`PUT /api/patients/{id}` is `AnyClinicRole` and writes `Allergies`/`MedicalHistory`/`Notes`/`ImportantNotes`, while
`POST /api/patients` inserts `PatientMedicalHistory` rows — reception could always type a patient's history through
« Modifier » and was refused reading it one tab over. The charter is now **record yes, erase no**: the four deletes
(fiche, document + blob, antécédent médical, antécédent familial) keep `AdminOrDoctor`, and those four attributes
are the *only* gate left, so removing one is a silent widening. Pinned by `ClinicalRecordAccessTests` (expectation
table + a drift guard on new actions) and `ClinicalRecordDeletionAuthorizationTests`.
⚠️ **Opening document authorship required a second fix, not just a policy edit.** `PractitionerRenderSnapshot`
resolved the cachet + n° d'ordre CNOMDT from the **caller's** `Doctor` record, so a document authored by anyone
without one rendered with *no* practitioner identity — silently, on forms whose whole purpose is to carry it.
`ResolveAsync` now takes an `IssuingDoctorId` (chosen practitioner → caller → none, the chosen id tenant-checked
against the clinic roster) and the editor sends the practitioner it already had the user pick. No schema change was
needed: the *resolved* snapshot has always been persisted into the document's `ContentJson`, so what was missing
was a selector on the request, not a stored FK — which is what the old « would require a persisted DoctorId » note
had concluded. ⚠️ `IssuingDoctorId` is deliberately **not** stripped from the `generate-pdf-download` body, unlike
the four reserved snapshot values: it is a selector checked against the caller's own roster, whereas
`DoctorCachetKey` is a storage key the unauthenticated `PdfGenerationJob` dereferences.

### Core clinical / scheduling
| Route | Controller | Notable | Auth |
|---|---|---|---|
| `api/appointments` | `AppointmentsController` | GET (list, date/doctor filter), GET/{id} (single — notification deep-link target; other-clinic id → 404), POST, PUT/{id} (cancel via `status="Cancelled"`; also **moves/clears the treatment-plan act link** — `treatmentPlanId` + `treatmentPlanItemId`, validated by `AppointmentPlanLink`. **Tri-state**: omitting the field leaves the link untouched, an explicit `null` clears it — see `UpdateAppointmentCommand.TreatmentPlanItemIdSpecified`); **recurring series**: `GET/POST recurring`, `POST recurring/{id}/cancel` (scope Occurrence/Following/WholeSeries) | `AnyClinicRole` |
| `api/notifications` | `NotificationsController` | In-app staff feed: GET (50 newest, viewer-scoped), `GET unread-count` (`{ unreadCount }`), `GET pending-reviews` (post-visit "how was the visit" popup), `PUT {id}/read` (tenant/missing → 404), `PUT read-all` | `AnyClinicRole` |
| `api/patients` | `PatientsController` | GET (list), GET/{id}, POST, PUT/{id}, `GET export` (CSV), **`GET file-summaries`** (the « Fichiers » directory — one page of patients with their file count, total bytes and last upload; `searchTerm`/`withFilesOnly`/`sort`, all in SQL. A literal segment, so it is matched ahead of `{id}`, and `sort` binds as a **string** the action clamps: bound onto the enum, `[ApiController]` would turn a typo in a shared URL into an automatic 400). **`POST import/preview`** (L5 dry run — reads the uploaded CSV, applies the mapping, reports per row, **writes nothing**) and **`POST import`** (the commit). Both multipart, 8 MB cap; `mapping` is a **JSON string** and `createAnywayLines` a comma-separated list, because a multipart form cannot carry a nested object and the per-key workaround (`mapping[LastName]=0`) binds partially and *silently*. ⚠️ Nothing is staged between the two calls: the commit re-sends the same file and mapping, which is what lets one planner produce both answers | `AnyClinicRole`; **`DELETE {id}` + `{id}/deletion-check` `AdminOnly`**, `archive`/`unarchive` **and both `import` actions** `AdminOrDoctor` |
| `api/patients/{id}/medical-history` · `/family-history` · `/dental-records` · `/files` · `/odontogram` | `PatientMedicalHistoryController`, `PatientFamilyHistoryController`, `DentalRecordsController`, `PatientFilesController`, `OdontogramController` | Per-patient history, dental charting, storage-backed files, tooth-state history. **`PUT /files/{fileId}`** (rename / describe / move, tri-state) and **`PUT /files/folders/{folderId}`** (rename) are `patient-file-uploads` P4 — the first callers of `PatientFile.Rename`/`UpdateDescription`/`MoveToFolder` and `PatientFolder.UpdateName`; they stay on the class policy because recording is reception's job (AC-4.4). **`GET /files` is paged** (`page`/`pageSize`, AC-5.9) and returns the standard envelope; omitting both still reads everything. **Every one of the four deletes is `AdminOrDoctor`** and that is now the only gate on them: deleting a fiche detaches the invoice lines and devis acts built from it, and an antécédent is where an **allergy** lives. `odontogram DELETE conditions/{id}` is the exception that stays open — it removes a charted *diagnosis* (charting's undo) and cannot touch a treatment entry | **`AnyClinicRole`** — read + record; the four `DELETE`s `AdminOrDoctor`. (`/files` was always `AnyClinicRole` — reception scans documents in.) |
| `api/patients/recalls` | `RecallController` | Recall / relance: due list, `settings` GET/PUT (interval months), `{id}/contacted`, `{id}/snooze`, `{id}/send` (SMS/WhatsApp, connectivity-gated) | `AnyClinicRole` (+ `AdminOnly` on `settings` PUT) |
| `api/waiting-list` | `WaitingListController` | Salle d'attente CRUD + `{id}/promote` (→ real appointment) | `AnyClinicRole` |
| `api/lab-orders` | `LabOrdersController` | Prosthetics/lab work orders CRUD + `{id}/status` (Sent/InProgress/Received/Fitted). The status action now surfaces the domain's **transition table**: an illegal move returns the aggregate's French message as a 400, and `LabWorkOrderDto.AllowedNextStatuses` tells the UI which stages to offer | `AnyClinicRole` |
| `api/doctors` · `api/procedure-types` · `api/stock` | `DoctorsController`, `ProcedureTypesController`, `StockController` | Doctor profiles, procedure catalog, stock/inventory. `GET procedure-types` takes a **`category`** filter alongside `search` (both applied in SQL before the page is cut; an unrecognised category matches nothing rather than failing, so a stale bookmark shows an empty list); **`GET procedure-types/categories`** serves the clinical disciplines to offer — the canonical set unioned with the clinic's own. Sibling of `GET colors`, and served rather than shipped as a browser constant for a stronger reason: only the server knows which categories the clinic has used | `AnyClinicRole` (catalog **writes** `AdminOnly`; **`DELETE /stock/{id}` `AdminOnly`**; `PUT /doctors/{id}` + its working hours `AdminOrDoctor`) |
| `api/suppliers` | `SuppliersController` | **Les fournisseurs** (`stock-fournisseurs`) — the cabinet's outside contacts: laboratoires de prothèse, laboratoires d'analyses, dépôts dentaires, maintenance. `GET` (q · category · includeInactive · paged; omit paging for the pickers), `POST`, `PUT/{id}`, `DELETE/{id}`. ⚠️ **`AnyClinicRole` throughout, deliberately**: ordering supplies and chasing a prothèse is reception's job and none of this is clinic-wide money, so `AdminOrDoctor` would leave the person who actually phones the laboratory unable to find its number. ⚠️ The delete is **refused** for a referenced fournisseur with `supplier_in_use` naming the counts **per table** and pointing at « Désactiver »; the `ON DELETE RESTRICT` FK is the backstop, and reaching it would surface as a 500 instead of the French sentence | `AnyClinicRole` |
| `api/dashboard` | `DashboardController` | **`GET api/dashboard?period=Today\|Week\|Month`** (default `Month`) → the whole dashboard in one call: the resolved window, the comparable **Activité** (RDV honorés, nouveaux patients, taux d'absence, devis acceptés) and **Argent** (encaissé, facturé, dépenses, net) sections, the point-in-time **créances** total, the **À-traiter** counts (salle d'attente, devis en attente, relances, prothèses en retard, stock bas/périmant), and a 6-month collected **trend**. `period` is the **only** window input — both bounds and the previous window are derived server-side (`DashboardPeriod` + `ClinicClock`). *(Replaced `GET stats`, which took six client-supplied boundaries; deleted.)* | **`AdminOrDoctor`** — the Argent section *is* the clinic's revenue |
| `api/medical-documents` | `MedicalDocumentsController` | Document CRUD (JSON or multipart+`IFormFile`) — ordonnance, certificat, lettre de liaison, bulletin CNAM and arrêt de travail are all this one controller, discriminated by `DocumentTypes`. `POST {id}/generate-pdf` (enqueues `PdfGenerationJob`, returns `{ jobId }`), `POST generate-pdf-download` (inline PDF; cachet/ordre/city are re-resolved server-side, never trusted from the body — but `issuingDoctorId` **is** honoured, see the ⚠️ above). **`DELETE {id}` stays `AdminOrDoctor`** — a signed clinical instrument, and deleting it takes the blob with it | **`AnyClinicRole`** (documents are typed at the desk); `DELETE` `AdminOrDoctor` |

### Billing / CNAM
| Route | Controller | Notable | Auth |
|---|---|---|---|
| `api/invoices` | `InvoicesController` | Notes d'honoraires (lines + payments), issue/pay, PDF; cancel-issued is `AdminOrDoctor`. **`POST from-dental-record/{id}`** bills a fiche de soins — prices its acts, **issues** the note and optionally records the payment, atomically. ⚠️ Unlike `from-plan` it does *not* return a draft: a number is consumed, so a correction means an avoir and the client must confirm first | `AnyClinicRole`; **`revenue` `AdminOrDoctor`**, and `AdminOrDoctor` on cancel / void / avoir / `DELETE`. `POST {id}/payments` stays open — reception takes the payment |
| `api` | `BillingController` | Unified per-patient billing summary (`solde patient`) + caisse. **`GET billing/caisse/ledger`** is the « extrait de caisse »: every movement behind the totals, oldest first, over the *same* window and the same clinic-local default as `billing/caisse` — divergent windows would make the lines describe a different period from the figures above them | class `AnyClinicRole`, but **`billing/caisse`, `/caisse/ledger` and `billing/receivables` are `AdminOrDoctor`** — this controller *is* the fork the two policies exist for: `patients/{id}/billing-summary` and the payment receipt stay open because reception cannot collect without them, while the clinic-wide totals do not |
| `api/expenses` | `ExpensesController` | Clinic expenses / caisse cash-out CRUD | **`AdminOrDoctor`** (an expense moves the reported Net) + **`AdminOnly` `DELETE`** (deleting one silently *raises* Net) |
| `api/treatment-plans` | `TreatmentPlansController` | Devis / plans (planned acts + installments). GET responses carry a **derived read-back** — per-act `scheduledAppointmentId/At/Status`, plan-level `itemsDone/itemsTotal`, `nextAppointmentAt` and the linked-invoice triple — computed per request by `TreatmentPlanWorkflowProjection` (two batched reads, never per plan). **Amendment**: `POST {id}/amend` (add/remove acts + échéancier) and `PUT {id}/installments` are **`AdminOrDoctor`** — they alter what a patient owes on a numbered document, the same class as cancelling an issued invoice; `PUT {id}/items/order` carries **no** method-level policy (reordering is cosmetic). `POST {id}/cancel` is `AdminOrDoctor` too. All four are pinned by `TreatmentPlansControllerAuthorizationTests`, which also **fails on any unclassified new action**. *(Seeding a plan from the odontogram is a **frontend** prefill — there is no backend seed command.)* | `AnyClinicRole` for reading, collecting and printing; **`AdminOrDoctor` for authoring** (create / update / accept / complete / amend / revise / reorder / cancel / delete) |
| `api/cnam-nomenclature` · `api/dental-acts` · `api/medications` | `CnamNomenclatureController`, `DentalActsController`, `MedicationsController` | CNAM nomenclature/letter-values (backs BS1 estimate), dental-act catalog, medication catalog (ordonnance picker). Read endpoints `AnyClinicRole` (the pickers inside a form need them); **catalog-write endpoints are `AdminOnly`**. ⚠️ **`POST cnam-nomenclature/reimbursement-estimates`** (P6, AC-P6.15) is the one action on a catalog controller that uses a mutating verb and is *not* admin-gated: it is a **read** — the acts of one bulletin are a list, and a GET would have to encode N cotations plus N care dates into the query string — and gating it would put the estimate out of reach of the secretary filling the bulletin in. It persists nothing. `AdminSurfaceCoverageTests` carries it as a named `NonMutatingExemptions` entry (declaring type + action name, never a predicate) plus a second test asserting the exemption still names a real action, so a rename cannot leave a pre-approved hole | `AnyClinicRole` + `AdminOnly` writes |

### AI / auth / infra (Local-oriented)
| Route | Controller | Notable | Auth |
|---|---|---|---|
| `api/auth` | `AuthController` | **Local-account profiles only** (`login`/`refresh`/`setup` 404 where `UsesLocalAccounts` is false; `mode` always works). `mode` → `{ mode, selfRegistrationEnabled, publicSignupEnabled, requiresSubscription }` — the second field was added by US-3 and is **not derivable from the first**: the browser learns the mode from Next's own `AUTH_MODE`, which reads `local` on a clinic's PC *and* on the hosted backend, so `/join` could not tell them apart. `login` (email+pw → JWT, 401 on fail), `setup` (first-run clinic+admin — **loopback-gated**, and a one-time bootstrap; over the internet the `provision-clinic` verb replaces it), `register` (clinic-code self-registration — **creates the account *inactive*, pending an admin's activation**, I5). ⚠️ **`register` is gated on `AllowsSelfRegistration`, not `UsesLocalAccounts`** (US-3): the old guard was true in *both* account-owning profiles, so hosting would have left a **six-character** clinic code as the only barrier between the internet and an account that reads every patient record. `change-password` (no method policy: the class's `Authenticated` is right, since a forced post-reset change may have no role in the JWT yet). **`signup` + `signup/verify`** (`clinic-self-signup`) are the newest bootstrap pair: the first writes a pending `ClinicSignup` and emails a single-use token, the second consumes it and provisions the clinic through `LocalClinicProvisioning`. Both 404 **before the mediator** where `AllowsPublicClinicSignup` is false, so the handler and its mail sender are never resolved. ⚠️ Gated on that capability and **not** on `AllowsSelfRegistration`, which answers the opposite question (« join an existing clinic with its shared code », still ✗ here) — and `mode` reports the two as separate fields for exactly that reason. ⚠️ `signup` returns **202** with one neutral sentence for every outcome (no enumeration oracle) and `signup/verify` returns **no session at all** — no token, no cookie; the visitor signs in at `/login` with the password they chose. ⚠️ **`requiresSubscription`** (`clinic-subscription` Part C) is a fourth field on the same read, and the client mounts the « Abonnement » entry and (Part D) the banner from **it**, never from probing `GET /api/subscription`: a network failure and a genuine 404 are indistinguishable to a probe, and EC-13 requires a failed read to be *retryable* rather than read as « aucun abonnement ». The 404 stays as the server-side guarantee | class `Authenticated`; the bootstrap actions `[AllowAnonymous]` |
| `api/clinics` | `ClinicsController` | Clinic CRUD + settings; most settings writes + `regenerate-code` are `AdminOnly` | `Authenticated` (the onboarding trio is role-less); `PUT` + settings writes + `regenerate-code` `AdminOnly`; `reminder-log` and `logo` `AnyClinicRole` |
| `api/users` | `UsersController` | Admin user mgmt: list, **`POST` (create a colleague's account — US-3)**, `{id}/reset-password`, `{id}/status`, **`PUT {id}/role`** (admin/doctor/secretary; validated against `User.AssignableRoles`, keeps email + full name, refuses a self-demotion leaving no other active admin, bumps `TokenVersion`). ⚠️ **`POST` had to be added because every other action here operates on an account somebody else created** — until US-3 the only door was `auth/register`, which `HostedMultiTenant` closes, so a hosted clinic could not add staff at all. It returns the one-time password once (`CreatedClinicUserDto`, deliberately not `ClinicUserDto` + a field), creates the account **active** (unlike a self-registration: this *is* the approval), takes the clinic from the caller's own record — there is no clinic id on the command — and 404s where `UsesLocalAccounts` is false. Gated on that and **not** on `AllowsSelfRegistration`: those are opposite questions, and this is what a profile with self-registration closed uses *instead* | `[Authorize(AdminOnly)]` (class) |
| `api/googlecalendar` | `GoogleCalendarController` | OAuth + manual sync. `POST connect` (mints CSRF `state` → server cache + HttpOnly cookie; **`AdminOnly`**), `GET callback` (**only** `[AllowAnonymous]`; validates state double-submit, exchanges code, persists refresh token **onto the clinic entity** — per-clinic isolation, DB — then redirects to `FrontendUrl`), **`POST disconnect`** (`AdminOnly`; the one action here that goes through **MediatR** — `DisconnectGoogleCalendarCommand` — because it is an ordinary clinic mutation, which makes it unit-testable and gets the realtime broadcast, unlike the OAuth plumbing that legitimately works the repositories directly), `sync-from-google`/`sync-appointment/{id}` (`AdminOnly`), `status`, `redirect-uri` | class **`AdminOnly`** (integration administration); `status` `AnyClinicRole` (every role's agenda reads it); `callback` `[AllowAnonymous]` |
| `api/connectivity` | `ConnectivityController` | **Local-only** (404 in Cloud). `GET` → `ConnectivityStatusDto { internetReachable }`, server-side egress probe polled by the frontend to gate AI + Google Calendar offline | class `AnyClinicRole`, the single action `[AllowAnonymous]` |
| `api/meta` | `MetaController` | **`GET /api/meta/upload-policy`** (`patient-file-uploads` P3, AC-5.1) → what the patient-file door accepts, projected from `FileTypeCatalog`: the picker's `accept` string, the per-format caps and previewability, and the server's **own** French refusal sentences, so a client-side pre-check cannot word a refusal differently from the server that re-checks it. Deliberately *not* on the client-version floor's exemption list — only `client-requirements` earns that. Plus **`GET /api/meta/client-requirements`** (`mobile-native-shells` P3, AC-28) → the native shells' version floor, the release the stores carry, and both store URLs, read from operator config (`Clients:*`). ⚠️ It is **both** `[AllowAnonymous]` **and** the one `/api` route `ClientVersionMiddleware` exempts from the floor — a below-floor shell must be able to read it, or the single endpoint that says where to update is the single endpoint it cannot reach (AC-29). The DTO it returns, `Models/ClientRequirements`, is also what the middleware measures a caller against, so the floor a client is *told* about is the floor it was refused by | class `AnyClinicRole`; **two** actions `[AllowAnonymous]` — `client-requirements` and `client-download` |
| `api/push-devices` | `PushDevicesController` | **OS push registry** (`mobile-native-shells` P6). `POST` registers / refreshes / **rebinds** one device token — one verb for all three, because the shell cannot tell them apart; `DELETE {token}` deregisters (idempotent); **`GET availability`** reports what this installation can do **per platform** (AC-51/AC-52). ⚠️ The two writes **404 where the deployment supports neither platform** — absent rather than present-and-always-refusing, so a shell learns « no push here » in one call instead of reading a 400 as retryable — while `availability` deliberately answers anyway: it is the endpoint that *says* push is off, so refusing it would make the one call that can explain the state the one call that cannot be made (the same exemption logic as `meta/client-requirements` and its own floor). ⚠️ **`AnyClinicRole`, and that is the point**: the notifications this subscribes to are the agenda events reception acts on first, so `AdminOrDoctor` would exclude the role most likely to be holding the phone. Nothing here reads clinic-wide money | `AnyClinicRole` |
| `api/platform/auth` | `Controllers/Platform/PlatformAuthController` | **The vendor console's sign-in** (`platform-console`). `login` (e-mail + password + a one-time code), `totp/enrol` (binds the secret the bootstrap verb issued and returns the recovery codes **once**), `recovery` (a single-use code) and `password` (AC-8.6 — the only account action on the web). ⚠️ Reachable **only** on the console's own Kestrel listener: `ConsolePortGate` 404s `/api/platform/*` on the public port, 404s everything else on the console port, and 404s every console path when `Console:Port` is 0. ⚠️ The status comes from the refusal's **`code`**, never its French sentence — the spec gives these four distinct statuses, and recovering one by matching prose is the `Contains("déjà facturée")` defect. The three anonymous actions are on `ControllerAuthorizationCoverageTests`' reviewed list and inside the anonymous-auth rate limits | `[Authorize(PlatformConsole)]` (class); `login`/`totp/enrol`/`recovery` `[AllowAnonymous]` |
| `api/platform` | `Controllers/Platform/PlatformPortfolioController` | **The vendor's portfolio** (`platform-console` Part 2). `GET clinics` (state · dormant · q · sort · page) and `GET summary`. ⚠️ **Read-only by construction** — there is no write action here, and « it has no write path » is the strongest proof a surface that can read every practice can offer. ⚠️ Every activity figure comes from the per-cabinet **counter snapshot**, never from the ledger per request, which is what lets `sort=activity` and `dormant=true` apply to the *portfolio* rather than to the page already selected (AC-2.4a). ⚠️ What it may return is a **closed set of field names** (`PlatformReadShape`), enforced by a build-failing reflection test — not by the tenant filter, which is deliberately lifted here (AC-7.2a). Same `ConsolePortGate` reachability as the auth controller above. **Part 3 adds `GET clinics/{clinicId}`** — the list's figures, the six-month trend and the administrator's contact. ⚠️ It is the one console read that **writes**: it records a `PlatformAccessEntry` (AC-7.3), while `GET clinics` records nothing (AC-3.5) because one list read touches every cabinet. ⚠️ A cabinet deleted since the list was drawn is **404 with `code: "clinic_not_found"`**, and the console branches on the code, never on the French sentence. ⚠️ **`state` was missing from `ListClinics`' signature from Part 2 until it was reported, so all four of AC-2.3's entitlement filters were dead end to end** — the console sent `?state=expired`, model binding had nowhere to put it, and the read narrowed nothing while every layer behind it (the SQL predicate, `ParseState`, the screen's chips) was correct. `PlatformPortfolioQueryTests` asserts the *handler* forwards every filter it is given, which it did; nothing looked at the wire hop. `PlatformPortfolioControllerTests` now holds both halves — the six arguments arriving verbatim, and a **derived** guard that every settable property of `ListPlatformClinicsQuery` has a parameter to arrive on, so the next filter added cannot be dropped in silence. ⚠️ **`sort` falls back to `createdAt`, the newest cabinet first**, and the console's own default is the same value (`DEFAULT_PORTFOLIO_SORT`) — which is what lets the default be omitted from the URL; moving one side alone leaves « Création » looking active on an alphabetical list | `[Authorize(PlatformConsole)]` (class) |
| `api/platform` | `Controllers/Platform/PlatformSubscriptionsController` | **The console's four writes** (`platform-console` Parts 4–6, US-4/US-5/US-6). `POST clinics/{clinicId}/subscription-periods` records a payment already received and extends the cabinet's entitlement; **`POST clinics/{clinicId}/subscription-periods/{entryId}/cancellation`** strikes one entry through with a mandatory motif and lets the date recompute. ⚠️ **Their own controller, deliberately**: `PlatformPortfolioController` says « read-only by construction », and that claim stops being checkable the moment one action on it writes. ⚠️ Both carry **`[AllowsWithoutSubscription]`** — a console account is not a cabinet, so they pass the gate on that ground today, but « the endpoint whose purpose is to END a refusal must never be refused » belongs at the endpoint rather than inferred from how the tenant scope happens to resolve; for the cancellation the lapse is often *what the mis-keyed entry caused*. ⚠️ **A repeated submission returns the FIRST outcome, not a conflict** (AC-4.6), and two *different* grants both land (EC-6): the surplus one is corrected by a cancellation, never by refusing money already received. ⚠️ **The cancellation is a POST and not a DELETE, and it carries no idempotency key** — nothing is deleted (AC-5.2), and « déjà annulée » is a **409 with `period_already_cancelled`** rather than a silent replay, because that entry was struck through by *somebody* and the vendor needs to see who. ⚠️ **No action here names any of the three vendor commands** — `SubscriptionVendorCommandReachabilityTests` source-scans `Controllers/` for those type names, and a `using` or a comment is enough to fail it, which is why the wrappers are `CancelSubscriptionPeriodFromConsoleCommand` and `SetClinicSuspensionFromConsoleCommand` (neither contains `CancelSubscriptionPeriodCommand` or `SetSubscriptionSuspensionCommand` as a substring). **Part 6 adds `POST clinics/{clinicId}/suspension` and `…/suspension/lifting`** — a cabinet stopped for abuse, and released. ⚠️ **Two routes over one command**, so the direction is the URL and a truncated body cannot flip « suspendre » into « lever »; lifting carries no body at all. ⚠️ **A POST and not a DELETE** for the lift too: what is cleared is a flag on a live entitlement, and the two journal rows stay for ever. ⚠️ Both carry `[AllowsWithoutSubscription]` on a stronger argument than the payment routes' — suspension is **not** a payment state (AC-6.3), so making it wait on the cabinet's own entitlement would be incoherent in both directions: a fraudulent practice that stopped paying is exactly the one still to be stopped, and a mistaken suspension must stay liftable. ⚠️ « Déjà suspendu » / « pas suspendu » are **409s with codes**, not silent successes | `[Authorize(PlatformConsole)]` (class) |
| `api/platform` | `Controllers/Platform/PlatformAccessLogController` | **« Journal » — the console's own access ledger** (`platform-console` Part 3, FR-5, AC-7.3). `GET access-log` (`accountId` · `clinicId` · page), newest first, with the « Compte » filter's options on the response itself, derived from the rows so an account that has opened nothing is never offered. **Read-only by construction**, exactly as `AuditController` is: rows are written only by the reads and writes they record, and there is no action that creates, edits or deletes one. ⚠️ A **console** read, not a clinic one — showing a practice which vendor account opened its file is named out of scope by the spec, so there is no counterpart under `/api/audit`. ⚠️ A ledger nobody can read is a promise nobody can check, which is why this endpoint is part of the guarantee rather than a convenience on top of it | `[Authorize(PlatformConsole)]` (class) |
| `api/meta/webhook` | `MetaWebhookController` | **Meta's template-status deliveries** (`vendor-whatsapp-messaging-quota` § 34) — `GET` answers the subscription handshake with `hub.challenge`, `POST` applies a `message_template_status_update` to the cabinet it names. ⚠️ **`X-Hub-Signature-256` is verified over the RAW body**, and an **unconfigured** `Meta:AppSecret` refuses every delivery rather than trusting it. ⚠️ **`UseSystemWide` is its first act**: Meta authenticates as nobody, so with no scope the clinic filters return **zero rows** and the write would silently apply to nothing while answering 200 — which is why its test asserts with the *real* `TenantScope` rather than a hand-set one. It resolves the cabinet by `GetByWhatsAppBusinessAccountIdAsync`, not by any id in the URL. ⚠️ It is the **second** writer of template state; the daily poll is the other, and neither is a substitute — a webhook can be missed and a poll can lag. Both actions are on `ControllerAuthorizationCoverageTests`' reviewed anonymous list, and the controller is named in `SystemWideCallerCoverageTests`' second reviewed set (`ScopedDespiteHavingAnHttpContext`) with a sibling assertion that the file still exists, so a rename cannot leave it checking nothing | the two actions `[AllowAnonymous]`; 404 where `SellsVendorMessaging` is false |
| `api/platform` | `Controllers/Platform/PlatformMessagingController` | **The vendor's forfait writes** — record an allocation, cancel one. Its own file for `PlatformSubscriptionsController`'s reason, both `[AllowsWithoutSubscription]`, and 404/409 decided by **code** and never by the French sentence. ⚠️ **It names neither vendor command** — `MessagingVendorCommandReachabilityTests` source-scans for those type names and a **docstring mention is enough to fail it**, which it did once — so the controller describes the commands rather than naming them | `[Authorize(PlatformConsole)]` (class) |
| `api/audit` | `AuditController` | **`GET /api/audit`** — « Journal d'activité », the audit ledger (I6): one page of the clinic's mutations newest-first (`entityType`/`entityId`/`from`/`to`/`action` filters, paged through `PageRequest`), plus the entity types it has rows for. **Read-only by construction** — rows are written only by `AuditSaveChangesInterceptor` and there is no endpoint that creates, edits or deletes one; a ledger with a write endpoint is a ledger somebody can correct | `[Authorize(AdminOnly)]` (class) |
| `api/subscription` | `SubscriptionController` | **« Abonnement »** (`clinic-subscription` Part C, US-2) — `GET /api/subscription` (state, end date, countdown, forfait, the deployment's **published tariff**, how to pay, who to contact) and `GET /api/subscription/history` (one page of the ledger, newest first, a cancelled entry kept and struck through). **Read-only, and there is no write endpoint anywhere in this feature** (FR-6) — granting, cancelling and suspending are vendor console verbs, because a cabinet able to extend its own entitlement over HTTP would not have one. ⚠️ **The class policy is `AnyClinicRole`, a deliberate exception** to « a secretary sees no clinic-wide money screen »: these amounts are owed to the software *vendor*, reach no clinic money read (FR-2), and reception is who meets the refused save that points here (AC-2.2, EC-10). Only the **history** is `AdminOnly`. ⚠️ **Both actions 404 *before the mediator*** where `RequiresSubscription` is false (`AuthController`'s `AllowsPublicClinicSignup` precedent), so on the other two deployment kinds the handler, its repository and its pricing accessor are never resolved — AC-7.1/7.2 is « byte for byte unchanged », not « unchanged plus two reads ». ⚠️ **The `GET` reads the ledger**, unlike the gate: the entitlement row cannot say whether the cover in force is the free **trial**, which is what `SubscriptionStateReader`'s `isTrial` parameter exists for. Both carry `[AllowsWithoutSubscription]` as **documentation only** — the gate never inspects a GET, and the coverage guard is blind to them by design | class `AnyClinicRole`; `history` `AdminOnly` |
| `api/outbox` | `OutboxController` | **`GET /api/outbox`** (multi-tenant-cloud US-6) — the depth of the clinic's two background queues: reminders (pending / **due** / blocked / failed-recent) and document emails — each with **the age of its oldest waiting row**. ⚠️ **The age is the diagnosis, not the count**: « 40 pending » says nothing, since a reminder for next Tuesday is *supposed* to be waiting, while a row due three hours ago says the dispatcher is not running. It exists because `/hangfire` is loopback-only in **both** modes and behind a reverse proxy every request's `RemoteIpAddress` is the proxy container — correct as security, total as blindness — and because US-2's own R-1 is that a job with no tenant scope reads **nothing** and logs a clean run. ⚠️ Each queue's predicates are a **copy of its own dispatcher's**, clause for clause, or the read would report a backlog nothing will ever drain. Read-only, and there is no retry/clear action: a second write path into an outbox is how the depth and the queue stop agreeing. Named sections rather than one uniform row per queue, because only reminders can be `Blocked` and a document email has no scheduled instant — a zero standing in for a concept that does not exist is not a statement. Deliberately **no frontend caller**: its consumer is the operator, like `/health` | `[Authorize(AdminOnly)]` (class) |
| `api/backup` | `BackupController` | **`GET file-manifest`** (`patient-file-mirror`) is the newest read: one page of **every** file the cabinet holds — id, patient, name, type, size, upload time — so a machine keeping a browsable copy can work out what it is missing without opening each patient in turn. ⚠️ **A manifest, not a download**: it yields no bytes and **no storage key**, so it adds a *listing* reach and not a second, unguarded way to name an object in the store; fetching one file goes through `GET /api/patients/{patientId}/files/{fileId}/download`, which re-checks that patient's own clinic. ⚠️ **`AdminOnly` like the archive, not `AnyClinicRole` like the per-patient file list** — reading one patient's documents is reception's daily work, while enumerating the whole cabinet's holdings in one call is the same class of read as taking the archive out. ⚠️ **No step-up**, unlike `GET archive`: that one guards a single action handing over the entire record in one response. ⚠️ Its handler lives in `Features/Backup/Queries`, **not** a new `Features/<Area>` folder — a new area emits a realtime resource key `web/lib/realtime/clinic-hub.ts` must then declare, and this read has no screen and nothing to broadcast. ⚠️ It orders **ascending** on `UploadedAt` then `Id`, against every other list in the product: a caller walking the pages is racing uploads, and newest-first would push an unread row past the cursor every time somebody scans a document. `POST` → `BackupNowCommand` (pg_dump + **`pg_restore --list` verification** + file-storage copy to a timestamped folder); success carries `verifiedObjectCount`, failure = operator reason as 400 (never silent). **`GET history`** → the recorded attempts, newest first, plus « Dernière sauvegarde réussie », the resolved default destination and the schedule — the read that made « when did a backup last work? » answerable at all (it used to live in a React `useState`). **`PUT schedule`** → the unattended-backup settings, the caller `Clinic.SetBackupSettings` ships with. ⚠️ **The two writes 404 where `DeploymentProfile.BacksUpItsOwnData` is false** (both hosted kinds): the `deploy/` `backup` sidecar already dumps that deployment off-server, and one database holds every cabinet, so an in-app `pg_dump` — which has no tenant predicate — would be both weaker and a cross-tenant read. Absent rather than present-and-refusing, `push-devices`' shape. ⚠️ **`GET history` deliberately answers anyway**, returning `managedByHost: true`: it is the read that *explains* why there is no button, so refusing it would leave the screen unable to say anything — the same exemption logic as `meta/client-requirements` against its own version floor. Before this, a hosted clinic met « L'outil pg_dump est introuvable » on that panel plus a « sauvegarde périmée » bell alert nothing available to them could clear. **`GET archive` + `POST archive/restore`** (`clinic-data-archive-and-restore`) are the per-clinic answer `pg_dump` structurally cannot be: a tenant-filtered `.zip` of one cabinet's rows and blobs, and an **additive** restore keyed on the original ids — missing rows re-inserted with their own ids and document numbers, present-and-identical rows counted and untouched, present-but-**different** rows skipped and counted apart. ⚠️ **Neither is gated on `BacksUpItsOwnData`**, unlike the three above, and the asymmetry is the point: that capability turns `pg_dump` off because it takes `--dbname` and has no tenant predicate, and neither half of that applies to a read that goes through the tenant filter like every CSV export. ⚠️ Both carry `[AllowsWithoutSubscription]` (AC-8), and the download is a GET the gate never inspects anyway. ⚠️ The restore's cap is `Backup:ArchiveMaxSizeMb`, applied by **`[ArchiveUploadLimit]`** (`API/Filters/`) before the form is bound and re-read by the action's own French refusal. `[RequestSizeLimit]` takes a compile-time constant and could not be operator-set; but checking it in the action alone was **dead code above 128 MB** — `[DisableRequestSizeLimit]` lifts Kestrel's body cap and nothing else, so the form reader's own `MultipartBodyLengthLimit` threw during model binding and the caller got a generic 500 naming no limit, which is exactly the outcome the check exists to avoid. The value is hand-parsed (`GetValue<int>` *throws* on a typo, on the path a practice uses to recover its records). The console door carries the same filter; it had no size gate at all. ⚠️ **The download returns a `Stream` over a self-deleting temp file**, not a `byte[]`: the archive was built into a `MemoryStream` and copied again by `ToArray()`, so a cabinet with twenty years of radiographs cost ~2× the archive in contiguous large-object-heap allocations before a byte reached the client — on a process shared with every other cabinet | `[Authorize(AdminOnly)]` (class) |
| `api/platform` | `Controllers/Platform/PlatformClinicRestoreController` | **The vendor re-creates a cabinet from the archive its owner kept** (`clinic-data-archive-and-restore`) — `POST clinics/restore`, the only path that works when the practice's own accounts are gone too. ⚠️ **Its own controller**, on `PlatformSubscriptionsController`'s precedent and for a second reason: this is not a subscription action either, so folding it in would make « the console's writes are about entitlements » false as well. ⚠️ **It restores the `Clinic` row rather than provisioning a cabinet** — provisioning first creates a *new* row, leaving the archive's own « présent mais différent » and therefore skipped, i.e. the practice back with its patients and its money but a blank name, no billing settings and no working hours. Only what an archive deliberately does not carry is created after: the admin (one-time password, shown once) and the entitlement, the latter through `LocalClinicProvisioning.StageEntitlementAsync` rather than a second answer. ⚠️ A cabinet that is **still live** is **409 `clinic_exists`**, never a merge: its own admin can restore it from « Paramètres » with their own eyes on the result. ⚠️ It records **`PlatformAccessAction.RestoredClinic`** — the heaviest row in that ledger and the only console action that writes a practice's *clinical* records, at the one moment nobody at the practice can see it happen | `[Authorize(PlatformConsole)]` (class) |

Supporting: `Models/` (request DTOs: `UploadFileRequest`, `LoginRequest`, `SetupRequest`, `RegisterRequest`, `ChangePasswordRequest`, `SetUserStatusRequest`, `BackupRequest`, `CreateClinicRequest`, `UpdateClinicRequest`, `UpdateDoctorProfileRequest`, `CreateMedicalDocumentRequest`, `CnamNomenclatureRequests`, `MedicationRequests`); `Swagger/` (`FileUploadParameterFilter` + `FileUploadOperationFilter` — registered; `FileUploadDocumentFilter.cs` exists but is **not wired**, effectively dead).

## Realtime (`Hubs/`) — SignalR
- **`ClinicHub`** (a bare `[Authorize]`, mapped at **`/hub/clinic`**, and deliberately left bare: the hub is not a controller action, it carries no data of its own, and it broadcasts only a resource **key** — a client that refetches on it still meets the endpoint's own policy) — on connect resolves the caller's clinic (same `IUserRepository` lookup the REST handlers use; `IHttpContextAccessor` is unreliable in a hub, so it reads `HubCallerContext.User`) and joins the `clinic-{id}` group. Server→client event `"entityChanged"` carries the lowercase resource key that changed so clients refetch only affected views.
- **`SignalRRealtimeNotifier`** (`IRealtimeNotifier`, registered scoped) — broadcasts to the clinic group; a broadcast failure is logged and **swallowed** (realtime is additive, never fails the committed use case). It is the API-side implementation of the Application `RealtimeBroadcastBehavior` seam.
- **`ClinicGroups.Name(clinicId)`** = single source of the `clinic-{id}` group name.
- ⚠️ **A hub method runs with NO tenant scope** (US-2): HTTP middleware does not run per invocation, so
  `TenantScopeMiddleware` never fires for one and it lands in `Unset` — where the clinic query filters now return
  **nothing**. `ClinicHub` is safe only because it reads `User`, which is unfiltered; a hub method touching a
  filtered entity must set the scope itself from the clinic it already resolves. `ClinicHubTenantScopeTests`
  fails if a filtered repository is injected here, because the broken version *succeeds* with an empty result.
- Hub JWT: a browser WebSocket handshake can't set an Authorization header, so the SignalR client sends the token as `?access_token=`; `CreateHubJwtEvents()` (`Program.cs`) reads it into the token **only for `/hub/*` paths** — same signed token, same mode-branched validation. Security: framework request logging is suppressed, so the token in the query string is not logged; **do not** enable HTTP request logging for `/hub/*` without scrubbing the query string.

## Background Jobs (`BackgroundJobs/`) — Hangfire
PostgreSQL storage (same `DefaultConnection`) + a Hangfire server. Dashboard at **`/hangfire`**, gated by `HangfireAuthorizationFilter` (defined in `Program.cs`): **loopback-only in BOTH modes** (`LocalRequest.IsLoopback` — server PC yes, LAN clients / proxied requests no; changed from the prior Cloud `return true;`). Jobs are plain classes resolved from DI when enqueued.

⚠️ **`SystemWideCallerCoverageTests`' console-verb branch had never matched a single type until `clinic-subscription` Part F** — its filter was `IsAbstract: false`, and every verb here is a `static class`, which is abstract *and* sealed in metadata. The verbs were covered only incidentally, by that test's sibling `CreateScope()` source scan. Fixed there; worth knowing before writing another reflection guard over this folder.

⚠️ **Every job declares its tenant scope as its first act** (multi-tenant-cloud US-2), beside the `IAuditActorProvider.RunAs` declaration it already made. The clinic query filters refuse an unset scope, so a job that omits it reads **nothing and logs a clean run** — reminders and document emails would simply stop, which is the story's own R-1. The five scanning jobs call `UseSystemWide(reason)`; **`PdfGenerationJob` calls `UseClinic`** instead, because it renders one document — and it resolves that document's owning clinic through `IMedicalDocumentRepository.GetOwningClinicIdAsync`, the one deliberately `IgnoreQueryFilters()` read in the repositories, since a `MedicalDocument` has no `ClinicId` and its `Patient` is filtered (the answer is needed *in order to* set the scope). Pinned by `SystemWideCallerCoverageTests`, which derives its candidate set by reflection rather than from a folder list, so a **new** job that forgets fails on the day it is written.

| Job | Does | Trigger |
|---|---|---|
| `NotificationJob.ProcessPendingNotifications()` | SMS/WhatsApp reminder outbox dispatcher. Connectivity-gated (`IInternetProbe` — offline sends nothing, consumes no retry). Per-clinic effective settings (`IReminderSettingsProvider`), phone → +216 E.164, per-row commit, bounded retry (`RecordFailedAttempt` → `Failed` at `Reminders:MaxRetries`). Re-checks the appointment is still active before sending. **A row that reaches `Failed` is surfaced**: an in-app `ReminderFailed` staff notification via `INotificationGenerator` (all staff, no actor exclusion), and for a **recall** — once every channel of that one send has failed — `Patient.ClearRecallSnooze()` puts the patient back on the relance list (`TryReturnPatientToRecallListAsync`; a sibling row still `Pending` defers the decision to a later tick, a sibling `Sent` means they really were reached). Deliberately **not** surfaced for the cancelled/no-show void — that is a correct suppression already announced by `AppointmentCancelledAsync`. Both are best-effort: `SurfaceFailureAsync` never throws, so a feed write can neither abort the batch nor cause a re-send. The due-row scan is **bounded** by `Reminders:DispatchBatchSize` (50) (AC-P4.31), and after the dispatch loop it **purges terminal rows** older than `Reminders:RetentionDays` (90) - the table had no purge of any kind, so it grew forever. The purge runs *after* dispatch (EC-13) and never deletes a `Pending` row (AC-P4.34); a purge failure is swallowed so housekeeping cannot stop reminders going out. Served by `IX_Notifications_Status_ScheduledFor`. **Since `clinic-subscription` Part G it also parks a row whose cabinet may not record new work** (FR-8, EC-7) — see the ⚠️ under the push job below, which shares the mechanism. `[DisableConcurrentExecution(600)]` + `[AutomaticRetry(3)]` | **Recurring** — `Cron.Minutely` (`"process-notifications"`). No-op until a `Reminders` channel + credentials configured |
| `AppointmentProgressJob.StartRunningAppointments()` | **Auto-start a visit once its own slot has begun** — a 14:00→15:00 booking reads « En cours » at 14:16 with nobody pressing anything. Per clinic: `Start()` each Scheduled/Confirmed appointment the read returned, one `SaveChangesAsync`, then one `appointments` broadcast so open agendas repaint. Declares `UseSystemWide` + `RunAs`. **A job and not a write hook** for `StockExpiryJob`'s reason: a slot is entered by the *passage of time*, and no write happens at the minute it starts. ⚠️ **A second pass closes what has ended** (`CloseElapsedAppointments`, same tick, 30-day lookback): a patient-bearing visit whose slot has passed becomes **`AwaitingClosure`** — « Séance passée » — and a « créneau occupé » becomes `Completed`, that having nothing to close and nobody to ask about. It still never marks « Terminé » or « Absent » for a *patient*: leaving a slot is not evidence they came, so both stay human decisions. What it fixes is that the alternative used to be « En cours », which asserts somebody is in the chair — and for a blocked hour, nothing ever cleared it, which is why the day board, `chairClaim`, every day figure and the agenda's status strip each grew their own workaround. ⚠️ The residual is stated: a visit older than the lookback keeps its status and is still listed by « À clôturer ». Served by the new `IX_Appointments_Status_AppointmentDateTime` — `Status` had no index at all. ⚠️ **The selection is split across two layers on purpose**: `Duration` is persisted as **ticks** behind a value converter, so `AppointmentDateTime + Duration` has no SQL translation and the database's own `AppointmentEndDateTime` (trigger-maintained for the exclusion constraint) is deliberately unmapped — the query therefore cuts a **one-day** window in SQL and applies the exact end-of-slot test to that bounded set. The residual is stated rather than hidden: a booking longer than a day is not picked up and simply keeps its current status, and the error only ever runs that way. `AppointmentProgressQueryTranslationTests` pins that the window really is pushed down. ⚠️ The broadcast key is asked of `RealtimeResourceResolver`, never typed as `"appointments"` — a literal would be a second authority over a contract the frontend test holds, and a wrong key is a signal nobody listens for, which on screen looks exactly like the job not running. ⚠️ The two skip terms are separate because **`Appointment.CanTransition` counts re-assigning the current status as legal**, so the transition guard alone lets an already-started visit through to a `Start()` that changes nothing — and then to an empty save and an audit row a minute, for ever, per visit in progress. `[DisableConcurrentExecution(300)]` + `[AutomaticRetry(3)]` | **Recurring** — `Cron.Minutely` (`"start-running-appointments"`), registered **unconditionally** and **not** connectivity-gated: it writes a status, so it must work on an offline LAN install (`StockExpiryJob`'s reasoning). Safe unconditionally because a clinic with nothing running right now costs one query and no writes. Not gated on the cabinet's entitlement either — like the backup and expiry passes it records **no new work**, it advances the state of work already booked |
| `StockExpiryJob.FlagExpiringStock()` | Approaching-expiry stock alerts (AC-P4.6). Per clinic: skips a non-positive `Clinic.StockExpiryLeadDays` (the alert is off — and does **not** clear existing alerts, since turning the window off should stop new ones, not erase what staff were told), then for each item calls `INotificationGenerator.EnsureStockExpiringSoonAsync` or `ClearStockExpiringSoonAsync`. **Deliberately NOT connectivity-gated**, unlike its two siblings: the alert is in-app, so it must work on an offline LAN install. Per-clinic failures logged and skipped. `[DisableConcurrentExecution(600)]` + `[AutomaticRetry(3)]` | **Recurring** — `Cron.Daily(6)` (`"flag-expiring-stock"`). **A job and not only a write hook** because expiry is crossed by the *passage of time*: the box nobody has touched is the case the alert exists for, and no write happens on the day it enters the window. `RestockStockItemCommand` also calls the generator inline so a short-dated delivery is flagged at once |
| `PushDispatchJob.DispatchQueuedPushes()` | **OS push outbox dispatcher** (P6), on `NotificationJob`'s template: connectivity-gated, bounded per tick **and per clinic**, per-row commit, `Blocked` review pass, retention purge that never drops a `Pending`/`Blocked` row. Declares `UseSystemWide`. ⚠️ **Eligibility is re-checked at dispatch, not trusted from enqueue**, and that is the difference between this queue and one carrying mere messages: a push draws on a lock screen, so every request-time guard is already spent. Each row re-reads its device (deregistered ⇒ fail), compares the binding against the `RecipientUserId` it was queued for (**rebound to a colleague on a shared tablet ⇒ fail**, or one user's notification lands on another's lock screen) and, **for a reminder only**, re-reads the appointment — a reminder speaks for a *future* visit and can be overtaken, while « rendez-vous annulé » must still go out after the cancellation. That re-check is also why the fan-out does not delete queued reminders at cancellation time: it covers the reschedule race a delete could not. ⚠️ **Both outboxes park an expired cabinet's rows rather than sending or dropping them** (`clinic-subscription` Part G · FR-8 · EC-7), through the one `OutboxSubscriptionGate` asked at dispatch **and** in the `Blocked` review pass. The review term is the load-bearing half: everything either reviewer checks is about the *channel* or the *platform*, all of which a row parked for expiry passes, so without it the reminder would go out **within a minute** on a cabinet that has not paid — which is why the two halves shipped in one commit. It is asked for *every* parked row, so a channel-parked row is not released into a queue about to park it again | **Recurring** — `Cron.Minutely` (`"dispatch-os-push"`), and **registered only where `IOsPushAvailability.IsAvailableAtAll`** — unlike its siblings, which are safe unconditionally because they no-op until configured. A recurring job on a clinic's own PC would sit in the Hangfire dashboard for ever rediscovering every minute that this topology has no store-distributed app to deliver to; `RemoveIfExists` drops it if an install loses its credentials or is reprofiled |
| `ClinicActivityCounterJob.CountClinicActivity()` | **The vendor console's activity counters** (`platform-console` Part 2). Per clinic: read one 30-day window of audit rows, restate **every day** of it in `ClinicActivityDays`, then rewrite the cabinet's `ClinicActivitySnapshot` — writes 7 j/30 j, RDV pris, jours actifs, dernier enregistrement, plus `patients` (a `COUNT`, **never** audit inserts), staff + last sign-in, and the month's takings through `PlatformCollectedReader`. Declares `UseSystemWide`. **Deliberately not connectivity-gated** (its output is a database row — `StockExpiryJob`'s reasoning). ⚠️ **The whole window, not just yesterday**: the rows are already in hand, and it makes the history self-healing — a missed run would otherwise leave a permanent hole in a trend nothing can reconstruct, since the audit window the pass reads is itself 30 days. Idempotent through the unique `(ClinicId, Day)` index. Per-clinic failures are logged and skipped, which is why `verify-schema` gained `clinic-activity-snapshot-covers-every-clinic` — a cabinet skipped every night would otherwise cost nothing visible. `[DisableConcurrentExecution(1800)]` + `[AutomaticRetry(3)]` | **Recurring** — `Cron.Daily(3)` (`"count-clinic-activity"`), 03:00 UTC = 04:00 Tunis: after the day it measures has ended, long before the vendor opens the console. Registered **unconditionally**, not behind `ServesPlatformConsole` — the counters are history, and a deployment switching the console on later would open it to « jamais mesuré » with nothing to backfill from |
| `PdfGenerationJob.GenerateAndAttachPdfAsync(documentId)` | Renders a medical document to PDF (`IPdfGenerationService`) and re-saves it via MediatR. `[AutomaticRetry(3)]` | **Fire-and-forget**, enqueued on demand (`MedicalDocumentsController.GeneratePdf`) |
| `ClinicRecoveryPointJob.TakeRecoveryPoints()` | **The daily per-clinic recovery point** (`clinic-recovery-points`) — per cabinet: build today's **rows-only** archive if due, store it under `clinics/{id}/recovery-points/`, record it in `ClinicRecoveryPoints`, prune to `ClinicRecoveryPoint.RetentionCount` (7), then keep the « archive ancienne » alert equal to the truth. On `BackupJob`'s template: `RunAs` + `UseSystemWide` first, one try/catch per cabinet, the `Running` row committed **before** the build so a crash leaves a visible row, prune **only after a success**. **Not** connectivity-gated (a row and an object in the deployment's own store). ⚠️ **Rows only, no blobs**: a cabinet's rows are megabytes of JSON and its radiographs are gigabytes, so seven daily full copies would be seven copies of the object store per practice — and a row deleted by mistake is the case this exists for, while a blob's durability is the object store's own problem and is not improved by copying it into the same store. ⚠️ **It cannot send `BuildClinicArchiveQuery`** — that resolves the cabinet from `IClinicContext.GetUserId()` and re-checks `IsAdmin()`, and a job has no caller; it calls `ClinicArchivePackager` directly with the clinic id it is iterating. ⚠️ **The prune deletes the OBJECT before the ROW**: the other order leaks an object nothing points at, invisibly, for the life of the deployment, while this order can at worst leave a row whose object is gone — which the restore names as a refusal rather than meeting as a crash. ⚠️ The staleness alert is about the copy that **left the building**, not about these rows: recovery points die with the deployment, so a cabinet whose points are perfectly healthy still gets it, measured from `Clinic.LastArchiveDownloadedAtUtc` (or the cabinet's creation, never the epoch) | **Recurring** — `Cron.Daily(2)` (`"take-recovery-points"`), 02:00 UTC = 03:00 Tunis, before the counter pass so the two do not contend for the pool. ⚠️ **Registered unconditionally**, unlike `BackupJob` beside it, and the asymmetry is the point: that one runs `pg_dump`, which takes `--dbname` and has no tenant predicate, so it is `BacksUpItsOwnData`-only. This goes through the tenant filter like every CSV export and carries one cabinet's rows, so it is correct on every kind — and on `SelfHostedLan` it is additionally the only **granular, online** recovery there is, since `restore-backup` stops the app and restores the whole database to undo one deleted fiche |

| `BackupJob.RunScheduledBackups()` | **Unattended backup (L4a)** — per clinic: run today's backup if due, prune to `Clinic.BackupRetentionCount` (oldest-first, only `clinic-backup-*` names, **never the last surviving one**), then keep the staleness alert equal to the truth via the `EnsureBackupStaleAsync`/`ClearBackupStaleAsync` pair. Records **every attempt** in `BackupRuns`, and the `Running` row is committed **before** the dump starts so a crash leaves a visible row rather than none. **Deliberately not connectivity-gated** (the output is a local file — same reasoning as `StockExpiryJob`). `[DisableConcurrentExecution(3600)]` + `[AutomaticRetry(0)]` — a retried backup would be a second dump of the same night, which the due-check would then have to unpick | **Recurring** — **`Cron.Hourly`** (`"run-scheduled-backups"`), and **registered only where `profile.BacksUpItsOwnData`** with a `RemoveIfExists` in the else (`SubscriptionWarningJob`'s precedent). ⚠️ Left registered on the hosted kinds it did real harm rather than nothing: with no `pg_dump` in the image it wrote a **failure row per clinic per attempt for ever** and raised a « sauvegarde périmée » alert no action available to the clinic could clear. ⚠️ **Hourly, not daily, and that is what makes the per-clinic hour real**: the schedule lives on the *clinic* (`BackupHourLocal`, clinic-local), so one daily cron could honour only one clinic's choice. It also serves the case a fixed 02:00 cron never can — **a clinic PC switched off overnight**, which a daily job silently never backs up at all. `IsDueAsync` will not back a clinic up twice in its own local day, and waits 6 h after a failed attempt so an unplugged USB disk cannot produce twenty failure rows |

| `MessagingAllowanceJob.ReviewMessagingAllowances()` | **The WhatsApp forfait's daily pass**, three duties in order: **provision** this Tunisian month's counting row for every cabinet (from the **fold**, not the policy default — a snapshot must equal the fold from the first instant or `monthly-allowance-matches-ledger` is red on every new cabinet), **reconcile** the 80/95/100 % warnings to what the cabinet currently meets, and **poll Meta** for template statuses whose webhook never arrived. ⚠️ **One try/catch per cabinet *per duty*** (R-9), so a failing warning pass cannot cost the counting row. ⚠️ It creates **no** row for a cabinet whose ledger folds to null — there is nothing to fold, and that cabinet is `messaging-report`'s « aucun forfait » rather than « non mesuré ». ⚠️ « Today » is a **parameter**; not connectivity-gated for the first two duties' reason (a database row and the in-app feed) | **Recurring** — `Cron.Daily(5)` (`"review-messaging-allowances"`), 05:00 UTC = **06:00 Tunis**: after the Tunisian month has genuinely turned, before the clinic opens. Registered **only where `profile.SellsVendorMessaging`**, with a `RemoveIfExists` in the else — pinned against `Program.cs`'s own source by `MessagingCapabilityRegistrationTests`, because a registration moved one block out of its `if` is correct in isolation and invisible to every behavioural test |
| `SubscriptionWarningJob.WarnExpiringSubscriptions()` | **The four expiry warnings** (`clinic-subscription` Part E, FR-5, AC-3.4–3.7) — per cabinet: read the entitlement, apply `SubscriptionStateReader`, and ensure exactly one in-app row for the threshold reached (7 / 3 / 1 / 0 days), or withdraw them all once the date has moved back beyond the window. **A job and not a write hook** for `StockExpiryJob`'s reason: a date arriving is crossed by the *passage of time*, and nothing happens on the day a cabinet enters the window. **Not** connectivity-gated (in-app) and **never** an OS push — `StaffNotificationRules.ReachesALockedPhone` answers `false`, so the decorator passes it through. ⚠️ **Two states are left untouched**: a **suspended** cabinet is not warned (the reader surfaces no countdown for one — EC-11), and an **expired** one is neither warned again nor **cleared**, because it is now meeting a refused save and those rows are what explain it. ⚠️ **It takes « today » as a parameter** — the Hangfire entry point resolves `ClinicClock.ClinicToday()` and calls the overload — for `SubscriptionStateReader`'s own reason: the thresholds and the midnight they turn on are otherwise untestable. `[DisableConcurrentExecution(600)]` + `[AutomaticRetry(3)]` | **Recurring** — `Cron.Daily(7)` (`"warn-subscription-expiry"`), and **registered only where `profile.RequiresSubscription`**, with a `RemoveIfExists` in the else (`PushDispatchJob`'s precedent): on the other two deployment kinds every entitlement is open-ended, so the pass could only ever loop over cabinets it must not warn. The job asks the capability again itself, for the reprofiled install whose registration is still in Hangfire storage |

**Net: two minutely recurring jobs** (`process-notifications`, connectivity-gated; `start-running-appointments`, **not** — it writes a status, so it must work offline) **+ two daily** (`flag-expiring-stock`, `warn-subscription-expiry`) **+ one hourly** (`run-scheduled-backups`) — none of the last three connectivity-gated — + on-demand `PdfGenerationJob`. Google→App calendar sync has **no job** — `Program.cs` keeps only a defensive `RecurringJob.RemoveIfExists("sync-google-calendar")`. App→Google runs inline on appointment create/update; Google→App runs only via the manual `GoogleCalendarController` endpoints.

## `Program.cs` startup pipeline
Reads the auth mode from an early `ConfigurationBuilder` **before** `WebApplication.CreateBuilder` (the Serilog log path + the outer startup-failure catch both need it early). In Local the log file is anchored to `LocalInstallPaths.BaseDirectory/logs/` (a Windows service's CWD is `System32`); Cloud keeps relative `logs/`.

**Console verbs are intercepted before the web host boots** (all Local-only, direct-DB/no-web, exit code):
- `reset-admin-password [email]` → `Maintenance/AdminPasswordResetCommand` (offline admin lockout recovery, wraps Application's `AdminPasswordRecoveryService`).
- **`provision-clinic --name … --admin-email … --admin-name …`** → `Maintenance/ProvisionClinicCommand`
  (multi-tenant-cloud US-3) — creates clinic #N and its first admin and prints a **one-time** password
  (`MustChangePassword`). ⚠️ **The only verb of the family gated on `UsesLocalAccounts` alone, deliberately**: it
  needs the connection string, not `pg_dump`, so gating it on `HasLocalDbTooling` like its siblings would make it
  refuse in `HostedMultiTenant` — the one profile it exists for. It shares
  `Application/Features/Clinics/LocalClinicProvisioning` with first-run `setup` rather than re-implementing it,
  and mints the clinic id *before* the work so it can declare `UseClinic(id)` over the writes it covers.
  Hosted: `docker exec clinic-api-prod dotnet ClinicManagement.API.dll provision-clinic …` (env is inherited, so
  `AddInstallLayers()` resolves the running app's connection string).
- `provision-cert` → `Maintenance/ProvisionCertCommand` (idempotently mints/reuses the CA + server cert into `.local/` and exits; the installer runs it before starting the service so first boot reuses the cert under the SCM timeout).
- `harden-permissions <dir>…` → `Maintenance/HardenPermissionsCommand`; `protect-credential`/`read-credential` → `Maintenance/CredentialProtectionCommand`.
- **`restore-backup <folder> [--force]`** → `Maintenance/RestoreBackupCommand` (L4g). The answer to
  `packaging/README.md`'s former « There is no in-app restore ». A console verb and **not** an endpoint because a
  restore runs with the app **stopped** — it drops and recreates every table the app holds open. **The ordering is
  the design**: validate the folder (`database.dump` present + `pg_restore --list` non-empty) → refuse while the
  app's ports are listening → **safety-dump the current state** into `clinic-pre-restore-*` (deliberately *not* a
  `clinic-backup-*` name, so retention can never prune the one copy that exists to survive a regretted decision)
  → `pg_restore --clean --if-exists --no-owner` → copy `files/` back (refused into a non-empty target without
  `--force`) → **bump every `User.TokenVersion`** so no token minted against the newer state stays live. Every
  refusal happens before anything is destroyed. Exit `0` restored / `1` refused or failed.
- **`platform-account create --email … --name …` / `--deactivate` / `--reset-totp`** → `Maintenance/PlatformAccountCommand` (`platform-console` AC-8.1/8.2/8.5) — creates, deactivates and re-secrets a **console** account. ⚠️ A verb and **no MediatR command behind it**, deliberately: AC-8.5 says there is no web path to any of the three, and a handler reachable through the mediator is one attribute away from being callable over HTTP — by an account that can read every cabinet. Gated on a configured connection string, **not** on `ServesPlatformConsole`: the first account has to be creatable *before* the listener is switched on, and a locked-out vendor must be able to re-secret one on a deployment where the console has just been turned off. Prints a one-time password and an enrolment secret, each shown once.
- **The five `subscription-*` vendor verbs** (`clinic-subscription` Part F, US-5 / FR-6) —
  `subscription-grant --clinic <id|email> (--months N | --days N | --until AAAA-MM-JJ) [--plan …] [--amount …]
  [--method …] [--reference …] [--note …] [--complimentary]`, `subscription-cancel … --entry <id> --reason "…"`,
  `subscription-suspend … --reason "…"`, `subscription-unsuspend …` and `subscription-report [--within 7]
  [--clinic <id|email>]`. **Verbs and not endpoints, deliberately**: a cabinet able to extend its own entitlement
  over HTTP would not have one, so no controller references the three commands behind them and a derived guard
  (`SubscriptionVendorCommandReachabilityTests`) holds that in both directions — including that every verb is
  actually *dispatched* here, since a missing branch boots the **web host** instead and reads to an operator as
  « the command did nothing ». All five gate on `MaintenanceDatabase.HasConnectionString`, never on a capability
  (amendment M3): they run no PostgreSQL binary and the hosted deployment they exist for above all has no local DB
  tooling. The four mutating ones declare **`UseClinic(id)`** (`provision-clinic`'s precedent — the narrowest scope
  for the narrowest work) after resolving the cabinet through `SubscriptionCabinetLookup`, which is safe *before*
  the scope only because `Clinics` and `Users` are the two unfiltered tables; the report declares `UseSystemWide`.
  Each also calls `IAuditActorProvider.RunAs(<verb>)`, so FR-12's journal reads `job|subscription-grant` rather
  than « unknown ». ⚠️ `subscription-report --clinic <id|email>` is the **only** thing that prints a period id, and
  `subscription-cancel` takes one — so without that mode a mistaken grant older than the current session would be
  uncorrectable from the console. It shares `reconcile-money`'s exit codes (0 / 1 / **2**), and a *suspended*
  cabinet is listed without counting as a finding: an alarm that is always on is one nobody reads.
- **The three `messaging-*` vendor verbs** (`vendor-whatsapp-messaging-quota` Part 3) —
  `messaging-grant --clinic <id|email> (--per-month N | --top-up N --month AAAA-MM) [--amount] [--method]
  [--reference] [--note] [--complimentary]`, `messaging-cancel … --entry <id> --reason "…"` and
  `messaging-report [--month AAAA-MM] [--clinic <id|email>]`. **Verbs and not endpoints**, on the
  `subscription-*` family's reasoning and held by the same kind of derived guard
  (`MessagingVendorCommandReachabilityTests`: no controller names either command, all three verbs actually
  dispatched by `Program.cs`, and the near-exhausted threshold a single named constant with no literal in the
  SQL predicate). All three gate on `MaintenanceDatabase.HasConnectionString`, declare their tenant scope, and
  call `IAuditActorProvider.RunAs`. ⚠️ **The server decides the effective month for a standing figure** — a raise
  applies this month, a lowering waits for the next, and the verb says which out loud; a **standing zero is a
  lowering**, so turning a cabinet off *now* is a cancellation. ⚠️ `messaging-report --clinic` is the **only**
  thing that prints an allocation id, and `messaging-cancel` takes one — without that mode a mis-keyed grant older
  than the current session would be uncorrectable. It shares `reconcile-money`'s exit codes, and its four finding
  kinds are ordered « aucun forfait » **before** « épuisé », because a cabinet with no forfait cannot meaningfully
  be out of one.
- **The two read-only report verbs**, which share exit codes (`0` clean / `1` couldn't run / `2` **ran and found drift**) so they can be scripted identically, and are both meant to be run **before and after a migration batch and diffed**:
  - `reconcile-money [months]` → `Maintenance/ReconcileMoneyCommand` (wraps `MoneyReconciliationService`).
  - `verify-schema` → `Maintenance/VerifySchemaCommand` (wraps `SchemaVerificationService`). The **only** gate for a schema-level change — nothing in the test project touches a database. Both build their container from `AddInfrastructure` **only**, never `AddApplication` — which since US-2 is what makes their `UseSystemWide(reason)` declaration *work* rather than what makes it unnecessary: that method registers `ITenantScope` plus a floor `ICurrentClinicProvider`, so a verb that failed to declare itself would report a clean, empty database. (Before US-2 they had no provider at all and read every clinic by accident.) `reset-admin-password` declares it too; the other four verbs are named exemptions in `SystemWideCallerCoverageTests` — three touch no database and `restore-backup` works over raw ADO, so no query filter is in play.

⚠️ **Since US-6's amendment M3 the gate on `verify-schema`, `reconcile-money` and `reset-admin-password` is « is a connection string configured? » (`Maintenance/MaintenanceDatabase`), not a deployment capability.** They used to refuse unless `HasLocalDbTooling` — a capability about `pg_dump`/`pg_restore` being on the box, which none of them runs — while their own refusal messages already said « needs a direct database connection ». That mattered twice: it made `verify-schema` unreachable in `HostedMultiTenant`, taking the product's **only** gate on a schema change away from the one topology where a bad migration hits every clinic at once; and it left a hosted clinic's locked-out admin with no recovery once `provision-clinic` could create one. All three run over `docker exec clinic-api-prod dotnet ClinicManagement.API.dll <verb>`. ⚠️ **`restore-backup` keeps its profile gate**, and that is the deliberate exception: its interlock is « refuse while the application is listening », enforced by looking for a listener on *this* machine, so in Docker the API listens in a sibling container, the check finds nothing and passes, and `pg_restore --clean --if-exists` drops every table under a live application.

**Startup migrations hold a lock (`Startup/MigrationLock.cs`, US-6).** `context.Database.MigrateAsync()` and the two per-clinic backfills run inside a PostgreSQL **session-level advisory lock**, because EF Core 8 takes none: two instances starting together — a scaled deployment, or simply a redeploy before the old container exits — both read « these migrations are pending » and both apply them, and the loser fails part-way leaving a schema that is neither the old one nor the new one. Advisory rather than a lock table (a table would need the migration it protects, and a crashed holder would wedge the next deploy for ever); the loser **blocks rather than skipping**, then finds nothing pending. ⚠️ `pg_advisory_lock`, never `pg_advisory_xact_lock` — the xact variant releases at the first commit *inside* the migration. ⚠️ Not used by `DeferredStartupService`: there the app is a single Windows service on the clinic's own PC, so there is no second instance, and the lock would also span its pre-migration `pg_dump`. `MigrationLockTests` pins the key, the session-level variant, and — against `Program.cs`'s own source — that the migrate call is actually inside the wrap.

**Config layers — `Startup/InstallConfiguration.cs` (L4e).** `appsettings.json` → **`appsettings.Install.json`**
(installer-owned, machine-derived: the connection string, the bundled tool paths, the ports — rewritten on every
install) → `appsettings.{Environment}.json` (**operator-owned**: written once when absent, never truncated again,
`.bak-<timestamp>`'d before it is touched) → environment variables. Split by **ownership** because the installer
used to truncate `appsettings.Production.json` unconditionally from `ssPostInstall`, erasing every hand-edited
value (`Cors:AllowedOrigins`, `Hosting:TrustPort`, `Security:EnableHsts`, the reminder keys) on every upgrade.
⚠️ **Every entry point uses `AddInstallLayers()`** — the host *and* all seven console verbs. A verb reading one
layer fewer would resolve a different connection string from the app it is maintaining.

Service registration order:
1. **Serilog** (console + daily rolling file, 7-day retention); `UseSerilog()`.
1b. **`UseWindowsService()`** — Local only (auto-start service, content root = install dir; no-op if not launched as a service).
2. `AddControllers()` (camelCase + case-insensitive JSON).
3. Swagger — maps `IFormFile`→binary, registers the parameter + operation upload filters.
4. **JWT bearer.** HS256 against the per-install `LocalAuthConfig` key (issuer/audience/lifetime/signature all validated). Still written as a `UsesLocalAccounts` branch, whose `else` now **throws**: a profile declaring it does not own its accounts has no identity provider left to fall back to, and booting on would leave every endpoint reachable with no token. Both use `CreateHubJwtEvents()` (hub `access_token` query param) and `ClockSkew = Zero`. When auth is configured, `AuthorizationPolicies.ConfigurePolicies(options, isLocalMode)` (Local → fail-closed fallback) + a scoped `RoleAuthorizationHandler` are added.
5. `AddHttpContextAccessor()` (for `ClinicContext`).
6. `AddApplication()` → `AddInfrastructure(config)`.
7. **SignalR** + `IRealtimeNotifier` → `SignalRRealtimeNotifier` (both modes).
8. **Hangfire** + server (PostgreSQL). The DB connection string is required — if empty the app logs Fatal and `return 1` (secrets are no longer committed; supplied out-of-band via env / `appsettings.Production.json` / `appsettings.Development.json`).
8b. **`DeferredStartupService`** — Local only (post-startup migrations, see below).
9. **CORS** `"AllowAll"`: `CorsOrigins.FromConfiguration(config)` = `FrontendUrl` unioned with optional `Cors:AllowedOrigins` (LAN origins), `AllowAnyMethod/Header` + `AllowCredentials()` (so no `AllowAnyOrigin`). Cloud collapses to `FrontendUrl`.
10. **YARP front door (Local only)** — `AddReverseProxy().LoadFromMemory(...)` registers one catch-all route (`/{**catch-all}`, least-specific) → the co-located Next server at `http://localhost:{Hosting:WebPort ?? 3000}`. Kestrel is the single browser-facing endpoint: `/api/*` controllers (more specific) run in-process; everything else proxies to Next. Cloud installs no proxy.
11. **HTTPS / Kestrel bind — mode-branched.** **Local always serves HTTPS**: if `Https:CertPath` is set it must exist (else `StartupDiagnostics.ReportFatal` + `return 1` — no silent HTTP downgrade), otherwise `CertificateProvisioner.EnsureServerCertificate()` self-generates a CA + cert into `.local/`; HTTP binds **loopback-only** on `Hosting:HttpPort` (5000, sole consumer is the Next BFF), HTTPS `ListenAnyIP` on `Hosting:HttpsPort` (5001, the only LAN port). Cloud: opt-in HTTPS only if a cert file exists (`ListenAnyIP` both), else honor `Hosting:Urls`. Cert password comes from `.local/`/env, never committed. A `certSource` (`generated`|`configured`|`cloud`) is logged.

Middleware order (after `Build()`): `UseSwagger`/`UI` (Dev only) → `UseHttpsRedirection` (**Cloud only** — Local must not redirect its loopback HTTP hop to the self-signed front door) → `UseCors` → `UseMiddleware<SecurityHeadersMiddleware>` → the trust-port gate (Local only) → **`UseAuthAttemptAccountCapture()`** → **`UseRateLimiter()`** → `UseMiddleware<ExceptionMiddleware>` (from Application; before auth) → **`ClientVersionMiddleware`** (every profile) → the **console port gate** (`ConsolePortGate`, unconditional) → `UseAuthentication` → **`AccountStateMiddleware`** → **`PlatformAccountStateMiddleware`** (console paths only) → `UseAuthorization` → **`PlatformTenantScopeMiddleware`** (console paths only) → **`TenantScopeMiddleware`** (every profile) → **`LocalAuthEnforcementMiddleware`** (Local only) → **`SubscriptionGateMiddleware`** (every profile, inert unless `RequiresSubscription`) → `MapControllers` → **`HealthChecks.Register` (`/health`)** → **`MapHub<ClinicHub>("/hub/clinic")`** → `UseHangfireDashboard("/hangfire")` → **`MapReverseProxy().AllowAnonymous()`** (Local only — the proxied Next app does its own auth; anonymous or the fail-closed fallback would 401 the login page).

**`UseAuthAttemptAccountCapture()`** (multi-tenant-cloud US-6) sits **immediately before** the limiter, because after it the partition has already been chosen. It lifts the submitted `email` out of a small JSON POST to `/api/auth/*` onto `HttpContext.Items` so the anonymous-auth window is spent **per account** rather than per client address — a whole practice reaches a hosted deployment through one NAT address, so one colleague mistyping their password used to spend everybody's budget. ⚠️ It buffers the body and rewinds it (`EnableBuffering`), and **anything it cannot read leaves no value behind**: a non-JSON body, a truncated one, one over 8 KB, `auth/refresh` (which carries no email at all) all fall back to the address, i.e. to exactly the behaviour that shipped before. Nothing about it can refuse a request — turning a JSON slip into a 500 at the limiter would make the login page unreachable. ⚠️ Re-keying alone would have been a hole, so `RateLimiting`'s **global** limiter now recognises the same path prefix and bounds it on the address with a separate, looser ceiling (`RateLimiting:Auth:AddressPermitLimit`, default 150 per the auth window): the account window is the brake, the address one is the ceiling, and both apply.

**`/health`** (`Startup/HealthChecks.cs`, multi-tenant-cloud US-6, **every profile**) — anonymous and exempt from the rate limiter, both required rather than tidy: where the authorization fallback is fail-closed a probe would 401, and a 429 reads to an orchestrator exactly like « unhealthy ». Two checks: the database (`SELECT 1`) and the storage (`IFileStorage.ProbeAsync`). ⚠️ **The grading is the substance** — the database is `Unhealthy` (→ 503, stop sending traffic) and the storage is **`Degraded`** (→ still 200), because a clinic with no object storage still books, records and collects, restarting the API does not bring MinIO back, and pulling every instance out of rotation would turn a partial outage into a total one. ⚠️ The body carries **check names and statuses only**; a failure's reason goes to the log, never to an anonymous caller. ⚠️ Deliberately **outside `/api`** (so it is measured by neither the limiter nor the client-version floor) and mapped **before** the YARP catch-all; in the hosted topology `deploy/Caddyfile` routes it explicitly, or it would reach the Next container and 404.

**`ClientVersionMiddleware`** (`mobile-native-shells` P3, **unconditional in every profile** — a client too old for the server is too old on a clinic's PC and in a datacentre alike, so it asks no capability): reads `X-Client-Version` and refuses a build below `Clients:MinimumShellVersion` with **426** and the canonical `{ error, code: "client_too_old" }`. Three decisions worth knowing. ⚠️ **Before `UseAuthentication`**, departing from the blueprint (plan R-11): the refusal needs no principal, and running it first makes a stale shell's *login* 426 rather than 401 — the client reads 401 as « signed out », and the requirement is the opposite (AC-33). ⚠️ **Scoped to `/api`**, and that is load-bearing rather than tidy: where the front door also serves the web app through the YARP catch-all, refusing every path would 426 the page itself and leave the shell showing raw JSON instead of the French update state that page renders; the BFF routes and `/hub/*` sit outside it, which is also AC-32's « a server-side hop is unaffected ». ⚠️ **Unreadable means accepted** — absent header, malformed version, unset or typo'd floor all pass. Nothing may take the whole API off the air for every browser in the clinic because an operator mistyped a version. Config is read **per request**, so raising the floor needs no restart.

**`PlatformAccountStateMiddleware`** (`platform-console` Part 1, fixed in Part 7): the console's own
per-request state check — a deactivated `PlatformAccount`, a stale `token_version` or a pending one-time password is
refused on the **next request** (AC-1.6, AC-8.1). It exists because a console request skips both middlewares that read
live account state: `AccountStateMiddleware` and `LocalAuthEnforcementMiddleware` resolve the caller's `User` row, and
a console principal has none.
⚠️ **It was inert in production until Part 7, and nothing in the build could see it.** It read `context.User`, which for
a console token is never populated here: `UseAuthentication` authenticates only the **default** (clinic) scheme, a
console token fails that scheme by design (AC-1.4), and the console's own scheme is authenticated inside
`AuthorizationMiddleware` because `AuthorizationPolicies.PlatformConsole` **pins** it — after this middleware. So the
account id was null on every request and everything passed through: a deactivated account kept reading every cabinet,
and the bootstrap password was one-time in name only. It now calls `context.AuthenticateAsync(PlatformConsoleScheme.Name)`
itself and **every** check reads that principal, `HasCurrentTokenVersion` included.
⚠️ **Not fixed by moving it after `UseAuthorization`** — which does write the combined principal back, and would work:
that lets a revoked token be authorized before being refused, and rests on a framework detail rather than on this file.
⚠️ Its ordering is still asserted against `Program.cs`'s own source, and `PlatformAccountStateTests` now installs a stub
`IAuthenticationService` rather than assigning `DefaultHttpContext.User` — assigning it is what hid the defect through
six parts.

**`AccountStateMiddleware`** (`SECURITY_REVIEW_2026-08`, findings B+C, **unconditional in every profile**):
refuses a deactivated account and publishes the caller's **DB-resolved role** on `HttpContext.Items` for
`RoleAuthorizationHandler` to prefer over the JWT claim. ⚠️ **It exists because offboarding did not work on
`CloudBrowser`**: the only per-request reader of live account state was `LocalAuthEnforcementMiddleware`, gated on
`EnforcesTokenState` (false there) and skipping non-local accounts anyway — so deactivating a user succeeded, bumped
`TokenVersion`, showed « Désactivé », and the ex-employee's token kept working while fresh Auth0 logins minted more.
Demotion had the twin defect: the role lives in Auth0 `app_metadata` and `ChangeUserRoleCommand` never told Auth0,
so a demoted admin passed `AdminOnly` for ever. ⚠️ **Not gated on a capability** — « a deactivated account cannot use
the API » is not a property of a topology, and treating it as one is how the gap arose. ⚠️ **Runs before
`UseAuthorization`**, because the role it publishes is what the handler reads; that costs one account lookup on a
request authorization then refuses, and `RequestAccount` caches it for everything downstream. Pinned by
`AccountStateEnforcementTests`, including a source-level guard on both the absence of a capability gate and the
ordering.

**`TenantScopeMiddleware`** (multi-tenant-cloud US-2, **unconditional in every profile**): sets `ITenantScope` from the caller's **DB-resolved** `User.ClinicId`, so the EF global query filters know whose rows the request may see. It must run before any controller, because those filters now **refuse** an unset scope. ⚠️ An unresolvable caller deliberately leaves the scope `Unset` — anonymous requests, the proxied web pages and a principal with no `User` row yet (Cloud onboarding) all land there and all still work, because `User`/`Clinic` carry no filter. Placed after `UseAuthorization` so a refused request never pays for the lookup, and before the middleware below so the two **share one** account query via `RequestAccount` (which also collapses the duplicated `sub`-claim reading).

**`LocalAuthEnforcementMiddleware`** (Local only): the app-issued JWT is stateless, so per authenticated request it enforces **token-version revocation** (401) and forces a pending password change (403 `{ error, code: "must_change_password" }`) except on `/api/auth/change-password`. ⚠️ The **active-account** check moved out of here to `AccountStateMiddleware` above: it was skipped entirely on `CloudBrowser`, which is what made deactivation a no-op there. What remains are the two things only a self-issued JWT can have, which is what `EnforcesTokenState` now accurately means. It reads the account through `RequestAccount`, i.e. the row the tenant-scope middleware already loaded.

**`SubscriptionGateMiddleware`** (`clinic-subscription` Part B, US-4 / FR-3): a cabinet past its entitlement becomes
**read-only** — every read, every CSV export and every PDF keep working, and only writes are refused, with **402**,
a code (`subscription_required` / `_suspended` / `_missing`) and a French sentence naming the date. The wording and
the codes live in one place, `Application/Features/Subscriptions/SubscriptionRefusals`; the state itself comes from
`SubscriptionStateReader`, the same rule the screen, the banner, the warning job and the vendor verbs read.
⚠️ **Reads are untouched by construction, not by a list**: the gate never inspects a GET/HEAD/OPTIONS, so AC-4.1
holds for every read that exists *and* every read added later — an allow-list of readable endpoints would have to be
kept complete, and the day it was not, an expired cabinet would lose part of its own records. Exports need no
exemption for the same reason: they are GETs.
⚠️ **Registered after `LocalAuthEnforcementMiddleware`, last before `MapControllers`, and the four lines matter.**
Placed before it the gate answers **402** for a **revoked token** (should be 401) and a **pending forced password
change** (should be 403 `must_change_password`) — telling a deactivated colleague the subscription lapsed, and
routing a user who must change their password to « Abonnement » instead of to the screen that unblocks them, stuck
in both directions. Placed after, it still has everything it needs: the tenant scope is set, `RequestAccount` has
the account cached, and endpoint metadata is available because the implicit `UseRouting` runs before *all* user
middleware (the same reason `UseAuthorization` works here with no explicit call). `SubscriptionGateMiddlewareTests`
asserts that ordering against **`Program.cs`'s own source** — the middleware is correct in isolation and only its
*position* is wrong, so nothing else in the build can see it.
⚠️ **An `/api` path that routes to NOTHING passes too**, and that is a distinct case from « this endpoint declared no exemption »: `GetEndpoint()?.Metadata…is null` conflated them, so on an expired cabinet a mistyped URL — or an old client calling a removed action — was answered **402** and the client fired `onSubscriptionRequired` on the strength of it. Naming the subscription is the loudest thing this gate can say; an unroutable path is routing's own 404 to answer.
⚠️ **A caller who is not a cabinet passes** (`ITenantScope.Kind != Clinic`) rather than meeting
`subscription_missing`: they have no entitlement to find, and that fault code would otherwise land on precisely the
vendor-console endpoints whose purpose is to *end* a refusal. Authentication already covers the anonymous case.
⚠️ **FR-3's exempt set is declared per endpoint** with `[AllowsWithoutSubscription("<reason>")]` (mandatory reason,
beside `AuthorizationPolicies` in Application) and is derived — not listed twice — by
`SubscriptionExemptionCoverageTests`, which asserts the applied set equals the reviewed one **in both directions**.
The writes on it: the whole of `AuthController` (class-level — AC-4.7/EC-2, of which only `change-password` is
genuinely refused without it), the three compute-only POSTs (batch CNAM estimate · CSV import **preview** ·
render-for-download), the four writes experienced as reading (mark-read / read-all · push register + deregister ·
default file folders · the user's own dashboard layout), and `backup`, `users/{id}/status` + **`users/{id}/reset-password`**. ⚠️ That last one is about **reads**, not writes: a staff member who has forgotten their password cannot log in at all, so they lose the reads, exports and PDFs AC-4.1/4.2 guarantee — and a hosted deployment has no other recovery (`change-password` needs the current one, `reset-admin-password` needs container access). ⚠️ **`users/{id}/status` is exempt in one direction only**, and the handler is what makes that true: its recorded reason is offboarding, but the action also **re-activates**, which is the same effect as `POST /api/users` — correctly gated. `SetUserActiveCommand` therefore refuses the reactivation direction with the gate's own sentence. ⚠️ The **Google
OAuth callback is not exempted**: it is a GET that writes, but the request that *starts* the flow is refused, so it
is unreachable. *(The AI chat used to be named here as a deliberate non-exemption — its action set booked and
cancelled appointments. The whole subsystem is deleted; see the Infrastructure guide's « AI — removed ».)*
⚠️ That guard classifies **non-GET actions only**, so it *cannot* fail when the attribute is removed from a GET-only
row (`MetaController`'s two, and every action of the coming `SubscriptionController`): those carry it as
documentation, and a green suite is not evidence they are load-bearing.

**EF migrations — mode-branched.** Cloud runs `context.Database.Migrate()` synchronously in `Program.cs` + `IClinicCatalogSeeder.SeedAllClinicsAsync()` backfill. Local defers both to `Startup/DeferredStartupService` (an `IHostedService` that fire-and-forgets the work after the host reports "started"), because applying migrations synchronously as a Windows service blew past the SCM ~30s start timeout (killed mid-migration). `PublishReadyToRun` speeds cold start for that window. The deferred service surfaces a DB-unreachable failure via `StartupDiagnostics.ReportFatal` + `StopApplication()`.

Outer try/catch: `catch when (startupProfile.SelfHostsFrontDoor && StartupDiagnostics.IsAddressInUse(ex))` → clear "port in use" message + `return 1`; else `Log.Fatal` + rethrow; `Log.CloseAndFlush()` in `finally`.

**Every mode branch in `Program.cs` asks a named capability of the resolved `DeploymentProfile`** (multi-tenant-cloud US-1), never `IsLocalMode`: `RunsAsWindowsService` (service host + install-relative log path), `UsesLocalAccounts` (JWT bearer), `FailClosedAuthz` (the fallback policy — `ConfigurePolicies` keeps its `bool` because it lives in Application), `SelfHostsFrontDoor` (YARP, HTTPS redirect, the port-in-use catch), `SelfSignsCertificate` (the Kestrel cert block + HSTS default), `EnforcesTokenState` (`LocalAuthEnforcementMiddleware`), `ExposesTrustEndpoints` (trust port + `/api/connectivity`), and **`DefersMigrations` vs `RunsStartupBackfills`** — two questions that used to share one `if`. The seven console verbs gate on what each actually needs (`HasLocalDbTooling`, `RunsAsWindowsService`, `SelfSignsCertificate`).

## Startup diagnostics (`Startup/`) — Local mode
`StartupDiagnostics` (static, unit-testable) classifies the two operator-recoverable boot failures into clear French messages: `IsDatabaseConnectionFailure` (socket/timeout/bare `NpgsqlException`, but NOT `PostgresException` = server answered) and `IsAddressInUse`. `ReportFatal(message, ex?)` fans to console + Serilog + (best-effort, Windows-only) the Windows Event Log. Invoked only on the Local startup path (Cloud keeps its fatal-rethrow).

## Key configuration keys (`appsettings.json` / `appsettings.Development.json`) — names only
Secrets are **not committed** (feature cloud-security-and-tenant-isolation, AC-3): committed `appsettings.json` has empty strings + `// SECRET` comments; supply real values via env / user-secrets / `.local/` / the installer's `appsettings.Production.json`.
- `ConnectionStrings:DefaultConnection` (PostgreSQL + Hangfire; **required**, empty → fail-loud)
- **`Deployment:Profile`** (`SelfHostedLan`|`HostedMultiTenant`) — absent ⇒ `SelfHostedLan` when `Auth:Mode=Local`; **anything else throws**, because the kind that fallback used to name (`CloudBrowser`) is retired and the two survivors disagree about accounts, storage, subscriptions and the second factor. An unrecognised value fails startup loud too
- **`DataProtection:KeyRingPath`** — where the Data Protection key ring is persisted. **Required in `HostedMultiTenant`: startup fails loud without it** (US-6). Ignored in `SelfHostedLan` (which resolves its own install-relative `.local/` path). ⚠️ The « optional » third case was `CloudBrowser`, which is retired — so the framework's ephemeral per-instance default is no longer reachable by configuration at all. ⚠️ The framework fallback is per-instance and ephemeral — it *works*, and then the first redeploy replaces the ring, so every clinic's encrypted reminder credentials become undecryptable and each channel reports « non configuré » with nothing in any log tying that to a deployment. A configured path with **no durable volume** behind it produces the identical symptom, which no code can detect — that half is stated beside the volume in `deploy/docker-compose.hosted.yml`
- **`Security:EnableHsts`** / **`Security:EnforceCsp`** — HSTS is opt-in only where the certificate is self-signed (on elsewhere); `EnforceCsp` promotes the CSP from `Content-Security-Policy-Report-Only` to enforcing. Default **`false` in every profile** and deliberately not derived from the kind: what makes enforcing safe is that somebody walked these pages *in this deployment*. The flag changes only the header **name** — the policy string is the same one the report-only walk validated
- **`RateLimiting:Auth:{PermitLimit,WindowSeconds,AddressPermitLimit}`** / `RateLimiting:Api:{PermitLimit,WindowSeconds}` — `Auth:PermitLimit` (30) is now per **submitted account**, `Auth:AddressPermitLimit` (150) the per-address ceiling over the same window. Both apply to `/api/auth/*`; see `UseAuthAttemptAccountCapture()` above for why the tight window moved off the address
- `Auth:Mode` (`Cloud`|`Local`), `Auth:Local:SigningKey` (optional; else generated `.local/signing-key`)
- `Connectivity:{ProbeUrl,ProbeTimeoutSeconds,ProbeCacheSeconds}` (Local egress probe)
- `GoogleCalendar:{ClientId,ClientSecret,RedirectUri,RefreshToken,CalendarId}` (runtime refresh token is stored per-clinic in the DB, not here)
- `MinIO:{Endpoint,AccessKey,SecretKey,BucketName,UseSSL}`, `FileStorage:BasePath`
- `Reminders:{Channels,LeadTimesHours,MinLeadHours,MaxRetries}` + `Reminders:Sms:{ApiUrl,SenderId,ApiKey}` + `Reminders:WhatsApp:{ApiUrl,PhoneNumberId,TemplateName,TemplateLanguage,AccessToken}` (live SMS/WhatsApp reminder outbox)
- `Meta:{AppId,AppSecret,GraphApiVersion}` (WhatsApp Embedded Signup — Cloud onboarding, 404 in Local)
- `Notification:Smtp:*` / `Notification:Sms:*` (dormant legacy)
- `FrontendUrl` (primary CORS origin + OAuth success redirect); `Cors:AllowedOrigins` (optional LAN origins)
- **`Clients:UpdateDirectory`** — where the Windows client installer this server shipped with is read from; default `{BaseDirectory}/updates`, which is where `clinic-server.iss` stages it. Absent or empty ⇒ `GET /api/meta/client-download` 404s and nothing is added to the requirements payload, which is the correct answer on a hosted deployment (no offline-LAN client to ship). ⚠️ When a package **is** present it fills `CurrentShellVersion`, `StoreUrls:Windows` and `WindowsSetupSha256` **only where the operator left them empty** — an explicit setting always wins, and the locally-computed hash travels only with the locally-served URL, or a download from somebody's own mirror would be refused against our bytes' digest
- **`Clients:MinimumShellVersion`** / `Clients:CurrentShellVersion` / `Clients:StoreUrls:{Android,Ios,Windows}` — the client version floor and where to update. Operator-owned, empty by default (**empty = no floor**), effective without a restart. ⚠️ **`StoreUrls:Windows` is not a store listing**: the desktop shell is an installer the operator hosts, so it is a plain download URL. It lives on the same record because « where does *this* client get a newer build? » is one question with three answers, and a second endpoint for the desktop half would let the floor and the current release drift apart
- `Https:{CertPath,CertPassword}`, `Hosting:{Urls,HttpPort,HttpsPort,WebPort}` (LAN hosting / front door; Local self-generates the cert into `.local/` when `CertPath` empty)
- `Backup:{PgDumpPath,DefaultDestination,TimeoutSeconds}` (one-click backup)
- `Serilog:*`, `Logging:*`, `AllowedHosts`

> Cloud still carries some non-secret real values (`Auth0:Domain/Audience`, `GoogleCalendar:ClientId`). Prefer env vars / user-secrets / a vault for anything sensitive.
