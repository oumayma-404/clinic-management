# ClinicManagement.Application

> The use-case layer. Implements CQRS with **MediatR**: every API operation is a `Command` or `Query` with a co-located handler. Depends on Domain (entities + repository interfaces); defines its own infrastructure-facing interfaces (implemented in Infrastructure — a few live here). Returns `Result<T>` instead of throwing for business failures.

## Dependencies
`MediatR` (14), `FluentValidation` (12.1.1), `Microsoft.AspNetCore.Authorization` + `Http.Abstractions` (for `IHttpContextAccessor` / policies / middleware), `Microsoft.EntityFrameworkCore` (8 — a few queries catch `DbUpdateException`; project references only Domain). DI entry point: **`Extensions.cs`** → `services.AddApplication()`.

## DI wiring — `Extensions.cs`
```
AddApplication():
  AddMediatR(RegisterServicesFromAssembly(...))     // all handlers (no INotificationHandlers exist)
  AddValidatorsFromAssembly(...)                     // FluentValidation — ZERO validators defined today
  AddTransient(IPipelineBehavior<,>, ValidationBehavior<,>)
  AddTransient(IPipelineBehavior<,>, LoggingBehavior<,>)
  AddTransient(IPipelineBehavior<,>, RealtimeBroadcastBehavior<,>)
  AddScoped<IClinicContext, ClinicContext>()         // needs IHttpContextAccessor (from API/Program.cs)
  AddScoped<ICurrentClinicResolver, CurrentClinicResolver>()
  AddScoped<INotificationGenerator, NotificationGenerator>()
  AddScoped<IAppointmentGoogleSyncDispatcher, AppointmentGoogleSyncDispatcher>()
  AddScoped<ICurrentClinicProvider, CurrentClinicProvider>()   // projects ITenantScope for the EF filter
                                                     // (ITenantScope itself: AddInfrastructure — the verbs need it)
  AddScoped<ICnamBillingCalculator, CnamBillingCalculator>()
```
`ClinicContext`, `CurrentClinicResolver`, `CurrentClinicProvider`, `NotificationGenerator`, `CnamBillingCalculator`, `AppointmentGoogleSyncDispatcher` are all **implemented in this layer** (`Common/Services/`) — they need only Domain repos + `IUnitOfWork` (+ `IRealtimeNotifier`/`IServiceScopeFactory`), no external infra.

## MediatR pipeline (request → response)
`Send(command/query)` → **ValidationBehavior** → **LoggingBehavior** → **RealtimeBroadcastBehavior** → **Handler** (registration order). Behaviors in `Common/Behaviors/`:

- **`ValidationBehavior<,>`** — resolves `IValidator<TRequest>`, throws `FluentValidation.ValidationException` on failure. **No validators exist**, so it is a no-op today; handlers validate inline and return `Result.Failure(...)`. (If one were added, `ExceptionMiddleware` maps the exception → 400.)
- **`LoggingBehavior<,>`** — logs "Handling/Handled {RequestName}".
- **`RealtimeBroadcastBehavior<,>`** — the single wiring point for "any edit is live". After the handler returns (post-commit), if the request is a mutating command **and** the response is a successful `Result`, it resolves the caller's clinic (`IClinicContext`→`IUserRepository`) and calls `IRealtimeNotifier.NotifyEntityChangedAsync(clinicId, "<area>")`; clients of that clinic refetch. Purely structural — the resource key is derived by **`RealtimeResourceResolver`** from the namespace (`...Features.<Area>.Commands` → `<area>` lowercased), so new commands broadcast automatically. Excluded (no broadcast): `Auth`, `AI`, `Backup`, `Connectivity`, all queries. Fail-safe: swallows any resolution/transport error so a broadcast never fails the committed command. `IRealtimeNotifier` is implemented in the API layer (SignalR `ClinicHub`); the key contract with `web/lib/realtime/clinic-hub.ts` is pinned by `RealtimeResourceResolverTests`.

## Result pattern — `Common/Models/Result.cs`
- `Result` — `IsSuccess`/`IsFailure`/`Error`; `Result.Success()`, `Result.Failure(error)`.
- `Result<T>` — adds `Value`; `Result<T>.Success(value)`, `Result<T>.Failure(error)`.
- **Convention:** nearly every handler returns `Result<TDto>`, wraps its body in `try/catch`, and converts business errors/exceptions into `Result.Failure(...)`. `ApiControllerBase` (API layer) maps this to HTTP. A **not-found** subset of newer query handlers instead throws `NotFoundException` → 404 via `ExceptionMiddleware` (e.g. `GetPatientBillingSummaryQuery`, the receipt-PDF queries).

## Clinic resolution (multi-tenant scoping) — two patterns
Every request is scoped to a clinic resolved from the **DB user record** (not just the JWT claim). Two idioms coexist:
1. **Newer / dominant** (~94 files): inject `ICurrentClinicResolver`, call `GetClinicIdAsync()` → `Result<Guid>` (JWT `sub` → `User.ClinicId`), short-circuit on failure.
2. **Legacy** (~34 files): inject `IClinicContext` + `IUserRepository`, do `GetUserId()` then `GetByAuth0SubAsync(...)` inline (e.g. `CreateAppointmentCommand`, `BackupNowCommand`, admin-guarded commands).

Both then verify each loaded aggregate's `ClinicId == clinicId` before use (defense in depth), and that check stays the **authoritative** one. `ICurrentClinicProvider` is the separate, **synchronous** projection of **`ITenantScope`** that the EF Core global query filter reads — two values (`IsSystemWide`, `ClinicId`) because a query filter lambda cannot await a resolver. ⚠️ Since `multi-tenant-cloud` US-2 it is **no longer fail-open**: `TenantScopeKind.Unset` presents as « not system-wide, no clinic », so the filter compares against `Guid.Empty` and returns **nothing**. It also no longer reads the JWT claim — the scope is set from the DB-resolved `User.ClinicId` in API middleware (C3′), so filter and handlers answer to one source.

### Typical handler shape
Resolve clinic → validate inputs / load related aggregates (tenant-checking each) → mutate via aggregate methods → repository `AddAsync/UpdateAsync` → **one `IUnitOfWork.SaveChangesAsync`** → map to DTO → `Result<TDto>.Success`. Post-commit best-effort side effects (notifications, reminders, Google sync) fire *after* the save and never roll it back.

## Feature folders (CQRS) — `Features/<Area>/{Commands,Queries}`
Each command/query file usually holds both the request (`IRequest<Result<...>>`) and its handler. `EventHandlers` folders are vestigial/empty — the domain-events pipeline was removed (no `INotificationHandler`, no `IDomainEvent` dispatch anywhere in the layer). Areas:

| Area | Notes |
|------|-------|
| **Appointments** | **Multi-act séances (`multi-act-appointments`)**: `Create`/`Update` take a `Procedures` list (`AppointmentProcedureRequest`), resolved by **`AppointmentProcedureSelection`** — one shared path for the list *and* the single-act `ProcedureTypeId` shorthand, so **every** act is tenant-checked and required active, not just the first. `Reconcile` promotes the shorthand (including a plan link with *no* procedure, which a hand-typed devis line is — returning an empty list there would lose the link the booking exists for); duration defaults to the **sum** of the acts. `AppointmentPlanLink.ValidateManyAsync` validates a grouped séance's steps against **one** plan load and **returns their désignations**, which is what names a link-only row — hence it runs *before* `ResolveAsync`. ⚠️ On update `Procedures` is **tri-state**: omit to leave the acts, `[]` to clear them. Conflating the two would delete every act on every cancellation (which posts `{ status }` alone) — the same defect the `ProcedureTypeId` tri-state fixed. `AppointmentProcedureMapping` is the single DTO mapper, so the four `AppointmentDto` build sites agree on the live-name-else-snapshot fallback. Single `Create`/`Update` + recurring series (`CreateRecurringSeriesCommand`/`CancelRecurringSeriesCommand`/`GetRecurringSeriesQuery`). Create/Update fire post-commit: `INotificationGenerator` (created / reminder / post-visit-review), `IReminderScheduler` (SMS/WhatsApp), `IAppointmentGoogleSyncDispatcher`. `AppointmentPlanLink` = shared treatment-plan-step link validation. **`AppointmentInvoiceLinks`** (P6, § 6.8) is the read-back of `Invoice.AppointmentId`: one batched projection per request tells both appointment queries which visits are billed and on which note (`AppointmentDto.InvoiceId`/`InvoiceNumber`), so a visit can finally answer « a-t-elle été facturée ? ». It is a shared helper, not inline code, because the list and single reads must agree on **which** invoice counts — a `Cancelled` note does not bill the visit (it would show « Facturé » with no money behind it *and* hide the action to raise a replacement), and where two non-cancelled invoices point at one visit the issued one wins. |
| **Patients** | Patient CRUD; medical/family history CRUD; dental records CRUD; **odontogram** (`DiagnoseToothCommand`, `RemoveToothConditionCommand`, `GetOdontogramQuery`, `ToothState`). Helpers: `DentalRecordActParser`, `DentalRecordLinker`, `DentalRecordMappingExtensions`, **`PatientFromRequest`**, **`PatientDuplicateIndex`**, **`PatientGender`**. ⚠️ **`PatientDuplicateIndex` is the single answer to « do we already have this person? »** — the same normalisation (`SearchTerm.Normalize` for names, `PhoneNumber.ToE164` for phones) and the same three signals for the CSV import's per-row planning *and* for `CreatePatientCommandHandler`'s guard, which runs before `PatientFromRequest.Build` and refuses with **`PatientDuplicateIndex.RefusalCode`** (`patient_duplicate`) unless the command carries `AllowDuplicate`. It was a **private nested class of `PatientImportPlanner`** and was *moved* here, not copied: the import was the only door that checked, so the hand-typed form and the appointment dialog's inline « Nouveau patient » created second files in silence. The guard passes the **raw** `request.DateOfBirth` — `default` means "not supplied", and `Build` is about to substitute « 30 years ago », which would match every patient stored with that day. It reads the clinic's whole `GetIdentitiesAsync` projection rather than issuing a targeted query, deliberately: a SQL predicate would be a second definition of "the same person" and would be the one to drift, and creating a patient is an occasional action. **CSV import (L5)** — `PreviewPatientImportQuery` (dry run) + `ImportPatientsCommand` (commit) over `Import/` (`PatientImportFields` · `PatientImportRowReader` · `PatientImportPlanner` · `PatientImportMapping`). ⚠️ **`PatientFromRequest.Build` is now the single construction-and-validation path for a new patient**, extracted out of `CreatePatientCommandHandler` (which is its other caller) rather than copied — the spec requires the import to reuse the command's validation, and sending the command per row was not an option: every command goes through `RealtimeBroadcastBehavior`, so a 3 000-row file would fire 3 000 broadcasts. Hence also the query/command split: the preview writes nothing and must **not** live in `.Commands`, while the commit does and emits exactly one `patients` key. The planner is **pure** (bytes + mapping + the clinic's `PatientIdentity` list in, a decision per row out) and shared by both, so the preview cannot promise what the commit will not do; the commit saves **once per row** (`IUnitOfWork.StopTracking` after each) because per-*row* atomicity is what the spec asks for and one save for the file cannot give it. |
| **Invoices** | Notes d'honoraires: `Create/Update/Delete/Issue/Cancel`, `RecordPayment`. Queries: get/list, revenue, invoice PDF, payment-receipt PDF. `IssueInvoiceCommand` assigns gapless `AAAA-NNNN` numbers (unique index + recompute-and-retry), taking the year from **`ClinicClock.ClinicYear()`** — `DateTime.UtcNow.Year` numbered a note issued at 00:30 on 1 January into the year that had just closed (§ 4.2, the audit's only 🔴). `AcceptTreatmentPlanCommand` and `CreateCreditNoteCommand` are the same construct and moved with it. `InvoiceMappingExtensions`. **`BillDentalRecordCommand`** (renamed from `CreateInvoiceFromDentalRecordCommand`; + **`DentalRecordInvoiceLines`**) bills a fiche de soins: it prices the session's acts, **issues** the note and optionally records the cash, in **one transaction** — the path that finally makes a session's payment reach la caisse, since `DentalRecord.AmountPaid` was read by nothing but the fiche's own display. ⚠️ It cannot produce a draft (a payment needs an `Issued` invoice), so a number is consumed and **every** validation — amount, method, date, and over-payment against the TTC computed up-front through `InvoiceCalculator.Compute` — runs *before* the transaction opens; a refusal after `Issue()` would leave a numbered, unpaid note behind. `DentalRecordInvoiceLines` is the single authority on the per-tooth rule (quantity × unit price vs. one flat fee), **moved** out of the patient page's browser code rather than copied. ⚠️ **`CreateInvoiceCommand` now validates `AppointmentId`** against the caller's clinic *and* the invoice's patient (AC-P6.12): the column had existed since the invoice was written with nothing ever populating it, so nothing had ever checked it either. `CreateInvoiceFromTreatmentPlanCommand`'s already-billed guard reads the light `GetTreatmentPlanLinksAsync` projection instead of every invoice of the patient with its lines and payments (§ 9.7, AC-P6.22). ⚠️ **Its already-billed branch is no longer a refusal** (`adoption-gaps-remediation` Part 2): it returns a typed **`DentalRecordBillingResult`** — a higher « Montant payé » records the *difference* on the **same** note (`ToppedUp`), nothing to add is `AlreadyBilled`, and lowering it / changing the acts after issue / a note that is annulée or entièrement créditée are `Result.Failure` with a code from **`DentalRecordBillingRefusals`**. The flat refusal was a money leak: raising the amount on a re-save put nothing in the till. **The authoritative guard is `UpdateDentalRecordCommand`'s, pre-commit** — `DentalRecordAutoBilling` runs post-commit, so a refusal raised there arrives after the edit is saved and « refusé » would not mean the save did not happen; both doors call the one **`DentalRecordBillingGuard`**. `IsAutomatic` exists for exactly one decision (A-1): a *re-save* over a **cancelled** note refuses and names it, while pressing « Facturer cette intervention » raises a fresh one. |
| **Billing** | Read-only cross-cutting money views: `GetPatientBillingSummaryQuery` (« Solde patient », invoices + plans + indicative CNAM split), `GetCaisseSummaryQuery`, **`GetCaisseLedgerQuery`** (the « extrait de caisse » — one merged, ordered movement list over the four money ledgers, with a window-relative running balance; **no movement table**, because reading the rows the totals sum is what makes `Σ movements == the totals` testable, and a table written by each money path could never prove it. Voided rows are returned and excluded from the balance; French labels are built server-side so the four kinds share one wording), `GetReceivablesQuery`. ⚠️ `CaisseSummaryDto.CashIn` is **gross** and `Refunds` is a separate field — the old netting broke the moment the statement listed a refund as money leaving, and the dashboard's Argent section carries the identical split. All three (plus `Dashboard`'s outstanding KPI) route their plan side through **`Domain/Services/PlanBillingRules`** — the single de-dup authority — so they cannot report different figures for the same patient. See the Domain guide. Since P6 every calendar-day comparison in them goes through `ClinicClock` (§ 4.1): the caisse's no-argument default is the **clinic-local** day as an inclusive range, and « en retard depuis N jours » counts the clinic's days, not UTC's — these reads take no date arguments, so no caller could compensate. `GetReceivablesQuery` resolves patient names through the batched `IPatientRepository.GetByIdsAsync` rather than one `GetByIdAsync` per row (AC-P6.21). ⚠️ **`CaissePeriod` is now the single authority on a caisse read's window** (`adoption-gaps-remediation` Part 2): the summary and the extrait held byte-identical bound arithmetic kept in step by a comment, and both now take bare **`FromDay`/`ToDay`** (`YYYY-MM-DD`) resolved through `ClinicClock` — the browser used to compose the instants itself, in the *workstation's* timezone, so « la caisse du 3 août » covered a window offset by hours from the Tunisian day (AC-6). The `From`/`To` instants still work and the day keys win. `GetExpensesQuery` takes them too — la caisse's dépenses table shares the window — but **only resolves a period when one was asked for**, since « no dates » there means « toutes les dépenses », not « aujourd'hui ». |
| **TreatmentPlans** | ⚠️ **A devis is accepted at creation — there is no draft stage.** `CreateTreatmentPlanCommand` numbers and accepts the plan in the **same** `SaveChanges`, through the shared **`DevisNumbering.AcceptAndSaveAsync`** (the gapless `AAAA-NNNN` sequence + recompute-and-retry, extracted so create and accept cannot drift). The draft stage was a second confirmation of a decision already made with the patient in the chair, and it silently kept the plan out of « Solde patient » / « Créances » until someone pressed a button. Consequences to know: a **number is consumed per created plan** (a mistake is `Cancel`led with a motif, never deleted — `CanBeDeleted` is Draft-only), acceptance **requires ≥1 act** so an empty plan is refused at creation, and a plan with no échéancier gets an auto lump-sum one. `AcceptTreatmentPlanCommand` is **kept for the drafts that already exist** — deleting it would strand them, since `SetItems`/`Accept` are the only Draft-legal operations. Every `isDraft` branch in the UI is therefore legacy-only and needed no change. Devis lifecycle: `Create/Update/Delete/Accept/Complete/Cancel`, `MarkTreatmentPlanItemDoneCommand`, `RecordInstallmentPaymentCommand`; **post-acceptance amendment** — `AmendTreatmentPlanCommand` (add/remove acts + re-spread the échéancier in one call), `ReviseTreatmentPlanInstallmentsCommand`, `SetTreatmentPlanItemOrderCommand`; devis + installment-receipt PDFs. Helpers: `TreatmentPlanMappingExtensions`, **`TreatmentPlanWorkflowProjection`** (the derived read-back: which appointment speaks for each act + which invoice bills the plan, two batched reads per request), `TreatmentPlanItemPricing`. ⚠️ The **billed-plan block** lives in the amend/revise *handlers*, not the aggregate — `TreatmentPlan` holds no invoice reference, and a plan represented by a non-cancelled invoice must refuse every amendment or added acts would be invisible in every money read. |
| **CnamNomenclature** | CNAM catalog CRUD (`Create/Update/Deactivate` entry, `UpdateCnamLetterValueCommand`, `ConfirmCnamDataCommand`), queries (nomenclature, letter values, `GetReimbursementEstimateQuery` + **`GetReimbursementEstimatesQuery`**). `CnamReimbursementCalculator` (pure: coefficient × VLC × age-rate, 70% ages 4–18 else 60%); `CnamEntryMapper`. The **batch** query (`POST /api/cnam-nomenclature/reimbursement-estimates`) exists so the BS1 editor stops reimplementing the calculator (§ 5.10, AC-P6.15): the editor shows a live estimate **per act row**, so the single-act endpoint per row per keystroke was never a usable substitute — which is the pressure that produced the client-side `CHILD_RATE`/`ADULT_RATE` duplicate in the first place. It reads the VLC set **once**, aligns results to the request by index, carries a per-item `CareDate` (the rate turns on age *at the care date*, and a bulletin's acts can straddle a birthday), and caps the item count. Still editor-only: never persisted, never on the BS1 PDF. |
| **DentalActs** | Dental act-code catalog CRUD + `ConfirmDentalActsCommand`; `GetDentalActsQuery`; `DentalActMappingExtensions`. |
| **Medications** | Medication catalog CRUD + `ConfirmMedicationDataCommand`; `GetMedicationsQuery`; `MedicationMapper`. |
| **Doctors** | `UpdateDoctorProfileCommand`, `SetDoctorWorkingHoursCommand`; queries: my profile, working hours, `GetDoctorCachetQuery`. |
| **Stock** | Item CRUD + `GetStockItemsQuery` (which reads the clinic's `StockExpiryLeadDays` to flag expiring lots) + `Consume`/`Restock` by delta. `Update` fires `INotificationGenerator.LowStockAsync` on a not-low->low crossing, **and now writes a `StockMovement`** whenever on-hand changes (AC-P4.15 - it used to overwrite the quantity with no ledger row, which is why sum-of-movements stopped reconciling with on-hand) using the new `StockMovementType.Adjustment`, plus `SetExpectedVersion` so a concurrent consume 409s instead of being silently overwritten. All three movement write sites now populate `Reason`; all three passed `null`. |
| **Expenses / LabOrders / WaitingList / Recall** | Expenses CRUD+list; lab work-order CRUD + status (+ a `Status` **stage filter** on `GetLabWorkOrdersQuery`, added by `dashboard-insights` — the list had none, which left « Prothèses en retard » with nowhere truthful to land; an unknown value is ignored, not refused, so a stale deep link shows the full list); waiting-list CRUD + `PromoteWaitingListEntryCommand`; recall (`SendRecallCommand`, `SnoozeRecallCommand`, `MarkRecallContactedCommand`, `SetRecallSettingsCommand`, `GetPatientsToRecallQuery`, settings). The relance due-date rule now lives in one place — **`Features/Recall/RecallDueRule`** (`AnchorUpperBound` = the widened SQL bound, `IsDue`/`DueDate` = the exact `AddMonths` test) — shared by the query and the dashboard's « patients à relancer » count, so the list and the number linking to it cannot disagree. ⚠️ **`SendRecallCommand` now branches on the enqueue's outcome**: it used to stamp « contacté » + snooze 30 days and return success even when `ScheduleRecallAsync` had queued nothing (no channel configured), so the patient silently left the relance list for a month with nobody contacted. A non-`Enqueued` outcome is a French `Result.Failure` naming the settings and « Marquer comme contacté », and the patient is left untouched. **Enqueuing is still not sending** — the dispatcher undoes the snooze via `Patient.ClearRecallSnooze()` once every channel of that one send has reached `Failed` (`NotificationJob.TryReturnPatientToRecallListAsync`). ⚠️ **`GetPatientsToRecallQuery` is now bounded** (AC-P4.41, § 9.6): it used to load *every* patient and *every* appointment in the clinic and derive the list in memory. Filters live in `IPatientRepository.GetRecallCandidatesAsync` (clinic, archived, snooze, future-booking as `EXISTS`, last completed visit as a correlated `MAX`, plus a date bound), and the handler no longer takes `IAppointmentRepository` at all. The date bound is a **deliberate superset**: the rule is `anchor.AddMonths(interval) <= now`, and inverting it to `anchor <= now.AddMonths(-interval)` is *not* equivalent — `AddMonths` clamps to the end of a shorter month (31 Jan + 1 month = 28 Feb, so on 28 Feb that patient is due, while 28 Feb − 1 month = 28 Jan drops them). SQL therefore gets the bound widened by three days (the largest possible clamp) and the handler applies the exact test, so results are identical (AC-P4.42). |
| **Notifications** (in-app feed) | `MarkNotificationReadCommand`/`MarkAllNotificationsReadCommand` (return `Result.Failure("...not found")` on tenant/missing → 404 by controller). Queries: `GetNotificationsQuery`, `GetUnreadCountQuery`, `GetPendingReviewsQuery` (due post-visit-review popups). |
| **Clinics** | `Create`/`Update`/`Join`/`UpdateDoctors`/`RegenerateClinicCode`, WhatsApp connect/disconnect, **`DisconnectGoogleCalendarCommand`** (admin-only; the first caller of `Clinic.ClearGoogleCalendarConnection()`, idempotent, and deliberately leaves every `Appointment.GoogleCalendarEventId` alone — those point at real events in the clinic's own Google account), `UpdateClinicReminderSettingsCommand`. Queries: user status, clinic logo, reminder settings, reminder status. `ClinicCodeGenerator`. ⚠️ **`LocalClinicProvisioning` is the single definition of « create a clinic with a password-backed admin »** (US-3), shared by `CreateClinicCommand`'s Local first-run branch and the `provision-clinic` console verb. It was **moved** out of the handler, not copied: the verb has no mediator and no HTTP context, and it could not reuse the command anyway — that branch refuses once **any** user exists (AC-1.2a), so it can create an install's first clinic and never its second, which is the only thing the verb is for. The bootstrap gate and the password-length policy therefore stay in the handler; the helper does construction only, and takes the clinic id from its caller so the verb can declare `UseClinic(id)` **before** the writes it covers. |
| **Users** | **`CreateClinicUserCommand`** (US-3 — an admin creates a colleague's account and gets a one-time password back; the account is **active**, unlike `User.CreateSelfRegistered`, because this *is* the approval a self-registration waits for, and the clinic comes from the caller's own record since the command carries no clinic id at all), `ResetUserPasswordCommand`, `SetUserActiveCommand`, **`ChangeUserRoleCommand`**, `ListUsersQuery` (all admin-only). The role change validates against `User.AssignableRoles` (the closed set — nothing validated a role before), leaves email/full name untouched, refuses a **self-demotion that would leave the clinic with no other *active* admin**, and relies on `User.ChangeRole` bumping `TokenVersion` so the old role does not stay live for the token's lifetime. |
| **ProcedureTypes** | CRUD + `InitializeDefaultProcedureTypesCommand`; `ProcedureTypeCatalogSeed`. **Categories (`procedure-type-categories`)**: `Category` is a real column now, `GetProcedureTypesQuery` takes a SQL `Category` filter (an unrecognised value matches nothing rather than failing — same tolerance as the lab-order stage filter), and **`GetProcedureTypeCategoriesQuery`** serves the twelve canonical disciplines **unioned with the ones this clinic has actually used**. The union is the load-bearing half: the field is open text, so without offering a clinic's own labels back, the second admin to want « Occlusodontie » retypes it and splits the group. It is a query rather than a browser constant precisely because that half is *data*. ⚠️ `Category` on the update command is **tri-state** like every other field there (omit = unchanged, `""` = unfile); the create path and the seed both pass **named** ctor arguments, since `description` and `category` are adjacent nullable strings and transposing them is how the original defect worked. **`SetProcedureTypeMaterialsCommand`** (AC-P4.14) is the missing caller for `ProcedureType.SetMaterials` — the entity method and the join table shipped without one, so a material list could only be inserted straight into the database, which made act-driven consumption unreachable. Deliberately a **separate** command from `UpdateProcedureTypeCommand`: the list has **replace** semantics (an empty list is the AC-P4.11 opt-out, not "unchanged") while every field of the update command is null-means-unchanged, and folding replace-semantics into a patch command is how a list gets silently wiped. It validates that every referenced stock item belongs to the caller's clinic — without that check a crafted request could point an act at another clinic's stock and `StockConsumptionService` would draw *their* stock down on fiche save. The DTO is now built by one shared `ProcedureTypeDto.ToDto()` rather than four hand-written copies, because adding `Materials` to three of four sites would have left it empty on the fourth — indistinguishable from the opt-out. |
| **Meta** | **`GetUploadPolicyQuery`** (`patient-file-uploads` P3) — the patient-file profile projected for the browser: the `accept` string, the per-format caps and previewability, and the refusal wording. It reads `FileTypeCatalog`/`FileUploadProfile` and nothing else — no repository, no clinic — because the catalog is install-wide; serving it is what removed the client's hand-mirrored allow-list, which was accurate the day it was written and hid every DICOM the day the catalog widened. |
| **Files / Documents** | Patient folder/file CRUD + download over `IFileStorage` (store-blob-then-persist; delete blob if the DB save fails). **`UpdatePatientFileCommand`** and **`RenamePatientFolderCommand`** (P4) are the first callers of four entity methods that had shipped with none — `PatientFile.Rename`/`UpdateDescription`/`MoveToFolder` and `PatientFolder.UpdateName`. ⚠️ `Rename` takes a **base** name and the entity recomposes from the *stored* extension, so changing a file's format through the API is unrepresentable rather than merely refused — and `FileNameSanitizer.SanitizeBaseName` bounds it so `base.extension` still fits. Every field is tri-state (omit = unchanged, `""` = cleared), and the folder move is tenant-checked against the *patient*, not just the clinic. **`GetPatientFilesQuery` is paged** (AC-5.9) over one `IPatientFileRepository.GetPageAsync(patientId, folderId, paging)`, ordered `UploadedAt` desc then **`Id`** — without the unique tie-break, `OFFSET` can show a row on two pages and skip another, which reads as « a scan disappeared ». Medical documents CRUD + PDF snapshot (`GetPractitionerRenderSnapshotQuery`, `DocumentFileNaming`, `DocumentTypes`, `PractitionerRenderSnapshot`). ⚠️ **`PractitionerRenderSnapshot.ResolveAsync` resolves whose cachet + n° d'ordre a document carries, and the order is `IssuingDoctorId` → the caller's own `Doctor` record → none** — `PractitionerAttribution`'s rule one candidate shorter, and for the same reason: the caller is the *last* resort, so the practitioner **named** on the document is the one it is stamped with. It used to resolve from the caller only, on a stated single-practitioner assumption; once `MedicalDocumentsController` opened to `AnyClinicRole` the routine author became reception, who has no `Doctor` record, and every such document rendered with **no** practitioner identity at all — silently, on forms whose purpose is to carry it. The chosen id is **tenant-checked** before it is believed and a foreign/stale/`Guid.Empty` one *falls through* rather than resolving nothing. No schema change was involved: the *resolved* snapshot has always been persisted into `ContentJson` by `ApplyTo` and preserved across edits by `ReadFrom` + `OrElse`, so what was missing was a selector on the request — not the persisted `DoctorId` the old note here claimed it would need. On the update path the resolution still sits inside the `user != null` guard, because the unauthenticated `PdfGenerationJob` feeds a stored document back through the same handler and must preserve its snapshot verbatim. |
| **Dashboard** | **`GetDashboardQuery`** — one composed read (`GET /api/dashboard?period=Today\|Week\|Month`) returning four sections. The handler is a thin composer that fans out to four **section readers** (`Readers/DashboardActivityReader` · `MoneyReader` · `AlertsReader` · `TrendReader`), the same shape as `TreatmentPlanWorkflowProjection`. Two primitives carry the design: **`DashboardPeriod`** (the *single* authority on period arithmetic — derives the current **and** previous bounds through `ClinicClock`, so a comparison's two halves cannot come from different rules; `ToInclusive` is the last **tick** of the window because `EndOfLocalDayUtc` is exclusive while the money reads are inclusive — finding #20 re-armed otherwise) and **`PeriodComparison`** (`Current`/`Previous`/`DeltaPercent`, with `Of` for absolute figures and `Rate` for rates whose denominator can be zero — an empty period has *no* taux d'absence, and `0 %` would assert perfect attendance). ⚠️ The readers are awaited **sequentially**: they share the request's `DbContext`, so `Task.WhenAll` throws. ⚠️ `MoneyReader` computes `PlanBillingRules.BilledPlanIds` **once** and passes it to both the cash and the debt call; it deliberately reuses the exact repository calls `GetCaisseSummaryQuery` uses, and `MoneyReadConsistencyTests` now pins dashboard-vs-caisse agreement. *(Replaced `GetDashboardStatsQuery` + `DashboardStatsDto`, which took six client-supplied boundaries and returned seven flat scalars — deleted.)* |
| **Audit** | **`GetAuditEntriesQuery`** — « Journal d'activité », the read side of the I6 ledger. Admin-only (the controller's `AdminOnly` policy), filterable by entity type / entity id / inclusive clinic-local date window / action, and **always paged**: unlike the pickers and the money totals there is no legitimate caller for « every audit row of this clinic », since the table grows with every save the practice has ever made. Omitting the paging parameters gets the *first page*, not everything — deliberately the opposite of the list reads. Dates go through `ClinicClock.StartOfLocalDayUtc`/**`LastTickOfLocalDayUtc`** so « le 3 août » means the clinic's day (the exclusive `EndOfLocalDayUtc` would count a midnight action in both adjacent days — finding #20). An unparseable `action` is **ignored, not refused**, the same tolerance as the lab-order stage filter. French wording lives in **`AuditLabels`** (server-side, like the caisse ledger's: the entity names are CLR type names a client could not translate without duplicating the map, and an unmapped type falls through to its own name rather than to « Inconnu »). |
| **Auth** (Local) | `LoginCommand` (email+password → JWT; lockout-before-password, generic `InvalidCredentialsError`, rehash-on-login, inactive disclosed only after correct password), `ChangePasswordCommand`, **`RefreshTokenCommand`**. ⚠️ The refresh exchange is **sliding expiry, not revoking rotation** (`mobile-native-shells` P2): it mints a *fresh* refresh credential each time — so an active user is never prompted by time alone — while the superseded one stays valid until its own expiry, because it is stateless and nothing stores it. `TokenVersion` remains the only revocation. Two tabs exchanging at once must both keep working, so a test asserting the superseded one is refused would pin a property this design does not claim. **`SignUpClinicCommand` + `VerifyClinicSignUpCommand`** (`clinic-self-signup`) are the public door onto a hosted backend: the first writes one pending `ClinicSignup` (hashed password, SHA-256 of a CSPRNG token that exists in plaintext only in the email) and creates nothing else; the second consumes it and calls `LocalClinicProvisioning.ProvisionAsync` — its **third** caller, unchanged. ⚠️ **They are here and not under `Features/Clinics/Commands` for a mechanical reason**: `RealtimeBroadcastBehavior` derives its key from the namespace, so there they would broadcast `clinics` — announcing a clinic that does not exist — while `Auth` is on the behavior's excluded list. ⚠️ Signup answers **one neutral French sentence** for free / already-an-account / already-pending alike (no enumeration oracle), and verification answers **one refusal** for expired / unknown / malformed / now-taken — spending the row in the last case, since the link can never do anything again. ⚠️ **No `ITenantScope.UseClinic`** in the verifier, deliberately: `provision-clinic` declares one because it has *no HTTP context*, not because the seed needs it — `ClinicCatalogSeeder` calls `IgnoreQueryFilters()` on every read and everything else in the provision is an `Add` or a read of the two unfiltered tables. |
| **PushDevices** | **OS push registration** (`mobile-native-shells` P6). `RegisterPushDeviceCommand` is deliberately **one** command for register / refresh / **rebind** — the shell cannot tell them apart and sends the same call at every sign-in, so which of the three it turns out to be is a fact about our table, not about the client. It refuses an unsupported platform (`IOsPushAvailability`) rather than queueing a row nothing can drain, and **never** 409s a token bound to somebody else: a shared reception tablet is the normal case (AC-41). `DeletePushDeviceCommand` deactivates rather than deletes and is **idempotent** — sign-out fires while the session is already being torn down, so a refusal it cannot act on would only produce a French error on the way out. ⚠️ Note the deliberate asymmetry: registration looks **across** clinics (the token is globally unique, so a scoped lookup would turn a rebind into a unique-index violation) while deregistration does not (it would let a caller unsubscribe another clinic's device). `GetPushAvailabilityQuery` reports every platform **always**, zeros included — « iOS : non configuré » is a statement, an absent row is not — and is read by both the settings card and the shell, which uses it to avoid burning the OS's single permission prompt on an installation that cannot push. |
| **Subscriptions** | **A cabinet's dated right to record new work** (`clinic-subscription`). Part A ships the two pure pieces every later part reads: **`SubscriptionProvisioning.CreateForNewClinic`** — the one definition of what entitlement a new cabinet starts with (a **trial** where subscriptions are enforced, **open-ended** where they are not, which is what makes FR-13 hold in all three topologies while nothing can expire in two of them) — and **`SubscriptionStateReader`**, the single FR-1 rule `(entitlement, clinicToday) → (State, AllowsWrites, ShouldWarn, DaysRemaining?)` that the gate, the screen, the banner, the warning job, the report and every vendor verb all read, so none of them can answer « is this cabinet expired? » differently. ⚠️ **Both take primitives and neither names `DeploymentProfile`** — it lives in Infrastructure and this project references **Domain alone**, so the capability arrives through `ISubscriptionPolicy`. ⚠️ **Neither computes a date**: provisioning builds a ledger entry and lets `ClinicSubscription.RecomputeFrom` fold it, so the arithmetic exists in exactly one place (see the Domain guide on `SubscriptionLedger`). ⚠️ `SubscriptionStateReader.Read`'s `isTrial` is a **label** parameter, not a lookup: Trial-vs-Active changes nothing about writes or warnings (AC-1.4), and deciding it inside would put the ledger on the gate's hot path. **Part B** adds **`SubscriptionRefusals`** — the three French sentences and their codes (`subscription_required` / `_suspended` / `_missing`) in **one** place, because the sentence and the code are one statement and three copies is how a reworded message silently stops matching the code it was paired with (the `Contains("déjà facturée")` defect one feature over). ⚠️ `MissingCode` is **distinct from `RequiredCode` on purpose** (EC-6): no entitlement row is a fault on our side, so « renouvelez votre abonnement » would be advice the cabinet cannot act on. ⚠️ Every sentence says what **still works** before what does not — it is read chairside, mid-consultation, by somebody wondering whether the patient's file is gone — and none of them mentions signing in or out, because the refusal never ends the session (AC-4.5). The enforcement itself is API-side (`SubscriptionGateMiddleware`); nothing here names a deployment profile. **Part C** adds the two reads behind « Abonnement » — **`Queries/GetSubscriptionQuery`** (state, dates, forfait, the published tariff, the payment instructions and contacts) and **`Queries/GetSubscriptionHistoryQuery`** (the ledger, paged) — plus **`SubscriptionLabels`**, the French name of all four closed enums, server-side on `AuditLabels`' precedent (a client-side map would be a second list to extend, and the one that forgets a new member renders a raw `SurMesure` to a dentist). ⚠️ **`GetSubscriptionQuery` reads the ledger and the gate deliberately does not**: the entitlement row carries one date and no memory of where it came from, so « is the cover in force the free **trial**? » needs the fold — which is precisely what `SubscriptionStateReader.Read`'s `isTrial` parameter exists for. The gate runs on every write and stays one indexed row; this runs when somebody opens a screen. ⚠️ **The history folds the WHOLE ledger and cuts a page afterwards** (`PagedResult.FromSource`): each entry's « période couverte » is a function of every non-cancelled entry recorded before it, so a SQL `OFFSET` would hand the fold a window and page 2's dates would restart from that window's first row — entirely plausible figures describing periods the cabinet was never entitled to. Same reason « Créances » and the « extrait de caisse » page in memory. ⚠️ **A missing entitlement row is a `Result.Failure` carrying `MissingCode`**, never an empty success: EC-13's « never aucun abonnement » applies to that path too. |
| **AI** | `ChatCommand` + separate `ChatCommandHandler` (`IAIActionService` then `IHuggingFaceAIService`). *(`Features/AISummary/` is empty scaffolding — the real summary is under `Patients`.)* |
| **Connectivity** (Local) | `GetConnectivityStatusQuery` (probes `IInternetProbe`; swallows errors → `internetReachable=false`, never a 500). |
| **Outbox** (operator read) | **`GetOutboxDepthQuery`** (multi-tenant-cloud US-6) — how deep the clinic's two background queues are: reminders and document emails. Admin-only (re-checks `IsAdmin()` like its siblings) and clinic-scoped from the caller's **own** `User.ClinicId`, never install-wide — an install-wide figure would hand one practice a fact about another's workload, and it buys nothing for detection since a stopped dispatcher stops for every clinic at once. ⚠️ **Both reads take ONE instant**, because the whole value of the read is comparing the figures with each other and with the reading taken five minutes ago. ⚠️ **Sequential, not `Task.WhenAll`** — the repositories share the request's `DbContext`, the same constraint the dashboard's section readers document. The `FailedRecent` window travels **on the DTO** (`FailedSinceUtc`), because « 4 failed » cannot say whether that is today or since the install. |
| **Platform** (vendor console) | The console's own feature area (`platform-console`). Auth (Part 1) plus, since Part 2, **`ListPlatformClinicsQuery`** and **`GetPlatformSummaryQuery`** — the cross-cabinet portfolio, paged through `PagedResult`/`PageRequest` over one bounded `Clinics ⋈ ClinicActivitySnapshot` JOIN, with the free-text term normalised by `SearchTerm` and matched in SQL. Four pieces carry the design. **`PlatformReadShape`** is the closed set of scalar field names this surface may return and is the *whole* enforcement of US-7 (AC-7.2) — the tenant filter explicitly is not (AC-7.2a), because it is deliberately lifted here. **`PlatformCounterPass`** is the pure counter derivation and owns AC-2.2's two exclusions (`job|…` and `console|…`, matched on `AuditActor`'s own constants). **`PlatformCollectedReader`** is « encaissé par le cabinet », reusing la caisse's repository predicates — it is why the console is the *fifth* read held by `MoneyReadConsistencyTests`. **`PlatformSubscriptionPlaceholder`** is the single named admission that entitlement is `features/clinic-subscription/`'s and is not here yet; Part 4 **deletes** it rather than widening it, which would be the FR-4 violation the feature is defined around. ⚠️ **`PlatformTenantScope` lives here, not in the API**: its `EnsureDeclared` backstop runs at the handlers' entry point and throws rather than reading zero rows — « je n'ai pas pu lire » and « aucun cabinet » are opposite facts with the same picture (EC-12). **Part 3 adds `GetPlatformClinicDetailQuery`** (one cabinet: the list's row verbatim, the administrator's contact, a six-month trend bucketed on `ClinicActivityDay.Day` — already a Tunisian calendar day, so no timezone arithmetic happens here at all) and **`GetPlatformAccessLogQuery`** (the console's own ledger, read back). ⚠️ **The detail is a `Query` that writes**, through the shared **`PlatformAccessLedger`** — a `Command` would broadcast into a clinic group on every page load, since `RealtimeBroadcastBehavior` derives its key from the namespace. ⚠️ **Its ledger row is the one non-best-effort side effect in this layer**: `INotificationGenerator` swallows because the operation it follows has already committed, whereas here the operation *is* what is being recorded, so an unattributable read fails instead of succeeding unrecorded. **`PlatformAccessLabels`** is the French wording (`AuditLabels`' counterpart, `fr-FR` pinned explicitly because a container's ambient culture would render « August 2026 » into a French screen) |
| **Backup** (Local) | `BackupNowCommand` (admin-only; re-checks `IsAdmin()`, delegates to `IBackupService`; catches `InvalidOperationException` → `Result.Failure`, lets other exceptions propagate). |

## Outbound interfaces — `Common/Interfaces/`
Implemented in **Infrastructure** unless noted. Grouped:

**Audit (impl here)** — **`IAuditActorProvider`** (+ the `AuditActor` record struct): who the ledger stamps on the rows written in this scope. Two implementations, both resolve-once so one operation carries one actor even when it changes the caller's own account mid-flight: **`AuditActorProvider`** reads the token (registered by `AddApplication`, so it wins in the API), and **`ProcessAuditActorProvider`** has no dependencies and is registered by `AddInfrastructure` with `TryAdd` — the console verbs build their container from `AddInfrastructure` *alone*, so without a floor implementation the first verb that writes anything could not resolve its own `DbContext`. A non-interactive caller declares itself with `RunAs(name)` and is recorded as `job|<name>`; the four Hangfire jobs and `reset-admin-password` all do. The token always outranks a declared process name, so a helper running inside somebody's request cannot claim their work.

**Tenant / context (impl here)** — `IClinicContext` (JWT claims → clinic/role/user/email; `BelongsToClinic`/`EnsureClinicAccess`→`ForbiddenAccessException`), `ICurrentClinicResolver` (async `GetClinicIdAsync()`→`Result<Guid>`), **`ITenantScope`** (`Unset`/`Clinic(id)`/`SystemWide(reason)` — whose rows this scope may read; impl `TenantScope`, registered by `AddInfrastructure`), `ICurrentClinicProvider` (its sync projection for the EF filter), `IUnitOfWork` (`SaveChangesAsync` + explicit `Begin/Commit/RollbackTransactionAsync` + **`SetExpectedVersion(entity, version)`** — validates the save against the version the *user* was editing, since the one the handler just loaded always matches and would detect nothing; `0` = not supplied, check skipped), `IRealtimeNotifier` (SignalR, impl in API).

**Cabinet entitlements (`clinic-subscription`)** — **`ISubscriptionPolicy`** (impl `SubscriptionPolicy` in
Infrastructure) is the capability seam, and it is **structurally required rather than stylistic**: `DeploymentProfile`
lives in Infrastructure and this project references Domain alone, so no Application type can name it. Two members
answering two *kinds* of question, which is the whole point of the split — `RequiresSubscription` is derived from the
deployment's **kind** and from nothing an operator can set (AC-7.3; a `Subscription:*` key able to flip it would put a
clinic's own PC one edit away from refusing its own records), while `TrialDays` **is** operator config.
`SubscriptionProvisioningTests` pins that no configuration value moves the first answer.
**`ISubscriptionPricing`** carries the per-deployment prices, payment instructions and contact details (AC-2.4 — a
price must not be compiled in); every member may be absent, and an unpublished price reads as *absent* rather than as
« 0,000 DT ».

**OS push (P6)** — **`IOsPushAvailability`** (impl `OsPushAvailability` in Infrastructure) is the single « can this installation push to this platform? », asked by the registration endpoint, the fan-out, the dispatcher and the settings surface so all four agree. It is the **AND** of two questions that belong in different places: `DeploymentProfile.PermitsOsPush` (derived from the deployment **kind** alone) and whether that platform's credentials are present (configuration). ⚠️ Keeping the second out of `DeploymentProfile` is the whole point — it is what lets `SelfHostedLan` stay ✗ *whatever* an operator configures, and it preserves that file's own stated invariant that no operator setting can flip a capability (the `httpsConfigured` trap, LEARNINGS `:45`). Deliberately **not** per-clinic: one mobile app per deployment means one Firebase project and one Apple team, so modelling it per clinic would promise control that does not exist.

**In-app feed / reminders** — `INotificationGenerator` (impl here — staff-feed writer: appointment created/reminder/cancelled/rescheduled, low-stock, post-visit-review, **reminder-delivery-failed**, **approaching-expiry stock**; best-effort, never throws, logs at Error). ⚠️ The expiry pair (`EnsureStockExpiringSoonAsync`/`ClearStockExpiringSoonAsync`, AC-P4.6) is an **ensure**, not a fire-once like `LowStockAsync`: low stock is crossed by a write, expiry by the passage of time, so the daily `StockExpiryJob` re-evaluates the same item every run and a fire-once call would write a duplicate row every day. Idempotency is matched on the message's **item+date prefix**, not the whole message — the message carries a countdown, so a whole-message comparison would differ daily and turn the ensure back into a daily broadcast. `Clear` is what allows the item's *next* batch to alert. `IReminderScheduler` (SMS/WhatsApp outbox enqueue/void, best-effort post-commit; `ScheduleRecallAsync` alone **returns** a `RecallDispatchOutcome` — a recall is user-initiated, so its caller has to be able to refuse instead of snoozing the patient 30 days having queued nothing), `IReminderSettingsProvider` (effective per-clinic-else-install settings + enabled channels), `IReminderSecretProtector` (encrypt/decrypt channel secrets over Data Protection), `IWhatsAppOnboardingService` (Meta Embedded-Signup steps; `WhatsAppOnboardingException`/`WhatsAppOnboardingError`).

**Billing / CNAM** — `ICnamBillingCalculator` (impl here — indicative reimbursable/out-of-pocket split; defines `CnamBillingLine`/`CnamSplit`), `IQrCodeGenerator` (the LAN trust page's QR), `IPdfGenerationService` (document / invoice / devis / receipt PDFs).

**Integrations / infra** — `IFileStorage` (mode-branched MinIO vs LocalDisk; **`ProbeAsync`** added by US-6 answers « is the backend reachable? » for `/health` — it **throws** with the backend's own message rather than returning a bool, since a health report that could only say « false » leaves the operator exactly as blind as the 503, and it stores **nothing**, because a container probe runs every few seconds for the life of the deployment. ⚠️ Since US-5 **both `UploadAsync` overloads require the owning `Guid clinicId`** and the backend composes `clinics/{clinicId}/…`, so a caller cannot write an unprefixed key; the second overload's path is **relative to the clinic** — a caller that adds a clinic segment of its own gets `clinics/{id}/{id}/…`. Download and delete take the stored key **verbatim**, which is what lets pre-US-5 flat keys keep resolving with no backfill), `IGoogleCalendarService` (low-level CRUD; `GoogleCalendarEvent`), `IGoogleCalendarSyncService` (two-way sync), `IAppointmentGoogleSyncDispatcher` (impl here — fire-and-forget, connectivity-gated post-commit push over `IServiceScopeFactory`), `IInternetProbe` (server-egress check, cached), `IHuggingFaceAIService` (sole wired AI chat backend), `IAIActionService` (AI-driven action decide/execute; `AIActionRequest`/`AIActionResult`), `IBackupService` (`pg_dump` + file copy; distinct `InvalidOperationException` per failure), `IClinicCatalogSeeder` (per-clinic default CNAM/medication/dental-act catalogs; idempotent, startup backfill).

**Auth** — `ILocalAuthService` (Local: PBKDF2 hash/verify with rehash outcome, HS256 JWT, CSPRNG temp password), `IAuth0ManagementService` (push `clinic_id`/`role` into Auth0 `app_metadata`; Local wires a no-op).

> **Retired:** `IGoogleTokenStore`/`FileGoogleTokenStore` were removed (cloud-security-and-tenant-isolation) — Google refresh tokens are now per-clinic in the DB, not a shared `.local/` file.

## DTOs & models
- **`DTOs/`** — request/response records for handlers + controllers. Clinical: `PatientDto`, `AppointmentDto` (`IsSyncedToGoogle` ← `GoogleCalendarEventId != null`), `RecurringAppointmentDto`, `DentalRecordDto`, `ToothStateDto`, `MedicalDocumentDto`, `PatientMedicalHistoryDto`/`PatientFamilyHistoryDto`, `PatientFlagDto`, `PatientFileDto`, `ProcedureTypeDto`. Money: `InvoiceDto`, `TreatmentPlanDto`, `PatientBillingSummaryDto`, `CaisseSummaryDto`, `ReceivableDto`, `ExpenseDto`, `DashboardStatsDto`. CNAM/acts/meds: `CnamNomenclatureEntryDto`, `CnamLetterValueDto`, `CnamInfoDto`, `ReimbursementEstimateDto`, `DentalActDto`, `MedicationDto`. Ops: `StockItemDto`, `LabWorkOrderDto`, `WaitingListEntryDto`, `RecallDto`, `PendingReviewDto`, `NotificationDto`, `ConnectivityStatusDto`, `BackupResultDto`. Clinic/users/auth: `ClinicDto`, `ClinicUserDto`, `UserDto`/`UserStatusDto`, `DoctorProfileDto`/`DoctorPersonalInfoDto`/`WorkingHoursDto`, `LoginResultDto`, `ResetPasswordResultDto`, `ReminderSettingsDto`/`ReminderStatusDto` (`ReminderSettingsMappings.cs`), plus request shapes (`CreateClinicRequest`, `JoinClinicRequest`, `UpdateDoctorsRequest`, …).
- **`Common/Models/`** — `Result`, PDF inputs (`MedicalDocumentPdfData`, `InvoicePdfData`, `DevisPdfData`, `ReceiptPdfData`), `ResolvedReminderSettings` (decrypted-in-memory effective reminder settings; `SmsConfigured`/`WhatsAppConfigured` single-source-of-truth sendability).
- **Mapping** lives in co-located static helpers in the feature folders (`InvoiceMappingExtensions`, `TreatmentPlanMappingExtensions`, `DentalActMappingExtensions`, `DentalRecordMappingExtensions`, `MedicationMapper`, `CnamEntryMapper`) + `DTOs/ReminderSettingsMappings.cs`. **`Common/Mappings/` is empty** (no AutoMapper).

## Cross-cutting — `Common/`
- **Services** (`Common/Services/`) — `ClinicContext`, `CurrentClinicResolver`, `CurrentClinicProvider`, `NotificationGenerator` (feed writer; `SafelyAsync` logs-and-swallows, broadcasts `"notifications"`), `CnamBillingCalculator`, `AppointmentGoogleSyncDispatcher`, **`WorkingHoursResolver`** (pure + static, not DI-registered — the **single** authority on effective working hours: doctor override → clinic → **none = unrestricted**; `IsWithin` converts the UTC instant to clinic-local before comparing, because the stored hours are wall-clock times. An unreadable stored value falls through to the next level *and is reported*, never silently read as "no restriction").
- **`Common/Services/StaffNotificationRules`** (P6) — the facts about a staff notification that **two** writers now
  need: the in-app feed (`NotificationGenerator`) and the OS-push fan-out that decorates it. The reminder lead time
  (24 h) and the doctor→user target resolution were *private details of the generator* and were **moved** here, not
  copied — a second copy of « the reminder fires 24 h before » would mean a banner arriving at a different hour from
  the feed row it announces, visible to a user and invisible to every test that mocks one side. It also owns
  `ReachesALockedPhone` (the five time-critical categories vs. the four operational ones) and `PushLabel` (the fixed
  French phrase that is the *whole* of what a lock screen may show). ⚠️ Both **throw** on an unclassified category
  rather than defaulting: `true` would put an unreviewed message on a lock screen and `false` would look like a
  decision nobody made. « The label equals the feed row's own title » is held by `PushFanOutTests` comparing the two
  rows one call produces — not by a retyped table here.
- **`Common/Services/StockConsumptionService`** (`IStockConsumptionService`) - draws an act's material list out of stock when its fiche is saved (AC-P4.10). Called **post-commit** from both dental-record handlers and best-effort in the strict sense: the whole run is wrapped in one swallow-and-log, so a stock failure can never fail or roll back the clinical record (AC-P4.13) - same contract as `INotificationGenerator`. Draws FEFO, writes one `StockMovement` per item, and **surfaces a shortfall as a low-stock notification rather than blocking**: the visit already happened, so on-hand goes negative instead of clamping to zero and losing the discrepancy (AC-P4.12). An act with no material list is a genuine no-op that reads nothing (AC-P4.11). ⚠️ On the **edit** path `UpdateDentalRecordCommand` passes only the acts the edit **added** - a fiche is re-saved routinely, and re-consuming the whole list each time would draw stock for materials already used.
- **`Common/ClinicClock.cs`** — the clinic's wall clock (Tunisia **UTC+1**, no DST) and, since P6, the **single authority on what a query means by "today"** (§§ 4.1/4.2, AC-P6.1–6.4). `ToClinicLocal`/`ToUtc`/`ClinicToday`/`ClinicYear`, `StartOfLocalDayUtc`/`EndOfLocalDayUtc`, plus **`LastTickOfLocalDayUtc`** / **`LocalDayRangeUtc`** / **`TodayRangeUtc`**. All the bound helpers return an **explicit UTC instant** — `ApplicationDbContext` treats `Unspecified` as UTC on write, so handing a bare local value to a query would shift every boundary by an hour.
  ⚠️ `EndOfLocalDayUtc` is the *next* midnight (**exclusive**) while every money read is inclusive on both ends, so `LastTickOfLocalDayUtc` is what those callers want — the exclusive bound counts a midnight payment in **both** adjacent periods (finding #20). `DashboardPeriod` delegates its own tick subtraction here rather than keeping a second copy.
  It was written in P1 claiming to replace the two private `ResolveTunisiaTimeZone()` copies (A-21) and **did not** — `NotificationGenerator` and `ReminderScheduler` still carried them until P6 actually retired them. The current state is one implementation, verified by the symbol returning zero hits outside this file.
  Consumers of the local-day rule: `GetCaisseSummaryQuery` (its no-argument default), `GetPatientBillingSummaryQuery`, `GetReceivablesQuery`, `GetPatientsToRecallQuery`, the two CNAM estimate queries, `DashboardPeriod`, `WorkingHoursResolver`, and `AIActionService` (Infrastructure). Consumers of `ClinicYear`: the **three** money-document sequences — `IssueInvoiceCommand`, `AcceptTreatmentPlanCommand`, `CreateCreditNoteCommand`.
- **`Features/Appointments/AppointmentScheduling.cs`** — the one place the overlap rule and the hours check live, read by create / update / recurring. The predicate previously existed in **three** copies that had already drifted (the recurring one excluded only `Cancelled`, not `NoShow`). Its `OccupiesSlot` is deliberately the same set the database's partial exclusion constraint uses so guard and constraint cannot disagree; its scan window no longer misses an appointment that began >24 h earlier (A-3).
- **Maintenance** (`Common/Maintenance/`) — `AdminPasswordRecoveryService`: offline admin-lockout recovery core (find admin → temp password → activate + `SetPassword` → persist). **Deliberately NOT DI-registered** (no HTTP-reachable reset); driven only by the API `reset-admin-password` CLI. Here so `UnitTests` (which references only Application) can test it.
- **Exceptions** (`Common/Exceptions/`) — `NotFoundException`, `ForbiddenAccessException`, **`ConflictException`**, and `ExceptionMiddleware` (ASP.NET middleware): `ForbiddenAccessException`→403, `NotFoundException`→404, **`ConflictException`→409**, `FluentValidation.ValidationException`→400, else→500. ⚠️ `ConflictException` must reach the middleware, so every handler catch-all that **returns a Result** carries `when (ex is not ConflictException)` (152 of them). A catch that only *logs* deliberately does **not** — it is a best-effort post-commit side effect and the core operation has already committed. All emit the canonical `{ "error": "<message>" }` body; the generic fallback comes from `Common/ErrorMessages.cs` (shared with the API `ApiControllerBase`).
- **Authorization** (`Common/Authorization/`) — `AuthorizationPolicies.ConfigurePolicies(options, isLocalMode)` defines **four** policies, and since `adoption-qa-i-access-control-and-audit` (I1) **every one of them is applied somewhere**: `Authenticated` (authenticated, no role — the onboarding surface, which in Cloud is reached before the role is in the JWT at all), `AnyClinicRole`, `AdminOrDoctor`, `AdminOnly`. `DoctorOnly`/`SecretaryOnly`/`DoctorOrSecretary` are **deleted** — they had zero usages for the product's whole life while 33 endpoints carried a bare `[Authorize]`, and they stayed green because the guard test only asserted a policy *existed*. The replacement guard asserts the defined set equals the **applied** set in both directions, which is only satisfiable with no exemption list if every policy is genuinely used, so an unapplied policy is deleted rather than parked. ⚠️ `AnyClinicRole` includes `admin` **deliberately** and that is why it replaced `DoctorOrSecretary`: `CreateClinicCommand` makes a clinic's creator an admin and links the single dentist's `Doctor` record to that same account, so a literal `{doctor, secretary}` policy on the agenda or the till would lock the owner out of their own practice. The secretary exclusion the feature is actually about is carried entirely by `AdminOrDoctor`. ⚠️ **What that exclusion covers was narrowed, reversing an I1 decision:** `AdminOrDoctor` no longer means « clinical authorship and clinical free text » — it is **clinic-wide money, the corrective money operations, deleting from the clinical record, and the bulk/lifecycle patient operations**. The clinical record itself (fiches de soins, odontogramme, antécédents, medical documents) is `AnyClinicRole` for reading and recording, because in a Tunisian cabinet the assistant(e) fills much of it in — and because the old boundary was never true of the code around it: `PUT /api/patients/{id}` is `AnyClinicRole` and writes `Allergies`/`MedicalHistory`/`Notes`, and `POST /api/patients` inserts `PatientMedicalHistory` rows, so reception could always type a patient's history through « Modifier » while being refused a *read* of it one tab over. **Record yes, erase no**; and the wider write surface is accountable because `AuditSaveChangesInterceptor` attributes every mutation while `PractitionerAttribution` puts the caller last, so a secretary recording a dentist's work never credits themselves. In **Local** mode it installs a fail-closed `FallbackPolicy = RequireAuthenticatedUser()` (endpoints without `[AllowAnonymous]` → 401); in **Cloud** the fallback stays null. `Requirements/RoleRequirement` (`params string[] AllowedRoles`) + `Handlers/RoleAuthorizationHandler` (reads role from `https://clinic-management.com/role` / `role` / `ClaimTypes.Role`).
  ⚠️ **`AllowsWithoutSubscriptionAttribute` lives here too and is not authorization** — it grants nothing and never
  widens a policy. It declares « this endpoint keeps working on a cabinet whose entitlement has ended » (FR-3), read
  from endpoint metadata by the API's `SubscriptionGateMiddleware`. It sits beside the policies because it is the same
  kind of thing — a per-endpoint declaration the controllers annotate themselves with — and they already import this
  namespace. **The reason is a mandatory constructor argument**: the exempt set is stated as *what each endpoint is*,
  so every entry has to answer « why may an unpaid cabinet still do this? » where a reader finds it, and a bare
  boolean flag would let the set grow by copy-paste.
- **Constants** — `ErrorMessages` (`Generic`, **`Conflict`**, **`RepeatedConflict`**), `PasswordPolicy` (`MinLength = 8`), `PaymentMethodLabels` (French `PaymentMethod` labels for receipts).
- **Maintenance** (`Common/Maintenance/`) also holds **`MoneyReconciliationService`** — the core of the
  `reconcile-money` console verb (nine cross-checks over both payment ledgers, plan échéanciers, monthly
  attribution old-vs-new, orphans, sentinels, over-credited invoices, duplicate bridge invoices). Like
  `AdminPasswordRecoveryService` it is **deliberately not DI-registered**: there is no HTTP-reachable money
  report, and it lives here so `UnitTests` can exercise it.
- **`Common/Maintenance/SchemaVerificationService`** — the core of the **`verify-schema`** console verb, and the
  only gate for a schema-level change (nothing in `UnitTests` touches a database, so a migration is the one class
  of change the suite structurally cannot verify). Reads both sides through `ISchemaVerificationReader` — what the
  **EF model** declares vs. what PostgreSQL's catalog **has** — and diffs them, so an index or FK added in a
  configuration file is checked without editing this service. Only what the model cannot express is named in it:
  `btree_gist`, the exclusion constraint's **partiality**, the two `(5,2)` rate-column exceptions, and the
  data-migration row counts — and, since `clinic-subscription` Part A, the three **cabinet-entitlement** checks:
  `every-clinic-has-an-entitlement` (a **derived count over every cabinet**, never a list of known doors, because the
  failure it exists to catch is a *third* clinic-construction door added later), `subscription-grandfathered-entries`
  (**Info with its count, never asserted** — « grandfathered == cabinets » would go red on the deployment's first
  signup, so AC-6.4's equality belongs to the prescribed before/after **diff**), and
  **`subscription-end-date-matches-ledger`**. ⚠️ That last one is the only check here that reads **rows rather than a
  count**, and the reason is R-6: comparing the stored `EndsOn` against the fold *in SQL* would mean re-expressing
  the exclusive-cursor arithmetic as a recursive CTE — a second copy of the one thing that must stay single, in a
  language where no compiler checks it against the first. So `ISchemaVerificationReader` projects
  `ClinicSubscriptionLedgerFact` and this service calls the **real** `SubscriptionLedger.Fold`. Also
  **`procedure-type-category-move`**, whose failure mode is exactly the kind
  only this verb can see: the column, the API and the UI can all be correct while an act's discipline is still
  sitting in its `Description`, and such an act renders as merely *unfiled*, indistinguishable from one nobody ever
  categorised (each nullable, so a part that has not run yet reports « not applicable » rather than
  a misleading `0`) — and, since L8, **`cheque-details-only-on-cheques`**, which is the clearest illustration of
  what belongs in this file and what does not: the cheque migration's *shape* (six columns, two widths, two partial
  indexes) is diffed against the catalog for free and is therefore named nowhere here, while « a cheque number only
  ever appears on a cheque » is a **domain** invariant (`ChequeDetails.For`), deliberately not a CHECK constraint,
  and so has to be verified rather than enforced twice. Same not-DI-registered rule as the two services above.

## Gotchas
- **No FluentValidation validators exist** — `ValidationBehavior` finds none; validation is inline via `Result.Failure`.
- Clinic scoping is DB-resolved (`User.ClinicId`), not the JWT `clinic_id` claim — including the EF filter's, since US-2. `ICurrentClinicProvider` projects `ITenantScope`, and an **unset** scope now returns **no rows** rather than switching the filter off. The per-handler DB check is still the authoritative guard (and the only one for the seven `ClinicId`-less clinical tables).
- **`ITenantScope` is single-assignment in both directions** — `UseClinic` then `UseSystemWide` throws, and so does the reverse. A widening call is how a one-clinic path silently becomes cross-clinic; per-clinic narrowing inside an iterating job needs a child scope. It is registered in **`AddInfrastructure`**, not here, so the console verbs (which build their container from that method alone) can declare themselves.
- Post-commit side effects (`INotificationGenerator`, `IReminderScheduler`, `IAppointmentGoogleSyncDispatcher`) are best-effort: they run after `SaveChangesAsync`, swallow/log failures, and never roll back the core operation.
- `CreateClinicCommand`/`JoinClinicCommand` are dual-path: a non-null `Password` switches to the Local first-run / self-registration branch; null keeps the Cloud/Auth0 flow. Only Local `AuthController` `setup`/`register` set `Password`.
- Some handlers swallow non-critical failures (e.g. Auth0 metadata update) so the core use case still succeeds.
- `IHuggingFaceAIService` is the sole wired AI backend. `IGoogleTokenStore` no longer exists (retired).
